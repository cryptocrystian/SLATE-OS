# Normalize all text files to LF in the repository and on checkout.
# Prevents CRLF drift from editors on Windows/WSL.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
*.woff binary
*.woff2 binary
