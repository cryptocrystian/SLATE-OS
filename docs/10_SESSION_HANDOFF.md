# SLATE Session Handoff

Use this doc when picking up SLATE work in a new session. It captures repo state, where things live, and how to start the next sprint without re-reading every canon doc end-to-end.

---

## Where We Are

Sprints 1–7 are complete. MVP Stabilization closed cleanly. The MVP Acceptance Audit returned 4.8/5 and approved the surface as the baseline. The Persistence/Auth architecture canon is drafted in `docs/persistence/`. **Persistence/Auth Steps 0–10 plus AI Synthesis Steps 1, 1.1, and 2 are complete and ship build-clean against a real Supabase project.**

**Latest (2026-06-04 — Sprint S5: Findings Approval Polish):** Implementation sprint per `docs/39` § 5. Polishes the operator findings approval/rejection lifecycle against S4's evidence-aware draft findings model. Source-clean + build-clean + boundary-clean. Full evidence: `docs/44_FINDINGS_APPROVAL_POLISH.md`. New pure-function helper `lib/findings/provenance.ts` (~250 lines) exports `summarizeFindingProvenance(refs, assumptionFlag)` (returns per-lane ref counts + dominant strength + needs-validation flag with reason code) and `buildOpportunitiesReadinessSignal(findings, options?)` (returns approved/rejected/draft/approvedNeedsValidation counts + `readyForS6` boolean at default threshold 5 + advisory warnings list). `summarizeFindingProvenance` decision rules: (a) `assumption_flag=true` → needs-validation `assumption-flagged`; (b) `totalRefs=0` → `no-refs`; (c) all refs `thin/missing` → `all-thin-or-missing`; (d) only scorecard + consultant refs (no stakeholder voice or document) → `only-engagement-context`; else clean. `components/findings/findings-workspace.tsx` renders new `FindingProvenanceChip` — compact mode on card list (single "Needs validation" warning chip when applicable; else nothing rendered), full mode on detail view (per-lane counts Stakeholder/Document/Scorecard/Consultant + dominant-strength chip + needs-validation badge with reason). `components/findings/review-action-bar.tsx` gained: (a) needs-validation warning panel above buttons for non-terminal findings (withdraws once approved / rejected / report-ready so it stops nagging settled records); (b) two-step rejection-reason flow — first click on `Reject…` opens reason textarea (10–500 chars or empty allowed; live char counter, browser maxLength=500); second click `Confirm reject` submits with `{reason?: string}`. `lib/findings/actions.ts` refactored — `setReviewStatus(id, status, options)` now accepts `{rejectionReason?}`; validates 10–500 chars or empty (returns `rejection-reason-invalid` on overrun); persists reason in `findings.reviewer_note` only when provided (never overwrites existing notes with null); emits sanitized `provenance: {totalRefs, stakeholderResponseRefs, uploadedDocumentRefs, scorecardAnswerRefs, consultantNoteRefs, dominantStrength, needsValidation}` + optional `rejectionReason` (only when status=rejected AND operator supplied one) in activity event metadata. Existing `reviewStatus / priorStatus / confidence / aiDrafted` fields preserved. `rejectFinding` signature widened to `(id, options: RejectFindingOptions = {})` — backward compatible. Defensive `mapDbSourceType` + `mapDbStrength` helpers fall back to `"consultant-note"` / `"thin"` on unknown DB enum values rather than crashing. Activity-metadata safety: **zero** raw `excerpt`, raw `source_label`, raw stakeholder name/email/role, raw answer text, raw reviewer-note text, token values, UUIDs other than the finding-id itself, `attio_company_id`, or external system identifiers. Only operator-typed string in metadata is the optional bounded `rejectionReason` (operators who want to keep rationale internal can leave the field empty). New `components/findings/opportunities-readiness-hint.tsx` (~130 lines) operator-only server component (zero client bundle cost) renders 4 stat tiles (Approved / Rejected / Still in review / Approved · needs-validation) + Ready/Not-yet status chip + first-4 advisories from the helper + boundary footer ("Sprint S6 opportunities drafting consumes approved findings…"). Read-only; does NOT block S6 (S6 not yet built); does NOT mint anything; does NOT contact a client. `app/app/engagements/[id]/findings/page.tsx` wires the hint below `CreateFindingForm` for persisted engagements; signal built inline from already-fetched findings array (no new DB fetch). `components/findings/create-finding-form.tsx` local `translateError` union widened to include `rejection-reason-invalid` for compile-time correctness (the form's own actions never surface it). **Both candidate engagements hold 0 persisted findings** — SLATE Pilot Test Client (`ed7f1f7d-…`) had its 8 audit-labelled responses correctly excluded by S4's boundary so zero findings were ever drafted; Sapient Digital (`76097653-…`) has not progressed past Stage 1 (Setup) with no intake responses. **Live lifecycle exercise deferred** until the first real finding lands on any engagement — operator either authors via `CreateFindingForm` OR runs S4 synthesis with an audit-logged `overrideReason` against non-audit data. Polish is build-validated against the existing `Finding` type contract and pure-logic-smoke-validated against the new helpers via `artifacts/s5-provenance-smoke.mjs` — 9/9 cases pass (5 provenance: strong-evidence clean / empty-refs flag / assumption-flagged flag / all-thin flag / only-engagement-context flag; 4 readiness: empty / 3-approved-below-threshold / 5-approved-at-threshold / 5-approved-all-needs-validation). Source-review validation of the lifecycle wiring traced approve / mark-report-ready / reject-no-reason / reject-with-reason / reject-bad-length / reopen / edit-note paths through the modified server action and confirmed: (a) all transitions preserve workspace-scoped RLS via cookie-bound auth, (b) no service-role usage anywhere, (c) no cross-workspace mutation possible, (d) bad-length reason returns `rejection-reason-invalid` to UI without state mutation, (e) empty reason is allowed and produces a rejection with no `rejectionReason` field in metadata. Lint clean ✅; production build clean ✅ — `/app/engagements/[id]/findings` route grew **12 kB → 13.3 kB First Load JS** (+1.3 kB; provenance chip + rejection-reason textarea); all other 28 routes byte-identical; `npm run check:send-to-client-disclaimers` ✅. Boundary scan clean: zero `mailto:` / `sendgrid` / `nodemailer` / `docusign` / `hellosign` / `adobesign` / `crm_push` introduced; zero `/r/` or `/p/` mint paths introduced; zero `service_role` introduced; zero Attio writeback functions introduced. **Boundary confirmation:** zero `/r` or `/p` mint, zero Send to Client, zero public SOW route, zero SOW share tokens, zero email send, zero CRM writeback, zero Attio writes, zero e-signature, zero opportunity scoring/drafting (S6 scope), zero roadmap generation (S7), zero report-section generation (S8), zero proposal generation (S9), zero Group-B claims, zero Sapient mutation, zero new package dependencies, zero new public routes, zero new migrations, zero roadmap sequence change. Files modified: 3 new (`lib/findings/provenance.ts`, `components/findings/opportunities-readiness-hint.tsx`, `docs/44`); 5 source-modified (`lib/findings/actions.ts` +~120, `components/findings/findings-workspace.tsx` +~80, `components/findings/review-action-bar.tsx` +~120, `components/findings/create-finding-form.tsx` ~+5, `app/app/engagements/[id]/findings/page.tsx` ~+30); 4 docs (`docs/39` S5 row landing notes, `docs/43` S5 cross-reference, `docs/08` S5 block at top, `docs/10` this paragraph). Throwaway `artifacts/s5-provenance-smoke.mjs` lives in gitignored `artifacts/`. **Next-planned milestone:** Sprint S6 — Opportunities AI Drafting + Quadrant Approval per `docs/39` § 5. Roadmap sequence unchanged. Operator-side prerequisites that do NOT block S6 source work but ARE preconditions for a live drafting exercise: (1) real findings must exist on the target engagement (today both candidates hold 0; operator-authors via `CreateFindingForm` OR runs S4 with `overrideReason` on non-audit data); (2) apply migration 0018 to deployed Supabase (Sprint S3-B carry-over) IF S6 wants to surface CRM context inside opportunity drafts (optional but recommended).

**Prior (2026-06-02 — Sprint S4: AI Findings Synthesis Integration):** Implementation sprint per `docs/39` § 5. Wires lane-attributed intake evidence + readiness gate + operator-override path into the existing findings synthesis pipeline. Source-clean + build-clean. Full evidence: `docs/43_AI_FINDINGS_SYNTHESIS_INTEGRATION.md`. New `lib/findings/evidence.ts` (~450 lines) pure server-only `buildEvidenceBundleForEngagement(engagementId)` filters `stakeholder_responses` to `response_status='ready_for_synthesis'` and buckets by canonical lane per `docs/39` § 4: primary `live_link` / secondary `transcript` + `meeting_notes` / tertiary `offline_operator` (`operator_entered` + `email_paste` + `document_upload`); excludes audit-labelled rows via prefix-match heuristic (`I3 WALKTHROUGH TEST`, `S1 AUDIT`, `S2 AUDIT FIXTURE`) so fixture/walkthrough data cannot accidentally enter synthesis; emits per-lane buckets + role coverage (compatible with the S1 readiness helper) + question coverage + CRM context summary via `getCrmContextForEngagement` (engagement-level only, never per-stakeholder) + warnings + the S1 readiness verdict. `lib/ai/findings-context.ts` extended with `evidenceBundle` field and invokes the aggregator alongside the legacy intake fetch. `lib/ai/findings-synthesis.ts` system prompt extended with explicit lane hierarchy: PRIMARY = live-link first-hand signal (high confidence); SECONDARY = transcript with speaker attribution (solid signal), without speaker attribution (moderate); SECONDARY ENRICHMENT = CRM context (engagement-level metadata only — NEVER attribute a stakeholder-shaped claim to CRM data); TERTIARY = offline_operator requires corroboration (when ALL evidence is tertiary-only the finding MUST be `assumptionFlag=true` with explanatory `assumptionNote`). Strength rule tightened: `'strong'` now requires multi-lane corroboration. User-payload extends with the lane-bucketed bundle alongside legacy intake shape. `lib/findings/synthesis-actions.ts` enforces the readiness gate — synthesis blocked when `bundle.readiness.ready === false` with `{ok: false, error: 'intake-readiness-not-met', readinessReasons: [...]}`. Operator can bypass with `overrideReason` (10–500 chars; sanitized server-side, length-capped, persisted in activity event metadata as `overrideReason: <string>` + `overrideApplied: true` + `ai_synthesis_runs.input_summary.overrideApplied=true`). New error codes: `intake-readiness-not-met`, `override-reason-invalid`. Activity event `ai_findings_generated` metadata extends with `evidenceLanes: {liveLink, transcript, offlineOperator, crmLinked}` counts + `overrideApplied` boolean + optional `overrideReason` string. ZERO raw `answer_text`, `stakeholder_email`, `entered_by`/`created_by` UUIDs, or `attio_company_id` raw value anywhere in activity metadata. `components/findings/generate-findings-form.tsx` extended with FindingsEvidenceSummary prop + evidence panel (4 stat tiles: live-link / transcript / offline_operator / required-roles-covered ratio; CRM chip showing brand or `not linked`; status chip showing `Ready` or `Below readiness threshold`; up-to-5 warnings list; gate-blocker reasons when applicable) + operator-override textarea (only surfaced when gate blocks; min 10/max 500 char live counter). Generate button label changes to **"Generate with operator override"** when override is in flight. Success banner surfaces `overrideApplied` state when present. `app/app/engagements/[id]/findings/page.tsx` fetches `buildEvidenceBundleForEngagement` in the same `Promise.all` as existing findings + candidates and passes the summarized bundle to the form. Controlled fixture validation on SLATE Pilot Test Client (engagement `ed7f1f7d-…`, Sapient Digital NOT touched): all 8 ready responses on the fixture are audit-labelled (7 from Sprint S1 + 1 from Sprint S2) and the aggregator correctly excludes all 8 — zero reach synthesis — readiness gate correctly blocks. This is the intended boundary behavior. Pure-logic smoke (`artifacts/s4-evidence-smoke.mjs`) on an 11-row synthetic sample confirms exclusion + bucketing + role-coverage match spec. **Live AI provider call NOT performed in this sprint** per task spec ("do not fake success"); the AI path is source-clean + build-clean and would fire if the gate cleared (verified: `OPENAI_API_KEY` present in `.env.local`, `isAiConfigured()` returns true). Findings stay `review_status='needs_review'` (draft) per pre-existing canon — synthesis NEVER auto-approves. Boundary confirmation: zero `/r` or `/p` mint, zero Send to Client, zero public SOW route, zero SOW share tokens, zero email send, zero CRM writeback, zero Attio writes, zero new CRM connector, zero e-signature, zero opportunity scoring (Sprint S6 scope), zero roadmap generation (S7), zero report section generation (S8), zero proposal generation (S9), zero Group-B claims, zero Sapient mutation, zero new package dependencies, zero new public routes, zero docs/39 sequence change. Lint clean ✅; production build clean ✅ — `/app/engagements/[id]/findings` route grew **10.8 kB → 12 kB First Load JS** (+1.2 kB, evidence panel + override input); all other 28 routes byte-identical; `npm run check:send-to-client-disclaimers` ✅. `docs/35` § 5 alignment note added — S4's audit-label exclusion preserves the readiness-gate "no test data" intent at the code layer alongside the operator-discipline layer that has held since docs/35 landed. Files modified: `lib/findings/evidence.ts` (new ~450 lines), `lib/ai/findings-context.ts` (+~25), `lib/ai/findings-synthesis.ts` (+~50 prompt + payload), `lib/findings/synthesis-actions.ts` (+~50 readiness gate + override), `app/app/engagements/[id]/findings/page.tsx` (+~30 bundle fetch + form props), `components/findings/generate-findings-form.tsx` (+~180 evidence panel + override input), `docs/43` (new ~520 lines), `docs/39` (S4 row landing notes), `docs/35` (S4 alignment note), `docs/08` (Sprint S4 block at top), `docs/10` (this paragraph). Throwaway artifact `artifacts/s4-evidence-smoke.mjs` lives in gitignored `artifacts/`. **Next-planned milestone:** Sprint S5 — Findings Approval Polish per `docs/39` § 5. Roadmap sequence unchanged. Operator-side follow-ons that do NOT block S5: apply migration 0018 to deployed Supabase (Sprint S3-B carry-over), create the 9 missing Attio Company custom properties (Sprint S3-B carry-over), and optionally run a live AI synthesis exercise against real non-audit data to observe S4's lane attribution in practice.

**Prior (2026-06-02 — Sprint S3-B: Attio Read Context Implementation):** Implementation sprint per `docs/42` § 12. Source-clean + build-clean + boundary-clean. Full evidence in `docs/42` Sprint S3-B landing note. **Two operator corrections accepted before kickoff** and persisted into the canon: env var name is `ATTIO_ACCESS_TOKEN` not `ATTIO_API_KEY` (Attio's own terminology — `docs/42` § 16.4 updated); workspace name is `Saipien Labs` with slug `saipien-labs`, not `Saipien Labs HQ` (operator renamed before token provisioning — `docs/42` § 16.1 updated; the slug is hard-coded into the mapper's Attio permalink builder). Attio API connectivity verified via `artifacts/s3b-attio-probe.sh` (gitignored, cleans up bodies on completion; token never echoed): `/v2/self` returned HTTP 200 with workspace `Saipien Labs` / `saipien-labs` matching operator-provided values exactly; `workspace_id: a48799e0-5a0b-46d5-a425-1bbcf202dcc1`; `token_type: Bearer`; `Companies` (object_id `dcd7c861-…`), `People`, and `Deals` objects all reachable. Property audit vs `docs/42` § 10 required list — of the 32 standard Attio Company attributes, only `last_interaction` (Last Touch) and `strongest_connection_user` (Relationship Owner proxy) are present; 9 of the 11 required custom properties are missing at S3-B kickoff (Brand, SLATE Account ID, SLATE Engagement ID, Lead Source, Pipeline Stage better on Deal, Deal Value better on Deal, Known Pain Points, Buying Timeline; plus Notes which uses a different `/v2/notes` model). **Per task spec, missing properties do NOT block S3-B implementation** — mappers handle missing properties gracefully with `CrmContextWarning` entries; UI renders "missing in Attio" hints; the same code path renders the full context with zero warnings once the operator creates the fields (~30 min operator-side step). Code shipped: migration `0018_accounts_attio_company_id.sql` (additive nullable text column + partial index; no CHECK, no RLS change); `lib/crm/types.ts` provider-neutral `CrmContext` + `CrmAccountContext` + `CrmContactContext` + `CrmDealContext` + `CrmActivityContext` + `CrmContextWarning` + `CrmContextSourceStatus` (linked / not-linked / fetch-failed / not-configured); `lib/crm/queries.ts` server-only `getCrmContextForEngagement(engagementId)` with workspace-scoped Supabase read preceding any outbound Attio fetch so an unauthorized caller can never trigger a CRM request for a foreign account; `lib/crm/actions.ts` `"use server"` `linkAccountToAttioCompanyAction` (cookie-bound auth + workspace-scoped write to `accounts.attio_company_id` only; NEVER writes to Attio; activity event metadata `{accountId, attioCompanyId}` only); `lib/crm/attio/client.ts` server-only client with `attioGet` + `attioPostQuery` (guarded to documented `/records/query` + `/notes/query` endpoints only; **no PUT/PATCH/DELETE helpers exist anywhere**); `lib/crm/attio/{types,companies,people,deals,mappers}.ts` connector internals with soft-fail on 400/404 (attribute-slug mismatch, missing Deals object, etc.); `components/engagements/engagement-attio-context-card.tsx` server-rendered four-state UI (silent when not-configured / link form when not-linked / broken-link warning when fetch-failed / brand chip + lead source + buying timeline + company name / domain / owner / last touch grid + known-pain-points preview + deals summary + contacts summary + missing-attribute warnings + "View in Attio" external link when linked); `components/engagements/link-attio-company-form.tsx` minimal client form for pasting Attio Company `record_id` with "Read-only. SLATE will not write to Attio." copy verbatim above submit; `app/app/engagements/[id]/page.tsx` wires the new card into the sidebar after `EngagementContextCard` for persisted engagements; `lib/activity/types.ts` adds `account_linked_to_attio` event + new `account` entity type; `components/activity/activity-timeline.tsx` adds info tone + "Account linked to Attio" label. **Boundary scan passes all 4 checks** (`artifacts/s3b-boundary-scan.sh`): zero `ATTIO_ACCESS_TOKEN` refs in `.next/static`; every CRM server module marked `server-only` or `"use server"`; zero PATCH/PUT/DELETE patterns in Attio connector; zero UI imports from `lib/crm/attio/` (UI imports only the provider-neutral `lib/crm/types.ts`). Lint clean ✅; production build clean ✅ — `/app/engagements/[id]` route grew 143 B → 1.66 kB First Load JS (+1.5 kB, only the link-form client component; connector + queries are server-only and ship 0 bytes to client); all other 28 routes byte-identical; `npm run check:send-to-client-disclaimers` ✅. **Migration 0018 NOT yet applied to deployed Supabase** (verified via `information_schema.columns` lookup against `public.accounts.attio_company_id` — no rows); live walkthrough deferred per task spec ("If migration 0018 is not applied to deployed Supabase: do not live-test the link action on staging; source/build test only; document operator deployment step"). Operator-side prerequisites before live context fetch surface activates: (1) apply migration `0018_accounts_attio_company_id.sql` to deployed Supabase project `hhglrcvsmwaheikdvijw` (Sprint I3 walkthrough used the Supabase MCP `apply_migration` pattern — same pattern works here); (2) create the 9 missing Attio custom properties in the Saipien Labs workspace (Brand, SLATE Account ID, SLATE Engagement ID, Lead Source, Known Pain Points, Buying Timeline on Company; Pipeline Stage + Deal Value on Deal; optional curated `relationship_owner` on Company); (3) Vercel Production promotion to pick up the new card. **Boundary preserved:** zero Attio writes, zero creation of Attio records, zero mutation of Attio properties, zero pushing SLATE leads into Attio, zero background sync, zero webhooks, zero OAuth flow, zero multi-CRM support, zero HubSpot/GHL fallback code, zero email/CRM automation, zero findings synthesis, zero `/r` or `/p` minting, zero Send to Client, zero public SOW route, zero Group-B wiring, zero new package dependencies, zero Sapient mutation, zero roadmap sequence change. Files modified: 9 new (`supabase/migrations/0018_accounts_attio_company_id.sql`, `lib/crm/types.ts`, `lib/crm/queries.ts`, `lib/crm/actions.ts`, `lib/crm/attio/{client,types,companies,people,deals,mappers}.ts`, `components/engagements/engagement-attio-context-card.tsx`, `components/engagements/link-attio-company-form.tsx`); 3 source-modified (`app/app/engagements/[id]/page.tsx`, `lib/activity/types.ts`, `components/activity/activity-timeline.tsx`); 4 docs (`docs/42` Sprint S3-B landing note added at end + § 16.1 + § 16.4 corrected, `docs/39` S3-B row updated with landing notes, `docs/08` Sprint S3-B block added at top, `docs/10` this paragraph). Throwaway artifacts (`s3b-attio-probe.sh` + `s3b-boundary-scan.sh`) live in gitignored `artifacts/`. **Next-planned milestone:** Sprint S4 — AI Findings Synthesis Integration per `docs/39` § 5. Roadmap sequence unchanged.

**Prior (2026-06-02 — Sprint S3-A: Attio CRM System-of-Record Canon):** Canon / planning sprint splitting the original Sprint S3 in the locked Consulting Module Completion Roadmap (`docs/39`) into **S3-A (this canon — landed)** and **S3-B (Attio Read Context Implementation — recommended next)**. Full evidence: `docs/42_ATTIO_CRM_SYSTEM_OF_RECORD_CANON.md` — 18 numbered sections covering decision summary, Attio rationale, HubSpot comparison (decision-NOT-re-opened), SLATE-as-system-of-record posture, read-only role boundary, CRM context hierarchy (engagement-level only, never per-stakeholder findings evidence unless explicit operator promotion), Saipien single-workspace model, Brand / Business Unit tagging (canonical initial list: Saipien Labs Consulting, Venture Studio, Sapient Digital, Ventrys, Pravado, Aivery, Other), object mapping (SLATE Account ↔ Attio Company via new `accounts.attio_company_id text null`; SLATE Stakeholder ↔ Attio Person optional/deferred; SLATE Engagement ↔ Attio Deal optional/deferred), 11 required Attio properties (Brand, SLATE Account ID, SLATE Engagement ID, Lead Source, Pipeline Stage, Deal Value, Last Touch, Relationship Owner, Known Pain Points, Buying Timeline, Notes/Recent Activity pointer), required SLATE fields (`attio_company_id` required for S3-B; `attio_person_id` + `attio_deal_id` deferred), S3-B implementation scope (`lib/crm/attio/` connector module, internal `CrmContext` shape, optional migration `0018`, `lib/crm/queries.ts` server-only fetcher, EngagementContextCard enrichment, `linkAccountToAttioCompanyAction` server action, `account_linked_to_attio` sanitized activity event), provider-neutral adapter boundary (UI imports `lib/crm/types.ts` only; Attio-specific mapping confined to `lib/crm/attio/`; NO multi-CRM build, NO connector marketplace, NO productized abstraction), migration/exit strategy (Attio IDs stored separately, SLATE remains canonical, future HubSpot/GHL migration possible via mapper swap + DB column rename), nine deferred items (writeback Sprint S18+, lead push, two-way sync, customer CRM integrations, CRM-triggered automation, email send via Attio, multi-brand pipelines beyond field/tag, multi-CRM connector marketplace never, Attio note → stakeholder_responses promotion Backlog Improvement), and five open operator decisions before S3-B (workspace name, final Brand values, custom-field creation timing, API-key-vs-OAuth recommendation API-key for single-workspace internal use, `attio_company_id` migration timing recommended inside S3-B). Deterministic-lookup posture: `accounts.attio_company_id` stored once at link time; subsequent fetches use the ID; domain-rematch is initial-linking-only; broken-link UI affordance surfaces when Attio IDs change upstream. `docs/39` § 5 updated minimally — Sprint S3 row split into S3-A (LANDED 2026-06-02) and S3-B (next), with full S3-B scope locked verbatim from `docs/42` § 12 so no drift mid-sprint; S4 (AI Findings Synthesis Integration) still follows S3-B with no other sequencing changes. **Zero source code changes. Zero data mutations. Zero migration runs. Zero Attio connection. Zero API key created. Zero engagement mutations. Zero `/r` or `/p` mint. Zero Send to Client. Zero send. Zero schema or package changes. Zero Sapient Digital interaction.** Files modified: `docs/42` (new ~430 lines), `docs/39` (S3 row split), `docs/08` (Sprint S3-A block added at top), `docs/10` (this paragraph). **All five operator decisions resolved this turn** (`docs/42` § 16 promoted from "Open operator decisions" to "Operator decisions — resolved"): (1) Attio workspace name = **`Saipien Labs HQ`**; (2) Brand property values locked at **Saipien Labs Consulting, Venture Studio, Sapient Digital, Ventrys, Pravado, Aivery, Future Brand** (the "Future Brand" placeholder replaces "Other" — operator renames when the actual sub-brand label is added); (3) Attio custom fields (the 11 properties listed in `docs/42` § 10) are created **manually by the operator before S3-B kickoff**, ~30 min operator-side; S3-B implementation still handles missing properties gracefully — any absent property renders as `null` in `CrmContext` without erroring; (4) Auth posture = **server-only `ATTIO_API_KEY` env var** set in Vercel Production + local `.env.local`, never bundled into client code, never echoed in logs, never committed; OAuth deferred indefinitely (extremely unlikely for an internal-only single-workspace connector); activity events that record link operations include only the `attioCompanyId` semantic field, never auth material; (5) Migration `supabase/migrations/0018_accounts_attio_company_id.sql` **lands inside Sprint S3-B** as a single review cycle rather than a separate micro-sprint — additive `add column if not exists`, nullable, no CHECK constraint, no RLS policy change (parent-row workspace scope inherits). No `attio_person_id` or `attio_deal_id` columns ship in S3-B; those remain deferred per `docs/42` § 11. **Next-planned milestone:** Sprint S3-B — Attio Read Context Implementation per `docs/42` § 18. Scope is locked verbatim from `docs/42` § 12; pre-conditions cleared. Operator-side prerequisite before Claude begins S3-B: create the 11 Attio properties in the Saipien Labs HQ workspace (~30 min) and provide the `ATTIO_API_KEY` value via the `printf '%s' "$VAL" | vercel env add` stdin pattern + local `.env.local` (never echoed to chat history).

**Prior (2026-06-02 — Sprint S2: Transcript / Notetaker Intake):** Second sprint in the locked Consulting Module Completion Roadmap (`docs/39`). Adds the secondary input lane per `docs/39` § 4.2: paste-first transcript / meeting-notes intake with deterministic segmentation. Full evidence: `docs/41_TRANSCRIPT_NOTETAKER_INTAKE_SPRINT.md`. Three new files (+ one wiring edit), zero new server actions, zero new migration, zero new package deps. `lib/intake/transcript-segmentation.ts` ships a pure-function `segmentTranscript(raw, options?)` — speaker-turn split first (≥2 distinct labels → split on each turn; matches `Speaker 1:`, `Alex:`, `Casey (Operations):`, optional leading timestamps), paragraph fallback on blank lines, sentence-chunk + hard-wrap when oversize. 1500-char default per-segment cap, 200-segment hard cap, truncation-aware. Same input → same output; safe to run server- or client-side. `components/intake/transcript-intake-panel.tsx` is the operator surface: paste textarea or `.txt`-drop (client-side `FileReader.readAsText`, 100,000-char ceiling), title + source-type (`transcript` / `meeting_notes`) form, live segmentation preview while typing, post-import per-segment review row with assign-stakeholder + assign-question dropdowns + Save-as-draft + Ignore controls. Mounted in `app/app/engagements/[id]/intake/page.tsx` inside the existing offline-intake section after `OfflineIntakePanel`, receiving `offlineSessions` for assignment. Reuses I2 action surface verbatim — `createEngagementIntakeDocumentAction` already accepts `source_type='transcript' | 'meeting_notes'` with `contentText` required; `createOfflineStakeholderIntakeSessionAction` + `createOfflineStakeholderResponseAction` already accept `source_type='transcript'` and default `client_visible=false`, `response_status='draft'`, `token_hash=null`, `sent_at=null`; `markStakeholderResponseReadyForSynthesisAction` + `voidStakeholderResponseAction` are lane-agnostic. Migration 0017 CHECK constraints already permit `transcript` across all three intake tables — no new migration. `docs/37` updated with reclassification note: transcripts and meeting-note imports are SECONDARY per `docs/39` § 4, not offline-tertiary; this file (`docs/37`) continues to govern operator-typed offline modes and the shared data-model contract. `docs/40` § 6.4 cross-references `docs/41` for the secondary-lane backing. Controlled walkthrough on `SLATE Pilot Test Client` (engagement `ed7f1f7d-…`, Sapient Digital NOT touched) staged a transcript-source stakeholder (`d85fcffa-…`); saved two transcript-source draft responses (`9e27e594-…` on `automation_wishlist` 190 chars, `dfba696f-…` on `handoff_pain` 152 chars); marked response 1 ready (`response_status='ready_for_synthesis'`); all four activity events sanitized with only `{sessionId, questionId, sourceType='transcript', responseStatus, supersedes, priorSupersededId}` — zero raw text, zero stakeholder name, zero email, zero operator-notes content. Local segmenter test (`npx tsx artifacts/s2-segment-test.mjs`) on the canonical 3-speaker-turn sample produced 3 speaker-turn segments with correct provenance (Speaker 1 + Speaker 2 detected; lengths 144 / 104 / 109; strategy `speaker-turn` for all three; no truncation). **Data path proven end-to-end via the already-deployed I3 offline-mode UI exercising the exact action chain the new panel calls.** The new transcript-panel UI itself is build-clean but not yet promoted to deployed Production (no Vercel promotion authorized this sprint per `docs/39` § 9 no-side-sprint discipline); next operator-authorized Vercel promotion picks up the panel automatically. Lint clean ✅; production build clean ✅ — intake route grew from 14.2 kB → 18.4 kB First Load JS (+4.2 kB transcript panel), all other 28 routes byte-identical. Send-to-client disclaimer pin ✅. Seven limitations (L-1 through L-7 per `docs/41` § 8) classified per `docs/39` § 11 as Improvement → Backlog or Deferred-Expansion → § 7: PII regex extension to transcript path (Improvement); transcript-source stakeholder creation FROM the panel (By-design — single-purpose); binary file upload backend (Per task spec); third-party notetaker webhook (Deferred-Expansion `docs/39` § 7); panel-UI walkthrough on deployed staging (Deployment posture); segment ↔ response cross-link table (By-design); English-centric sentence regex (Improvement). **No blockers; no roadmap reordering; no findings pulled into S2 scope.** Files modified: `docs/41` (new ~430 lines), `docs/39` (S2 row landing notes), `docs/40` (§ 6.4 cross-ref), `docs/37` (reclassification note), `docs/08` (Sprint S2 block at top), `docs/10` (this paragraph), `lib/intake/transcript-segmentation.ts` (new ~260 lines), `components/intake/transcript-intake-panel.tsx` (new ~530 lines), `app/app/engagements/[id]/intake/page.tsx` (+5 wire-up). `artifacts/s2-segment-test.mjs` thrown away (gitignored). All 13 boundary checks pass: zero new report/proposal tokens, zero delivery snapshots, zero Send-to-Client emissions, zero Sapient mutation, zero email / CRM / e-sign / mailto, `/s` + `/sow` continue to 404, live-link Mode A path untouched, transcript document + response `client_visible=false` throughout. **Next-planned milestone:** Sprint S3 — CRM Read Context per `docs/39` § 5. Scope locked: canon authoring for one CRM read connector; read-only OAuth/API-key auth; account-context fetch on engagement open; EngagementContextCard enriched with CRM fields. Non-goals: writeback (Sprint S18 — `docs/39` § 7 deferred-expansion), two-way sync, multi-CRM support, deal-stage automation, email (S17), synthesis (S4).

**Prior (2026-06-02 — Sprint S1: Online Intake Flow Audit + Stakeholder Intake Readiness):** First sprint in the locked Consulting Module Completion Roadmap (`docs/39`). Audited the canonical primary input lane (live-link `/intake/[token]` Mode A) end-to-end against the controlled fixture `SLATE Pilot Test Client` (engagement `ed7f1f7d-…`) — Sapient Digital was NOT touched. Operator-side `createStakeholderSession` minted stakeholder `1f79bc5e-…` with canon-correct DB shape (`source_type='live_link'`, `client_visible=true`, `token_hash` present, 21-day TTL via `token_expires_at`, `sent_at` = mint timestamp); activity event `intake_session_created` with sanitized metadata; UI surfaced the URL once with explicit "Email automation lands later" disclaimer; zero email sent. Public-side `/intake/[token]` route serves HTTP/2 200 with canon `cache-control: private, no-cache, no-store, must-revalidate`, renders role-specific welcome + 7 INTAKE_QUESTIONS, invalid token returns 200 + Invalid card (no leak via HTTP status). After submit: session `status='completed'`, `response_quality='strong'` (7 filled, 1522 chars); 7 response rows all `source_type='live_link'` + `response_status='ready_for_synthesis'` (default fired per canon — stakeholder typed it); activity event `intake_response_submitted` with sanitized `{responsesCount: 7, responseQuality: "strong"}` — zero PII, no raw token, no answer text. Three findings classified per `docs/39` § 11: (F-1, Improvement → Backlog) `/intake/[token]` lacks `X-Robots-Tag` + `Referrer-Policy` HTTP headers vs `/r`+`/p` canon — uses `<meta name="robots">` only — low-impact, future polish, recommended owner Sprint S17. (F-2, NOT a SLATE finding) Chrome MCP `computer.type` doesn't propagate text into React-controlled `<textarea>` elements — real-keyboard stakeholders unaffected; workaround documented (`_valueTracker.setValue('')` + native setter + `input` event + `form.requestSubmit()`). (F-3, Improvement → Backlog) Current 7-question `INTAKE_QUESTIONS` seed is a subset of the `docs/36` § 6 35-question packet; leaves business-model, target-customers, lead-source, document-inventory dimensions uncovered — reconsider in Sprint S5 when synthesis quality is observable. **No findings pulled into S1 scope; no roadmap reordering.** Readiness gate wording finalized (`docs/40` § 6) and aligned with `docs/39` § 4 hierarchy: minimum 3 of 6 required roles with ≥1 `ready_for_synthesis` response across any lane (live-link, transcript, or offline supplement cumulatively at the gate; per-lane weighting at synthesis follows § 4.5); minimum 14 cumulative ready responses; ≥1 supporting document OR operator no-documents acknowledgment; operator override path reserved for Sprint S11. Non-enforcing pure-function helper `lib/engagement-readiness/intake-readiness.ts` ships with the stable reason-code contract S11 will wire — `evaluateIntakeReadiness({roleCoverage, totalReadyResponses, totalDocuments, documentsClearedOrAcknowledged}) → {ready, missingRequiredRoles, reasons, highestLaneSignal, advisories}`. Pure function, no DB reads, no side effects, no UI mounts, zero call sites today. docs/35 § 5 updated with alignment note pointing at docs/40 § 6 for canonical wording; thresholds unchanged. All 17 boundary checks pass: zero new share tokens, zero delivery snapshots, zero Send-to-Client emissions, zero SOW events, zero email/CRM/e-sign wiring exercised, Sapient Digital `76097653-…` not touched, Smoke Test Co not touched, zero service-role SQL writes outside approved migration path (only read-only execute_sql SELECTs this sprint), zero source feature creep, roadmap sequence unchanged. Files modified: `docs/40` (new audit log, ~430 lines), `docs/35` (§ 5 alignment note added), `docs/08` (Sprint S1 block added at top), `docs/10` (this paragraph), `lib/engagement-readiness/intake-readiness.ts` (new — pure-function helper stub for S11). **Next-planned milestone:** Sprint S2 — Transcript / Notetaker Intake per `docs/39` § 5. Scope locked: canon authoring for transcript-import source-type subset (likely `docs/41`); transcript file upload backend (pulls forward previously-deferred Sprint I4 because transcript ingest is the actual use case for `engagement_intake_documents.storage_path`); per-segment extraction into `stakeholder_responses` with `source_type='transcript'` + `response_status='draft'`; "Import transcript" UI surface on the intake page with per-segment review + assign-to-stakeholder + assign-to-question. Non-goals: third-party notetaker webhook integration (file upload + paste only), CRM (S3), synthesis (S4).

**Prior (2026-06-02 — Consulting Module Completion Roadmap Lock):** Authored `docs/39_CONSULTING_MODULE_COMPLETION_ROADMAP.md` as the controlling roadmap for finishing the SLATE Consulting module without further reactive sprint drift. Canonical data hierarchy declared: **primary** = online stakeholder intake forms (`/intake/[token]` live-link Mode A); **secondary** = meeting transcripts / AI notetaker imports (currently absent — Sprint S2); **secondary** = CRM read context (currently absent — Sprint S3); **tertiary** = offline / manual operator entry (Sprint I3 path; now classified as support, not preferred). AI synthesis weighting rules codified (live_link + first_hand transcript full-weight; second_hand transcript + first_hand operator-entered moderate; inferred = "needs validation"; draft + voided discarded; CRM is engagement-level only). 13-sprint critical path frozen (S1 Online Intake Audit + Readiness → S2 Transcript/Notetaker → S3 CRM read → S4 AI Findings Synthesis (formerly Sprint I5) → S5 Findings Approval → S6 Opportunities → S7 Roadmap → S8 Report Section AI → S9 Proposal AI → S10 SOW Draft validation → S11 Pre-Delivery Audit code enforcement → S12 Sapient `/r`+`/p` mint → S13 Sapient Mark Sent + closure). 7-sprint MVP consolidation queued (S14 second real engagement → S15 time-to-deliverable optimization → S16 multi-engagement triage → S17 email send → S18 CRM writeback → S19 findings quality feedback → S20 post-delivery roadmap tracking). Deferred expansion lanes (canon-only, NOT scheduled): SLATE-mediated email send / CRM writeback / e-signature / public SOW share route / SOW template library — each with explicit pre-conditions before scheduling. No-side-sprint governance rule and Architect decision rule codified: discoveries route as blocker → current sprint, critical-path dependency → scheduled future sprint, improvement → backlog, expansion → canon only; operator (Architect) is the only authority that can reclassify; Claude proposes, operator decides. Claude role bounded to implementer + verifier + surfacer of blockers + documenter + canon pinner; Claude does NOT re-architect sprint sequence, self-schedule expansion-lane work, mint `/r` or `/p` for real clients before S11+S12, mutate real engagements outside authorized scope, or add third-party dependencies without authorization. Sprint I3 live-walkthrough cosmetic finding (live-link role-coverage map doesn't branch on `source_type`) reclassified as **Improvement** per § 11 — backlog, NOT a side sprint. Sprint I4 (binary document upload behind `engagement_intake_documents.storage_path`) **rolled into Sprint S2** because transcript ingest is the actual use case for that storage field; standalone I4 no longer scheduled. Files modified: `docs/39` (new), `docs/08` (roadmap-lock block at top), `docs/10` (this paragraph). **Zero source code changes. Zero migration runs. Zero engagement mutations. Zero `/r` or `/p` mint. Zero send. Zero schema/package changes. Zero Sapient Digital interaction.** **Next-planned milestone:** Sprint S1 — Online Intake Flow Audit + Stakeholder Intake Readiness. Audit the live-link `/intake/[token]` route end-to-end against a controlled test fixture (NOT Sapient Digital); finalize readiness-gate wording + stub the code-side enforcement hook (full enforcement lands in S11); align `docs/35` § 5 with the primary/secondary/tertiary hierarchy in `docs/39` § 4.

**Prior (2026-06-02 — Sprint I3 Live Walkthrough on Sapient Digital):** Promotes Sprint I3 from build-clean source-only landing to operationally ready against deployed staging. Full evidence: `docs/38_SAPIENT_DIGITAL_I3_LIVE_WALKTHROUGH.md`. Found and remediated a deployment gap — the canonical alias `https://slate-os-staging.vercel.app` was still pointing at the 13-day-old Production deployment from May 20 (predating Sprints I2 and I3). With operator authorization, triggered `vercel --prod` from staging head `a39a50c`; new Production deployment `dpl_9wFmhoX4K5FcLkzxxPiUMuSUwTZT` built in ~1m, canonical alias re-pointed automatically. Post-deploy curl confirms `/r/test-noop` + `/p/test-noop` serve canon `no-store + noindex + no-referrer` headers; `/s/test` + `/sow/test` stay 404. Applied migration 0017 to deployed Supabase project `hhglrcvsmwaheikdvijw` via Supabase MCP `apply_migration` (success, idempotent); schema verification confirms all 7 new columns on `stakeholder_intake_sessions`, all 7 on `stakeholder_responses`, full 20-column `engagement_intake_documents` table, 12 CHECK constraints including the canonical `token_hash` live-vs-offline mode gate, RLS enabled on all 3 tables with `{authenticated}`-only policies (zero anon). Live UI walkthrough on Sapient Digital (engagement `76097653-…`): staged one clearly-labeled test offline stakeholder `I3 WALKTHROUGH TEST — Operator-staged test stakeholder` (executive role, operator_entered source, inferred confidence); captured one test response on the `automation_wishlist` seed question with `test@example.com` included to validate the inline non-blocking PII-warning (warning fired correctly); marked ready for synthesis (status chip flipped draft→ready, mark-ready button removed per terminal-eligible canon); voided with audit reason via `window.prompt`-patched flow (status flipped to voided, answer text rendered strikethrough, both action buttons removed per terminal-state canon). All DB transitions match canon: session `91fea09e-…` (`source_type='operator_entered'`, `client_visible=false`, `token_hash=null`, `sent_at=null`); response `02aea7f1-…` (`draft → ready_for_synthesis → voided`, `client_visible=false` throughout). All 4 activity events emitted with sanitized metadata — zero `answer_text`, zero `stakeholder_name`, zero `email`, zero `operator_notes` content; only safe semantic fields (role/sourceType/sourceConfidence/sessionId/questionId/responseStatus/priorStatus/reason). All 17 boundary checks pass: zero new report or proposal share tokens, zero new delivery snapshots, zero Send-to-Client emissions, zero SOW events, zero documents inserted (`engagement_intake_documents` shipped with schema only, no upload UI yet), `/s` + `/sow` stay 404, `/r` + `/p` serve canon headers, `client_visible` never elevated for any offline row, `token_hash` never minted for offline session, `sent_at` never populated, no real Sapient identity captured, Sapient readiness gate stays at 1/15 (test data does not advance it), live-link Mode A code path untouched, zero email/CRM/e-sign/mailto wiring exercised. One cosmetic finding tracked in `docs/38` § 4.6 (non-blocking): the existing live-link role-coverage map + risk-derivation rules don't branch on `source_type`, so an offline-staged stakeholder still triggers the "1 stakeholder have not opened the intake link" risk note — future polish. Test-artifact DB rows retained (clearly labeled, response in terminal `voided` state, `client_visible=false`) for audit-trail preservation. Files modified: `docs/38` (new walkthrough log), `docs/37` (live-walkthrough landing note), `docs/36` (Path B cross-reference updated), `docs/08` (Sprint I3 Live Walkthrough block added at top), `docs/10` (this paragraph). **Zero source code changes** this sprint; zero schema/package changes outside applying the already-committed migration 0017. **Next-planned milestone:** Sapient Digital Offline Intake Data Capture — operator enters real Sapient Digital stakeholder responses collected via the `docs/36` § 6 question packet into the offline-intake UI on deployed staging; once enough role coverage lands, **Sprint I5 — Findings Synthesis Integration** (consume `response_status = 'ready_for_synthesis'` rows) becomes unblockable. **Sprint I4** (binary document attachment behind `engagement_intake_documents.storage_path`) remains optional.

**Prior (2026-05-28 — Sprint I3: Offline Intake Operator UI):** Wires the operator-facing offline-intake surface authorized by `docs/37` and backed by Sprint I2. 8 new components landed in `components/intake/`: source-type chip + response-status chip (presentational); `stage-offline-stakeholder-form.tsx` (calls `createOfflineStakeholderIntakeSessionAction` with affirmative "No message sent" / "Save without sending" chips above submit + ShieldOff boundary reminder); `offline-response-form.tsx` (calls `createOfflineStakeholderResponseAction` with inline non-blocking PII-warning that fires on email / SSN-shape / phone / 13–16-digit-numeric regex match); `offline-response-actions.tsx` (Mark ready + Void buttons; Void uses `window.prompt` for optional reason capped at 500 chars; terminal statuses surface no actions per canon transition matrix); `offline-intake-panel.tsx` (expandable session rows showing role badge + source chip + source-confidence chip + ready/draft counts; per-response action row + inline "Capture another response" `<details>` form + offline-documents section); `offline-intake-readiness-hint.tsx` (operator-only aggregate card with sessions / sessions-with-ready / draft / ready / superseded / voided / document counts + synthesis-gate reminder). New server-only read module `lib/intake/offline-queries.ts` exposes `getOfflineSessionsForEngagement` (excludes `source_type = 'live_link'`), `getEngagementIntakeDocuments` (non-voided only), `getOfflineIntakeReadinessSummary`. Page wiring: `app/app/engagements/[id]/intake/page.tsx` adds an "Offline intake" section between the existing CreateStakeholderForm and RoleCoverageMap (StageOfflineStakeholderForm + OfflineIntakePanel) and the OfflineIntakeReadinessHint to the aside; persisted branch fetches the three offline projections in the same `Promise.all` as existing fetches; live-link Mode A code path untouched. Source-tree fix: removed a stray `export { mapEngagementIntakeDocumentRow } from "./mappers"` re-export from `lib/intake/offline-actions.ts` that compiled in dev but failed `next build` (a `"use server"` file may only export async functions); consumers now import the helper directly from `./mappers`. Lint clean + production build clean (intake route grew from ~9 KB → 14.2 KB First Load JS driven by the new operator-side client components; all other 28 routes byte-identical) + send-to-client disclaimer pin script passing + zero new package dependencies + zero new public routes + zero `service_role` writes + zero `mailto:` / `sendgrid` / `nodemailer` / `docusign` / `hellosign` / `adobesign` / `crm_push` references + zero findings synthesis logic changes + zero `/r` or `/p` mint + zero Send to Client + zero Group-B claims. **Sapient Digital live walkthrough deferred** — migration 0017 is NOT applied to deployed Supabase project `hhglrcvsmwaheikdvijw` (verified via `information_schema.columns` lookup: none of the new `stakeholder_intake_sessions` columns exist; `engagement_intake_documents` table does not exist). Source code is build-clean and ready; live UI test against Sapient Digital is gated on the operator applying 0017 to deployed Supabase + restarting the deployed env. No mutation was performed against any deployed engagement in this sprint. Files: 1 page wiring change, 1 server action file (re-export removed), 7 new component files, 1 new query module, plus docs updates (`docs/37`, `docs/08`, `docs/10`). **Next-planned milestone:** Operator applies migration 0017 to deployed Supabase + walks Sprint I3 UI through Sapient Digital (operator-side, no Claude sprint required); on success, **Sprint I4 — Document Attachment / Source Capture** (binary upload backend hookup behind `engagement_intake_documents.storage_path`) or **Sprint I5 — Findings Synthesis Integration** (consume `response_status = 'ready_for_synthesis'` rows in the AI synthesis pipeline).

**Prior (2026-05-28 — Sprint I2: Offline Intake Data Model + Server Actions):** Implements the foundation authorized by `docs/37` Sprint I1. New migration `supabase/migrations/0017_offline_intake_extensions.sql` extends `stakeholder_intake_sessions` (`source_type` text NOT NULL default `'live_link'`, `entered_by`, `collected_at`, `source_confidence`, `operator_notes`, `client_visible` default true for backfill), drops `token_hash` NOT NULL invariant + adds CHECK constraint gating `token_hash` on live mode only; extends `stakeholder_responses` (`source_type`, `response_status` text NOT NULL default `'ready_for_synthesis'` for backfill, `entered_by`, `collected_at`, `operator_notes`, `supersedes_response_id`, `client_visible`); adds new `engagement_intake_documents` table (workspace-scoped RLS authenticated-only, soft-delete via `voided_at`/`voided_by`/`void_reason`, content via `content_text`/`external_url`/`storage_path`). Backfill safety: all pre-existing rows updated to live-link + ready-for-synthesis + client_visible=true so Mode A is byte-identical. Types extended in `lib/intake/types.ts` (228 new lines — `IntakeSourceType`, `IntakeDocumentSourceType`, `IntakeSourceConfidence`, `IntakeResponseStatus`, `EngagementIntakeDocument`, Create*Input/Result discriminated unions, `OfflineIntakeErrorCode` enumerating 19 distinct failure modes). Mapper extensions in `lib/intake/mappers.ts` (87 new lines — `DbEngagementIntakeDocumentRow` + `mapEngagementIntakeDocumentRow`). New action module `lib/intake/offline-actions.ts` — 6 guarded `"use server"` actions enforcing cookie-bound auth + workspace-scoped RLS (no service-role) + response-status transition matrix + per-content-mode validation + operator_notes length caps + soft-delete for void. Activity event types extended (`offline_intake_session_created`, `offline_intake_response_created`, `offline_intake_response_ready`, `offline_intake_response_voided`, `intake_document_created`, `intake_document_voided`) + new entity type `engagement_intake_document` + matching timeline tones (info/info/success/neutral/info/neutral) + labels deliberately using "staged"/"drafted"/"voided" to avoid implying SLATE collected the content. Activity event metadata carries only safe fields — never `answer_text`, never `content_text`, never raw email, never PII. Lint clean + production build clean + 29 routes byte-identical + zero new package dependencies + zero UI components + zero public routes + zero service-role SQL writes + zero email/CRM/e-sign/mailto/public-SOW/Group-B references introduced + live-link Mode A behavior unchanged + Sapient Digital data not mutated (migration not yet applied to deployed Supabase). Files: 4 modified (`lib/intake/types.ts`, `lib/intake/mappers.ts`, `lib/activity/types.ts`, `components/activity/activity-timeline.tsx`) + 2 new (`lib/intake/offline-actions.ts`, `supabase/migrations/0017_offline_intake_extensions.sql`) + 4 docs (`docs/37`, `docs/36`, `docs/08`, `docs/10`). **Next-planned milestone:** Sprint I3 — Operator UI for Draft Stakeholders + Offline Responses per `docs/37` § 7.

**Prior (2026-05-23 — Sapient Digital Offline Intake Canon, Sprint I1):** Authored `docs/37_SAPIENT_DIGITAL_OFFLINE_INTAKE_CANON.md` — the canonical authorization for operator-staged offline intake that closes the `docs/36` § 9 Observation 1 product gap. Canon defines 3 modes (Live / Operator-staged / Document-only), conceptual data model extensions (`source_type` + nullable `token_hash` + `entered_by` + `collected_at` + `source_confidence` + `operator_notes` + `client_visible` on `stakeholder_intake_sessions`; matching extensions on `stakeholder_responses` + new `response_status` enum + `supersedes_response_id`; new `engagement_intake_documents` entity for offline source attachments), UI requirements ("Stage offline stakeholder" form with no email required + no token minted + "Save without sending" + "No message sent" affirmation chips; per-stakeholder `Source` chips; per-response Mark ready / Void / Supersede actions; PII-warning toast on text paste), findings synthesis readiness gate (≥2 role perspectives + ≥4 of 6 question topics OR explicit operator override with audit-logged reason), Sapient Digital-specific 5-step path, 6-sprint implementation sequence (I2 Data Model + Server Actions → I3 Operator UI → I4 Documents → I5 Findings Synthesis Integration → I6 Sapient Digital Stage 2 Execution), 8 explicitly-preserved safety boundaries (no external send by default; no SLATE email/CRM/e-sign; no `/r` or `/p` mint during any I2-I6 sprint; no public SOW; no service-role SQL writes; no Group-B claims; no client-visible artifact from unapproved offline notes; no auto-override of synthesis gate), and 7 open decisions for operator review (question taxonomy enforcement, document storage backend, PII-warning threshold, name-optionality, packet ID assignment, bridge-state handling, Sapient-first-pilot framing). Zero source code changes; zero Sapient Digital engagement mutations; zero `/r` or `/p` mint; zero send; zero schema/package changes — read-only inspection of `lib/intake/types.ts`, `lib/intake/actions.ts`, `supabase/migrations/0005_stakeholder_intake.sql` for canon grounding only. Files modified: `docs/37` (new), `docs/36` (Path B canonized reference), `docs/35` (readiness plan updated), `docs/08`, `docs/10`. **Next-planned milestone:** Sprint I2 — Offline Intake Data Model + Server Actions (per `docs/37` § 7); OR operator may collect Sapient offline responses externally today using `docs/36` § 6 question packet without further SLATE-side work.

**Prior (2026-05-23 — Sapient Digital Stage 1-2 Execution Log):** Executed Stage 1-2 cleanup + planning sprint per `docs/35` recommendation. `docs/36_SAPIENT_DIGITAL_STAGE_1_2_EXECUTION_LOG.md` documents outcomes. Headline: (1) **Stale share-token cleanup landed** — 5 unlabeled stale tokens revoked via UI two-step Revoke (created May 14 + May 20 2026; all `Audience label required before marking sent`; none ever marked-sent since canon requires audience-label before mark-sent fires). Sapient Digital report share links now `10 total · None active`. Readiness gate advances 0/15 → **1/15**. (2) **Intake path decision: Path C (planning-only) selected.** Path A blocked by sprint's no-send boundary; Path B confirmed as **product gap** — SLATE intake UI mints token-gated URLs for operator manual delivery, no operator-pasted-response affordance ("Email automation lands later" per form copy). (3) **Stakeholder list NOT staged** — would mint real intake tokens against placeholder identities, boundary forbids. All 6 role slots remain Missing. (4) **Intake question packet authored inline** (`docs/36` § 6) — 5 universal questions + 30 role-specific questions across all 6 stakeholder slots, operator-distributable. (5) **Roadmap seed items preserved** (3 AI-generated pre-engagement-setup planning items; operator decides at Stage 5 sprint). (6) **Findings scaffold deferred** — no intake responses → would be thin scorecard-only output. (7) **5 product-side observations surfaced** (`docs/36` § 9): no operator-staged-response path; no bulk stakeholder import; no draft-stakeholder save mode; no bulk stale-token cleanup; `Generate AI roadmap draft` clickable before opportunities exist. Boundaries held: zero source changes, zero `/r` or `/p` mint, zero Mark sent, zero email/CRM/e-sign, zero public SOW, zero SOW share tokens, zero schema/package changes, zero service-role SQL writes, only Sapient Digital touched. Files modified: `docs/36` (new), `docs/35` (readiness gate updated), `docs/08`, `docs/10`. **Next-planned (operator picks branch):** A Stage 2 Live Intake Sprint (real contacts + send authorization); B Intake Workflow Product Gap Sprint (Path B canon + impl); C Pause + offline strategy session; D Switch to different real client engagement.

**Prior (2026-05-22 — Sapient Digital Engagement Readiness Plan):** First attempt to mint `/r` and `/p` share links against the real Sapient Digital lead/engagement (lead `fee3895a-7943-48d1-8a28-df98eafb74f2` → engagement `76097653-fedb-42e5-9ef6-e89a0e97f802`) for the first billable pilot hit the audit stop condition. Read-only inspection showed the engagement is at **Stage 1 of 6 — Setup** with no real Sprint content: 0 of 6 stakeholder roles invited, 0 findings, 0 opportunities, 3 unlinked seed roadmap items (`REPORT-READY INPUTS 0`), 0 of 12 report sections approved, 4 historical PDF candidates all draft-watermarked, **5 stale active share tokens** from prior Phase 1B audit cycles, 7 historical proposal candidates (latest voided). Authored `docs/35_SAPIENT_DIGITAL_ENGAGEMENT_READINESS_PLAN.md` covering: current snapshot, 6-stage gap analysis, required operator inputs (stakeholders, documents, findings approval, opportunity scoring, roadmap decisions, per-section approval, proposal scope edits, final sign-off, manual delivery + feedback capture), Claude-executable tasks via Chrome MCP (UI/action layer only), a **15-item content-quality gate** that must pass before any `/r` or `/p` is minted for this real client (currently 0/15 passing), explicit boundary block on send/mint until gate passes, and a sprint sequence (Stage 1-2 Execution → Stage 3 → 4 → 5 → 6A → 6B → Pre-Delivery Audit → Delivery). Recommended next sprint: **Sapient Digital Stage 1-2 Execution** — confirm stakeholder list, send/stage intake, collect responses, optional AI-draft initial findings. Strictly no mint, no Mark sent, no SOW external sharing during Stage 1-2. Zero mutations during this planning inspection — operator + Claude both committed to no-mint until § 5 quality gate is satisfied with explicit operator chat sign-off. Files modified: `docs/35` (new), `docs/08`, `docs/10`. Zero source code changes.

**Prior (2026-05-21 — Phase 1B Controlled First-Client Pilot Audit):** Operator-driven 7-lane controlled pilot audit against deployed Vercel staging host `https://slate-os-staging.vercel.app`. Test client: `SLATE Pilot Test Client` (Lead `58ccc2bf-…`, Engagement `ed7f1f7d-…`, audience prefix `CONTROLLED PILOT 2026-05-21`, test contact `devtest@sapientdigital.io`). **Verdict: ⚠️ Pilot passed with operator-tracked items.** All 7 lanes execute canon-correctly: Lane 1 (Free Audit → Lead row created, no auto-engagement), Lane 2 (Lead → Engagement via "Start AI Opportunity Sprint" promotion), Lane 3 (Report mint → 26 682 B canon-correct client-safe render → revoke → 8 584 B canon generic-unavailable), Lane 4 (Proposal mint → 20 587 B canon artifact with four-denial footer + zero e-sign/contract/pay leak → revoke → 8 892 B generic-unavailable), Lane 5 (SOW Draft generated — guard PASSED 16×71 patterns, confirming `docs/32` Lane 3 closure fix live on deployed — internal route renders Draft SOW · not executed, voided cleanly), Lane 6 (Mark sent to client on both report + proposal sides — canon-verbatim 3-ack modal, sendCount=1, channel `operator_mediated_copy_link`, no email sent by SLATE), Lane 7 (deployed routes canon-correct: /r + /p → 200 + security headers + body markers, /s + /sow → 404). Two operator-tracked findings: (1) Supabase URL config gap discovered + remediated mid-audit (initial magic link redirected to localhost:3001 due to project Site URL fallback; operator updated Supabase Dashboard URL Configuration; recommended permanent fix is extending docs/33 § 2 with explicit Supabase URL setup step); (2) custom subdomain DNS recommended for long-term posture (non-blocking; pilot proceeds on *.vercel.app). Boundary preservation across all 7 lanes: zero public SOW route, zero SOW share tokens, zero email/CRM/e-signature wiring, zero schema/migration/package changes, zero Group-B wiring, zero real client engagement touched, top-level Send to Client still LOCKED, zero source code changes. Files modified: `docs/34_PHASE_1B_FIRST_CLIENT_PILOT_AUDIT.md` (new), `docs/08`, `docs/10`. **Phase 1B Delivery Engine now cleared for controlled first-client billable pilot** via operator-mediated copy-link Send to Client flow against deployed host.

**Prior (2026-05-20 — Phase 1B Deployment FULLY Cleared for Controlled External Client Exposure):** Operator ran the 5 `docs/33` § 6 SQL verification queries against the `SLATE OS` Supabase project and pasted all result grids inline into `docs/32` § 4 Precondition 2. Outcomes: 4 share-token + delivery-snapshot tables present ✅, RLS enabled on all 4 ✅, both share-token tables expose only `{authenticated}` policy (zero anon) ✅, UNIQUE btree indexes on `token_hash` for both share-token tables ✅, `metadata jsonb` columns present on both share-token tables ✅. **Verdict promotes to ✅ Phase 1B Delivery Engine FULLY cleared for controlled external client exposure of /r and /p links via the deployed `slate-os-staging.vercel.app` host.** All three production-side preconditions (deployed env pepper, deployed-staging migrations parity, deployed-host curl) now verified end-to-end. Zero source code changes this turn; doc-only update: `docs/32` § 4 (full SQL evidence + verdict promotion), `docs/08` (Phase 1B Deployment block updated), `docs/10` (this paragraph). Operator may begin a controlled first-client pilot using the operator-mediated copy-link Send to Client flow. Optional next sprints (operator-choice, no canon prerequisite): Option B-2 UX polish round 2; Option C-1/C-2/C-3/C-4 (email / CRM / e-sign / public SOW share canons at `docs/34`+).

**Prior (2026-05-20 — Phase 1B Deployment Landed + Production Preconditions Mostly Cleared):** Executed `docs/33` end-to-end via Vercel CLI under explicit operator override of `docs/33` § 4's "Claude must NEVER see or set deployed env vars" clause. Created Vercel project `slate-os-staging` (team `christians-projects-bb2d10a3`, project id `prj_wAjfR7dy9FTzIwOwxek7NJwuyZuu`) linked to GitHub repo `cryptocrystian/SLATE-OS` with production branch `staging` (created from `persistence/step-0-1-auth-shell` at commit `0b09c4c`). Set 7 Production env vars via `printf '%s' "$VAL" \| vercel env add NAME production --force` stdin pattern (values never echoed to chat/history; fresh 64-char `SLATE_SHARE_TOKEN_ACCESS_PEPPER` generated, NOT reused from local). First deploy succeeded (~53 s build, Node 24.x default, framework auto-detected Next.js 14). Stable canonical public URL: **`https://slate-os-staging.vercel.app`**; per-deploy + team-scoped URLs gated by Vercel Deployment Protection (401, correct default posture). Precondition outcomes: (1) Deployed pepper ✅ PASS (Vercel env panel "Encrypted"), (2) Deployed migrations parity ⏸ PENDING (operator runs `docs/33` § 6 SQL in Supabase Dashboard SQL editor, 5 read-only queries ~2 min), (3) Deployed-host curl ✅ PASS (`/r/test-noop` + `/p/test-noop` → HTTP/2 200 with canon-correct `cache-control: private, no-cache, no-store, max-age=0, must-revalidate` + `x-robots-tag: noindex, nofollow` + `referrer-policy: no-referrer`; canon body markers present; body sizes 8405 B / 8713 B from production minification; `/s/test` + `/sow/test` → HTTP/2 404). docs/32 § 4 verdict promotes to "✅ Phase 1B Delivery Engine cleared for controlled external client exposure of /r and /p links — pending operator Supabase SQL verification (Precondition 2 only)." Zero source code changes; doc-only update: `docs/32` § 4, `docs/08`, `docs/10`. `.vercel/project.json` gitignored. Override discipline preserved: secrets never logged/pasted/screenshotted; temporary bootstrap script `.vercel-env-bootstrap.sh` deleted post-run; Supabase MCP `list_tables` against production project remained classifier-blocked as expected. Audit observation for `docs/30` follow-up: keep Vercel Deployment Protection on team-scoped URLs; canonical short URL is the intended public surface. **Next-planned milestone: operator runs `docs/33` § 6 SQL queries in Supabase Dashboard, pastes results into `docs/32` § 4 Precondition 2 placeholder block, verdict promotes to fully cleared. Operator may then begin controlled external client exposure of `/r` and `/p` links via the operator-mediated copy-link Send to Client flow.**

**Prior (2026-05-20 — Phase 1B Deployment Environment Decision + Setup Plan Sprint):** Operator/deployment planning sprint. Authored `docs/33_PHASE_1B_DEPLOYMENT_SETUP_PLAN.md` — the operator-runnable plan to stand up a deployed staging environment so the Delivery Engine verdict can promote from "✅ Staging cleared, production preconditions partially pending" to "✅ Cleared for controlled external client exposure." Chosen path: **Option A — Vercel staging deployment on the existing SLATE OS Supabase project** (`hhglrcvsmwaheikdvijw`), restricted to canonical test fixture engagement `76097653-…` with explicit `STAGING WALKTHROUGH YYYY-MM-DD` audience labeling. Option C (separate non-prod Supabase project) recommended as long-term staging posture once Option A clears immediate preconditions. Option B (same project + separate schema) declined as not a precondition-clearing path. Repo deployment readiness verified: Next.js 14.2.35, App Router, zero-config Vercel detection, no platform-specific build config needed. `docs/33` covers: Vercel project setup (branch `staging`, default Node 20.x), Supabase posture with hard operational guardrails (canonical test fixture only, no service-role SQL writes, no destructive SQL, no real-client mutation), env vars list with public-vs-server-only annotations, operator-side pepper-generation one-liner (`node -e "console.log(require('node:crypto').randomBytes(48).toString('base64url'))"`), post-deploy `curl -sI https://<host>/{r,p}/test-noop` + `/s/test` + `/sow/test` verification clearing Precondition 3, manual Supabase verification SQL (5 read-only queries) clearing Precondition 2, Vercel env-panel pepper presence check clearing Precondition 1, optional end-to-end smoke test, sign-off checklist. Zero source code changes; doc-only update: `docs/33` (new), `docs/32` § 4 (cross-reference + summary), `docs/08`, `docs/10`. Next-planned milestone: operator executes the `docs/33` plan, pastes evidence inline into `docs/32` § 4, then re-issues the Production Preconditions Verification sprint against the deployed host URL — at which point the verdict promotes to "✅ cleared for controlled external client exposure of /r and /p links."

**Prior (2026-05-20 — Phase 1B Production Preconditions Verification Sprint):** Operator-side deployment verification sprint. Goal: verify the three remaining production preconditions from `docs/32` § 4 (deployed env pepper, deployed-staging migrations 0012-0016 parity, deployed-host curl on /r + /p + /s + /sow). **Outcome: all three remain operator-pending — no deployed staging environment exists at this time.** Verdict unchanged: ✅ Staging cleared, production preconditions partially pending. Source-side evidence refreshed: local `.env.local` pepper is 96 chars (above 64-char min, never disclosed); all 5 migration files present at canonical sizes in source tree; localhost curl re-run after `.next/` cache clear confirmed `/r/test-noop` + `/p/test-noop` return 200 OK with `Cache-Control: no-store, must-revalidate` + `X-Robots-Tag: noindex, nofollow` + `Referrer-Policy: no-referrer` + canon generic-unavailable body; `/s/test` + `/sow/test` return 404. Deployed-side blockers: (1) repo has zero deployment config (no `vercel.json`/`netlify.toml`/`render.yaml`/`Dockerfile`) and GitHub API confirms zero deployments + zero environments registered; (2) Supabase MCP `list_projects` confirms only one SLATE project exists (the production-labeled `SLATE OS` project) — no non-prod SLATE Supabase project; (3) `list_tables` against the production project is classifier-blocked per the staging-sprint boundary. Zero source code changes; doc-only update: `docs/32` § 4 (precondition table + operator path forward), `docs/08`, `docs/10`. Operator path forward: provision a hosting platform (recommended Vercel) + non-prod Supabase project + set deployed env vars + re-run the precondition curl section. No code sprint is appropriate until the deployment exists.

**Prior (2026-05-19 — Phase 1B UX Polish / Operator Guidance Sprint):** Small operator-guidance + canon-drift-prevention sprint after staged clearance. (1) Added operator-only muted-callout guidance at the share-links section of both report + proposal panels summarizing canon (SLATE never sends; Mark sent records operator handoff only; copy URL through own approved channel; audience label required before Mark sent; recipient email optional + hashed at rest). (2) Clarified recipient-hash visual on every share-token row in both panels — explicit "Recipient hash present" vs "no recipient hash" states with consistent tooltip; never renders the hash value or raw email. (3) Landed `scripts/check-send-to-client-disclaimers.cjs` + `npm run check:send-to-client-disclaimers` (pure Node, no new deps) that pins 3 report + 3 proposal canonical substrings against BOTH `lib/client-delivery/send-to-client-types.ts:SEND_TO_CLIENT_DISCLAIMERS` AND `docs/29` § 13 — closes `docs/30` Audit Note 2. Lint clean + production build clean + disclaimer pin passes + 29 routes unchanged + zero new deps + zero schema changes + zero Group-B wiring + zero public SOW route + zero email/CRM/e-signature wiring. Source files changed: 2 panels + 1 script + 1 package.json + 3 docs. Next-planned: complete the three operator-side production preconditions in `docs/32` § 4 before any controlled client exposure; then Option C (CRM / email / e-sign / SOW share canon authoring at `docs/33`+) opens if business priority requires.

**Prior (2026-05-19 — Phase 1B Walkthrough Caveat Closure Sprint):** Closure sprint promoted `docs/32` verdict from "⚠️ Cleared with operator-tracked exceptions" to **✅ Staging cleared for controlled client use** (pending three operator-side production preconditions: deployed-env pepper, deployed-staging migrations parity verification, deployed-host re-curl). Two source-tree caveats closed by minimal targeted code changes: (1) Lane 1 — dev-only server diagnostic log added at `app/r/[token]/page.tsx:logBlockedAccessForDev` surfaces render-time eligibility rejection reasons in `npm run dev` stdout; public surface unchanged. (2) Lane 3 — `LEGAL_BOUNDARY_NOTICE` sanitized at 3 sites (`lib/proposals/sow-draft-eligibility.ts`, `lib/proposals/sow-draft-actions.ts`, `components/proposals/sow-draft-document.tsx`) from text enumerating banned legal terms to "Legal terms are intentionally omitted from this draft. Any legal terms will be provided separately during execution review."; SOW commercial guard unchanged in strength/scope/pattern count. End-to-end re-verification via Chrome MCP against canonical fixture `76097653-…`: SOW Draft `5fb27e8e-…` minted ("16 fields × 71 patterns · Passed"), opened internal route (full canon chrome), voided cleanly. `docs/30` Audit Notes 4 (Next.js RSC URL echo — framework not SLATE leak) and 5 (mint-vs-render eligibility divergence closure) added. Lint clean + production build clean + 29 routes unchanged + zero new dependencies + zero schema changes + zero Group-B wiring + zero public SOW route + zero email/CRM/e-signature/alternative-transport wiring. Source-tree files changed: 4 source + 3 docs. Next-planned milestone unchanged: operator confirms the three production preconditions, then chooses between **Option B — UX polish sprint** and **Option C — CRM / email / e-sign / SOW share canon authoring**.

**Latest (2026-05-19 — Phase 1B Delivery Engine UI walkthrough pass):** `docs/32` filled with execution evidence for all four UI-mediated walkthrough lanes (report mint→view→revoke, proposal mint→render→revoke, SOW Draft generate, Send to Client mark-sent on both lanes) against the canonical test fixture engagement `76097653-fedb-42e5-9ef6-e89a0e97f802` (Sapient Digital), under explicit operator authorization with hard guardrails (no service-role SQL writes, no schema modifications, no real client engagements touched, all artifacts labeled `STAGING WALKTHROUGH 2026-05-19`). Verdict: **⚠️ Cleared with operator-tracked exceptions** — 11/12 readiness conditions PASS or PASS-WITH-CAVEAT, 1/12 (deployed-staging migrations parity) operator-pending. Key observations: (a) Lane 1 report snapshot `9068f58f-…` exhibits a render-time eligibility divergence which is canon-correct fail-safe behavior — recommended `docs/30` Audit Note 5 to surface the specific rejection reason; (b) Lane 3 SOW Commercial Guard rejected the draft pre-mint for `governing_law` / `indemnification` / `warranty` legal-boundary violations — canon-correct, no draft minted; (c) `SLATE_SHARE_TOKEN_ACCESS_PEPPER` configured in local `.env.local` (operator must replicate in deployed staging env panel). Zero source code changes; doc-only update: `docs/32`, `docs/08`, `docs/10`. Next-planned milestone: operator choice between **Option B — UX polish / operator guidance sprint** (in-product mark-sent guidance + recipient-hash visual + canon-verbatim disclaimer CI pin + render-time eligibility logging) and **Option C — CRM / email / e-sign / SOW share canon authoring** (four independent canons per `docs/29` § 17). `Send to Client` remains at operator-mediated copy-link posture.

**Phase boundary (2026-05-07).** AdvisoryOps Phase 1A is the internal operating-system foundation and was accepted on 2026-05-07 with two minor fixes applied during the audit (stale `Mock — not wired` badges removed from `ProposalOptionDetail` and `FollowUpQueue`). It does not yet certify that reports, proposals, exports, or client collateral meet top-tier consulting quality. **Phase 1B must define and build the Consulting-Grade Deliverable Engine before customer-facing output claims are made.** The canonical Phase 1A audit is `docs/12_PHASE_1A_ACCEPTANCE_AUDIT.md`. The Phase 1B chart/exhibit canon has landed at `docs/13_PHASE_1B_CHART_EXHIBIT_CANON.md` — it approves Visx, formalizes the SLATE chart-vocabulary architecture, defines the eight required exhibits, and sets implementation order. The canon does not authorize building the remaining seven exhibits — that requires a separate sprint approval.

**Phase 1B Exhibit Sprint 1 (2026-05-07)** shipped the first real exhibit: `RiskAdjustedPriorityQuadrant` at `components/charts/exhibits/risk-adjusted-priority-quadrant.tsx`. Analytical 2×2 (impact × complexity), bubble size = business impact, color = risk band (`success / info / warning / risk` from existing chart-tone vocabulary). Pure server component. Accepts a narrow `RiskAdjustedQuadrantPoint[]` shape; exports `opportunityToRiskQuadrantPoint` adapter for future report wiring. Rendered alongside the proof-of-fit Executive Summary 2×2 on the unlinked `/app/charts-preview` route. **Not yet wired into the report or proposal builders — Sprint 1 proves the component and preview rendering only.** No package dependencies added; no new Visx packages.

**Phase 1B Exhibit Sprint 2 (2026-05-08)** shipped the Capability Maturity Heatmap at `components/charts/exhibits/capability-maturity-heatmap.tsx` plus a new reusable primitive at `components/charts/primitives/chart-heatmap-cell.tsx`. Capability × dimension grid; cell color encodes a 4-band maturity scale (0–39 risk · 40–59 warning · 60–79 info · 80–100 success) and the cell label is the score in tabular mono. Plain SVG `rect` + `text` cell — **`@visx/heatmap` was deliberately NOT installed**; the primitive will back the future Stakeholder Coverage Matrix without changes. Pure server component. Accepts a narrow `CapabilityMaturityCell[]` shape plus `capabilities[]` and `dimensions[]` row/column arrays. Rendered as the third exhibit on `/app/charts-preview` (Executive Summary 2×2 + Risk-Adjusted Priority Quadrant + Capability Maturity Heatmap). **Sprint 2 ships the preview component only — not yet wired into the report or proposal builders.** No package dependencies added.

**Phase 1B Exhibit Sprint 3 (2026-05-08)** shipped the Stakeholder Coverage Matrix at `components/charts/exhibits/stakeholder-coverage-matrix.tsx`. Role × topic heatmap surfacing evidence-coverage gaps before a report is treated as fully grounded. 4-tone evidence-strength scale (`missing → neutral`, `thin → warning`, `adequate → info`, `strong → success`); present cells show `responseCount`, missing cells render an em-dash at recessive opacity so coverage gaps visibly differ from low-strength responses. **Reuses the Sprint 2 `ChartHeatmapCell` primitive without modification** — the canon's primitive-reuse promise is now realized. Pure server component. Accepts `StakeholderCoverageCell[]` plus `roles[]` and `topics[]` row/column arrays. Rendered as the fourth exhibit on `/app/charts-preview`. **Sprint 3 ships the preview component only — not yet wired into the report or proposal builders.** No package dependencies added.

**Phase 1B Exhibit Sprint 4 (2026-05-08)** shipped the Roadmap Gantt with Dependencies at `components/charts/exhibits/roadmap-gantt-with-dependencies.tsx` plus two new generic primitives: `components/charts/primitives/chart-gantt-bar.tsx` and `components/charts/primitives/chart-dependency-arrow.tsx` (the latter exports `ChartDependencyArrowheadMarker` for the SVG `<marker>` arrowhead). 30/60/90-day timeline with phase headers, dashed phase boundaries, a brand-primary Today marker, and right-angle dependency arrows. Bar color = `RoadmapStatus` (`planned → info`, `in_progress → brand`, `blocked → risk`, `complete → neutral` muted). Item titles in the left margin; bars are clean colored blocks. Defensive clamping for out-of-range / invalid item geometry. Both new primitives are domain-agnostic. Pure server component. Accepts `RoadmapGanttItem[]` plus an optional `todayOffset`. Rendered as the fifth exhibit on `/app/charts-preview`. **Sprint 4 ships the preview component only — not wired into reports/proposals, no roadmap AI drafting added.** No package dependencies added; no new Visx packages.

**Phase 1B Benchmark Data Canon (2026-05-08)** has landed at `docs/14_PHASE_1B_BENCHMARK_DATA_CANON.md`. It defines three benchmark-data tiers (`illustrative` / `internal_directional` / `validated`), exact source-note strings for each, a future TS data shape with validation rules (`p25 ≤ p50 ≤ p75`, methodology required for validated, etc.), prohibited / allowed claim language, three readiness gates, and six proposed dimensions.

**Phase 1B Exhibit Sprint 5 (2026-05-08) · Gate 0 illustrative only** shipped Benchmark Comparison Bars at `components/charts/exhibits/benchmark-comparison-bars.tsx` plus the new `ChartPercentileBand` primitive at `components/charts/primitives/chart-percentile-band.tsx`. Per-dimension client score plotted against an illustrative p25/p50/p75 percentile band. Brand-primary diamond marker (visually primary), info-tone IQR box (muted scaffolding), median line, faint rail, neutral `SCORE · 0–100` axis framing (the canon prohibits "better"/"stronger" copy because `Workflow friction` is inverted). Exports `validateAndClampPoint` (rejects rows with bad percentile order — never silently sorts) and `defaultBenchmarkSourceNote` (derives the exact canon-prescribed source-note string from `dataset.status`). Default takeaway is conservative ("Illustrative comparison structure only; validated benchmark data is required before client-facing use."). Legend includes a status pill (`Illustrative · Gate 0`) so the credibility tier is visible chrome. Pure server component; rendered as the sixth exhibit on `/app/charts-preview`. **MUST NOT be wired into reports, proposals, public-scorecard, or PDF until a Gate 1 / Gate 2 dataset exists.** No package dependencies added; no benchmark data files; no schema.

**Phase 1B Financial Assumptions Canon (2026-05-08)** has landed at `docs/15_PHASE_1B_FINANCIAL_ASSUMPTIONS_CANON.md`. Four-tier validation system (`illustrative` / `operator_estimated` / `client_validated` / `finance_approved`), exact source-note strings per tier, 13 required financial inputs (baseline / rates / costs / risk / time-to-value / confidence / owner / status / reviewedAt), future TS data shapes (`FinancialAssumptionSet`, `SavingsWaterfallContribution`, `RoiBridgePoint`) with 12 validation rules, prohibited claim language ("guaranteed savings" / "payback in X months" / "will save $X" / "board-ready ROI" all gated on Gate 3 finance approval), allowed-by-tier language table, four readiness gates (Gate 0 preview → Gate 3 final-client-facing), plus dedicated rule blocks for AI-Savings Waterfall and ROI Bridge.

**Phase 1B Exhibit Sprint 6 (2026-05-08) · Gate 0 illustrative only** shipped AI-Savings Waterfall at `components/charts/exhibits/ai-savings-waterfall.tsx` plus the new `ChartWaterfallBar` primitive at `components/charts/primitives/chart-waterfall-bar.tsx`. Cost-baseline → per-contribution savings / cost bars → modeled-state bar with dashed-outline "modeled" treatment. Y-axis is COST, not savings — savings descend the cost stack (`success` tone), costs ascend (`warning` tone). Dashed connectors at the running cost stack between adjacent bars. Exports `validateAssumptionSet`, `validateContribution`, `defaultFinancialSourceNote`, and `deriveBaselineCost` — all pure helpers. Default takeaway ("Illustrative savings structure only; validated financial assumptions are required before client-facing use."), legend status pill ("Illustrative · Gate 0"), and exact illustrative source note ("Source: Illustrative sample data · not a financial model") all carry the Gate 0 framing deliberately. Pure server component; rendered as the seventh exhibit on `/app/charts-preview`. **MUST NOT be wired into reports, proposals, public-scorecard, or PDF until a Gate 1 / Gate 2 / Gate 3 assumption set exists.** ROI Bridge is NOT implemented in this sprint. No package dependencies added; no financial data files; no schema.

**Phase 1B Report Exhibit Wiring Sprint 3 (2026-05-12) · internal preview PDF** ships an **operator-only** print-friendly preview path for `/app/engagements/[id]/report`. **Option A** (HTML print route + browser "Save as PDF") chosen — **zero new package dependency**, zero server-side PDF binary generation, zero stored PDF artifacts. New route at `app/app/engagements/[id]/report/print/page.tsx` (server component, `/app/*`-gated, persisted-UUID-only, same query helpers + same five Group-A adapter calls + same `<ReportExhibitSlots>` renderer as the internal report preview); new `components/reports/report-print-document.tsx` (server component — internal banner, engagement / report identity card with engagement-id + report-id codes, full section list with status / confidence / AI-drafted badges + slot reference + summary + draft preview + evidence notes + reviewer notes + linked-evidence counts, Group-A exhibit slots, Group-B omission note, footer banner reinforcing "internal preview · not client-facing"). `app/app/engagements/[id]/report/page.tsx` gains a ghost-variant `Internal preview PDF` button (`target="_blank"`) next to the existing `LockedActionButton label="Export Report"` — the locked client-facing export remains locked verbatim; the new button is operator-internal only. `components/layout/app-shell.tsx` gets three additive `print:hidden` Tailwind hints (radial-glow + sidebar `<aside>` + `<TopBar>` wrapper) plus `print:p-0` on `<main>` — purely additive print-stylesheet behavior, no app behavior change. **Mock / legacy slug engagements** render an explanatory empty state. **No proposal / public / API / schema / package / AI / Group-B file changed**; `BenchmarkComparisonBars`, `AISavingsWaterfall`, `RoiBridge` are NOT imported by `print/page.tsx` or `report-print-document.tsx`; all four `LockedActionButton` controls remain locked. `npm run lint` clean; `npm run build` clean. **28 routes** (was 27 — new `/app/engagements/[id]/report/print` at `178 B / 96.1 kB`); report page stays at `7.5 kB / 111 kB`. Polish backlog (documented, not fixed): chart SVG dark fills print as dark gray bands — light-theme palette work is Sprint 4+ territory gated on the commercial / client-delivery sprint.

**AI Synthesis Steps 3–5 Acceptance Audit (2026-05-12)** has landed at `docs/18_AI_SYNTHESIS_STEPS_3_5_ACCEPTANCE_AUDIT.md`. Outcome: **Accepted with notes** — operator dev/staging walk-through pending. The audit verifies inventory, UI gating, persistence safety (every audited write path is partial-field for Steps 3 + 4 and INSERT-only for Step 5; `exhibit_slot` / `pricing_placeholder` / `recommended` / `option_type` / `position` / link rows / existing roadmap rows are preserved verbatim), prompt-context safety (no PII / answer text / file binaries / Saipien Fit Score / Group-B exhibit data / unapproved drafts), claim-guard coverage (26 patterns across 3 families — financial 14 / commercial-finality 6 / roadmap-commitment 6 — enforced pre-persistence; violation = no DB write + run row `failed`), and run-row + activity-event wiring (`run_type` values, `ai_*_drafted` event types, per-item `roadmap_item_created` from Step 5). No source code modified by the audit. Lint clean. Build deferred — `next/font` hit a transient `SSL alert 49` against `fonts.gstatic.com` during the audit run; the identical source tree built clean during Step 5 implementation (27 routes; `/app/engagements/[id]/report` 7.5 kB / 111 kB · `/proposal` 6.53 kB / 110 kB · `/roadmap` 5.91 kB / 109 kB). Group-B exhibits remain isolated to `/app/charts-preview`; all four `LockedActionButton` instances remain locked. The audit ships with a 17-row operator test plan (3-A through 5-G) covering strong-evidence, locked-state, missing-env, mock-engagement, banned-claim, and existing-items scenarios with per-row expected DB / run / activity behavior. **Recommended next sprint: Phase 1B Report Exhibit Wiring Sprint 3 — internal preview PDF**, after the dev/staging walk-through completes.

**AI Synthesis Step 5 (2026-05-11) · roadmap item drafting** ships operator-triggered AI drafting for **planned roadmap items**, mirroring Steps 3 and 4 against the roadmap-side persistence. **Engagement-level batch action** (one click → 2–6 planned items appended) and **append-only** — existing roadmap items are NEVER modified, deleted, reordered, or finalized; new rows insert with `status='planned'` and `position` after the current max in their phase. New: `lib/ai/claim-guard.ts` gains `ROADMAP_COMMITMENT_PATTERNS` (6 patterns: `guaranteed completion`, `binding timeline`, `final implementation schedule`, `committed delivery date`, `legally binding timeline`, `binding delivery commitment`); combined Step-5 scanner runs financial + commercial-finality + roadmap-commitment patterns (26 total). New: `lib/ai/roadmap-context.ts` (server-only — engagement + account + approved findings + scored/selected opportunities + existing roadmap items + operator-touched report sections + proposal options; no PII / answer text / file binaries / Saipien Fit Score). New: `lib/ai/roadmap-synthesis.ts` (deterministic JSON prompt requesting 2–6 items + validator with strict DB-aligned phase/priority enums + UUID-bounded `linkedOpportunityId` (only opportunities present in the supplied array; null otherwise) + **case-insensitive title deduplication against `existingRoadmap` titles** + combined banned-claim scanner). New: `lib/roadmap/synthesis-actions.ts` (`generateRoadmapDraftAction({ engagementId })` — opens `ai_synthesis_runs` row with `run_type=roadmap_draft`, calls the pipeline, **inserts only new rows** at `status='planned'`, emits a per-item `roadmap_item_created` event plus a single `ai_roadmap_items_drafted` batch-summary event, revalidates roadmap + report + proposal routes). `lib/ai/provider.ts` gains `getAiRoadmapProviderConfig()` honoring `SLATE_AI_ROADMAP_MODEL`. `ai_roadmap_items_drafted` added to `ActivityEventType` + activity timeline with tone `ai`. New: `components/roadmap/generate-roadmap-draft-button.tsx` — engagement-level client trigger mounted on the roadmap page only when `isPersisted && isAiConfigured()`. **Existing roadmap actions (`createRoadmapItem`, `setRoadmapItemStatus`, `removeRoadmapItem`) are unchanged.** No proposal / report / public-scorecard / PDF / export / schema / migration / API / package / chart-primitive / Group-B file was touched. `docs/14` / `docs/15` / `docs/17` did not need amendment. `npm run lint` clean; `npm run build` clean. **27 routes**; `/app/engagements/[id]/roadmap` lands at `5.91 kB / 109 kB` (+1 kB / +1 kB vs Step 4 for the client trigger + error/notice handlers); every other route is within 0 KB of the Step 4 baseline.

**AI Synthesis Step 4 (2026-05-11) · proposal-option drafting** ships operator-triggered AI drafting for a single proposal option at a time, mirroring Step 3 against `proposal_options` rows. New: `lib/ai/claim-guard.ts` (extracted shared module exporting `FINANCIAL_CLAIM_PATTERNS` (14 patterns, also used by Step 3) and `COMMERCIAL_FINALITY_PATTERNS` (6 Step-4-specific patterns: `ready for signature`, `approved by finance`, `final commercial terms`, `binding quote`, `binding offer`, `executed SOW`), plus a generic `scanForBannedClaims(fields, rules)` helper); `lib/ai/proposal-option-context.ts` (server-only context builder — engagement + account + proposal commercial-lever copy + target option + sibling options + approved findings + scored/selected opportunities + roadmap items + operator-reviewed report sections; no PII, no answer text, no file binaries, no Saipien Fit Score); `lib/ai/proposal-option-synthesis.ts` (JSON prompt + validator + combined banned-claim scanner — financial + commercial-finality patterns); `lib/proposals/synthesis-actions.ts` (`generateProposalOptionDraftAction({ engagementId, optionId })` — partial-field update touching only `title` + `best_fit_scenario` + `scope_summary` + `timeline` + `deliverables[]` + `assumptions[]` + `dependencies[]` + `risks[]`; **`pricing_placeholder`, `recommended`, `option_type`, `position`, `proposal_id`, link rows, and the entire `proposals` row are NEVER touched**; opens / closes `ai_synthesis_runs` with `run_type=proposal_option_draft`; emits `ai_proposal_option_drafted` activity event; revalidates the proposal page). `lib/ai/provider.ts` gains `getAiProposalOptionProviderConfig()` honoring the optional `SLATE_AI_PROPOSAL_OPTIONS_MODEL` env override. `lib/ai/report-section-synthesis.ts` (Step 3) is **refactored** to use the shared `claim-guard` so the 14 financial patterns live in one place — Step 3's behavior is unchanged. UI trigger in `components/proposals/proposal-option-action-bar.tsx`: a "Generate AI draft" button surfaces only when the engagement is a persisted UUID AND `isAiConfigured()` is true server-side. `ai_proposal_option_drafted` added to the `ActivityEventType` union + activity timeline with tone `ai`. **No schema / migration / DB column changes; no PDF / export / Send / Share / SOW / e-signature unlock; no proposal / proposal-status auto-change; no recommendation auto-flip; no public-scorecard changes; no package dependency changes.** Existing proposal actions (`approveProposal`, `markProposalNeedsReview`, `reopenProposal`, `updateProposalOption`, `markProposalOptionRecommended`, `updateImplementationCredit`, `linkProposalOption`, `unlinkProposalOption`) continue to use partial-field updates and never touch the AI-only columns — no regression. `npm run lint` clean; `npm run build` clean. **27 routes**; `/app/engagements/[id]/proposal` lands at `6.53 kB / 110 kB` (+0.6 kB / +1 kB vs Step 3 for the new client-side button + error/notice handlers); every other route is within 0 KB of the Step 3 baseline. `docs/14` / `docs/15` / `docs/17` did not need amendment.

**AI Synthesis Step 3 (2026-05-11) · report-section drafting** ships operator-triggered AI drafting for a single report section at a time. New: `lib/ai/report-section-context.ts` (server-only context builder — engagement + account + target section + sibling sections + approved findings + scored/selected opportunities + roadmap items + intake aggregates; no PII, no answer text, no file binaries, no Saipien Fit Score); `lib/ai/report-section-synthesis.ts` (deterministic JSON prompt with strict gating rules + validator + **banned-claim scanner** rejecting `guaranteed roi`, `guaranteed savings`, `payback`, `break-even`, `cash-flow positive`, `will save`, `will reduce cost`, `top quartile`, `above average`, `industry benchmark`, `peer benchmark`, `finance-approved`, `board-ready roi`, and a catch-all `guaranteed <financial>` pattern — any match aborts with `ai-claim-violation`); `lib/reports/synthesis-actions.ts` (`generateReportSectionDraftAction({ engagementId, sectionId })` — validates UUIDs + auth + AI config, opens `ai_synthesis_runs` row with safe input counts only, calls the pipeline, persists via **partial-field update** so `exhibit_slot` + link rows + reviewer note are preserved verbatim, demotes status to `needs_review` per `docs/17` § Acceptance Criteria, bumps timestamps, emits `ai_report_section_drafted` activity event, revalidates the route). `lib/ai/provider.ts` gains `getAiReportSectionProviderConfig()` honoring the optional `SLATE_AI_REPORT_SECTIONS_MODEL` env override. UI trigger lives in `components/reports/report-section-action-bar.tsx`: a "Generate AI draft" button surfaces **only** when the engagement is a persisted UUID AND `isAiConfigured()` is true on the server. Final sections are NOT draftable. Mock / legacy slug engagements never reach the AI path. `ai_report_section_drafted` added to the `ActivityEventType` union + `activity-timeline.tsx` with tone `ai`. **No schema / migration / DB column changes; no PDF / export / client-facing send / share / SOW; no proposal / public-scorecard changes; no package dependency changes.** `report_sections.exhibit_slot` behavior is preserved (the partial-field update never drops it; every other existing action remains partial-field too). `npm run lint` clean; `npm run build` clean. **27 routes**; `/app/engagements/[id]/report` lands at `7.5 kB / 111 kB` (+0.6 kB / +1 kB vs Sprint 2.1 for the new client-side button + error/notice handlers); every other route is within 0 KB of the Sprint 2.1 baseline. `docs/17` / `docs/14` / `docs/15` did not need amendment.

**Phase 1B Report Exhibit Wiring Sprint 2.1 (2026-05-11) · domain freshness surfacing** is a narrow cleanup: surface existing persisted `updated_at` / `last_reviewed_at` / `last_activity_at` timestamps through the TS-mapped domain shapes so chart adapters can show truthful `fresh` / `stale` / `unknown` freshness. **No schema, no migration, no new DB columns.** New: `latestIsoTimestamp()` helper in `lib/charts/adapters/types.ts` (pure, ignores null / unparseable, never throws, never calls `Date.now()`). Surfaced fields: `Opportunity.updatedAt` (from `opportunities.updated_at`); `Finding.updatedAt` + `Finding.lastReviewedAt` (from `findings.updated_at` and `findings.last_reviewed_at`); `RoadmapItem.updatedAt` (from `roadmap_items.updated_at`); `Stakeholder.lastActivityAt` (from `stakeholder_intake_sessions.last_activity_at`, alongside the existing display-only `lastActivity` string). `app/app/engagements/[id]/report/page.tsx` and `app/app/engagements/[id]/chart-diagnostics/page.tsx` both compute the best available `lastTouchedAt` per slot via `latestIsoTimestamp(...)` and pass it into the corresponding adapter call. Adapters are unchanged: same `generatedAt` + `lastTouchedAt` shape, same `deriveFreshness` 7-day threshold, same `"unknown"` fallback when `lastTouchedAt` is null. Mock fixtures continue to omit the new optional fields and fall through to `"unknown"` — the honest behavior. **`report_sections` / `exhibit_slot` / `ReportWorkspace` / proposal / public scorecard / PDF / AI / API / chart primitives / Group-B exhibits / package dependencies are all untouched.** `docs/17` did not need amendment. `npm run lint` clean; `npm run build` clean. **27 routes**; every route's First Load JS is within 0 KB of the Sprint 2 baseline — the freshness pipeline is pure server-side computation.

**Phase 1B Report Exhibit Wiring Sprint 2 (2026-05-11) · persisted report-section slot references** introduces the persisted reference model so `report_sections` rows name a Group-A `ReportExhibitSlot`. Chosen approach: **option (a) from `docs/17` § Report Wiring Sequence Sprint 2 — a nullable `exhibit_slot text` column on `report_sections`** with a SQL `CHECK` constraint enforcing the Group-A allowlist (`'executive_summary_portfolio'`, `'findings_risk_priority'`, `'diagnostic_capability_maturity'`, `'diagnostic_stakeholder_coverage'`, `'roadmap_90_day_sequence'`). Group B (`benchmark_comparison_bars`, `ai_savings_waterfall`, `roi_bridge`) is rejected at the SQL boundary. New: `supabase/migrations/0012_report_section_exhibit_slot.sql` — idempotent (`add column if not exists` + DO-block-guarded `add constraint` + partial `engagement_id, exhibit_slot` index + five null-only `UPDATE` backfills mapping canonical `section_type` → default Group-A slot per `docs/17`). Rollback documented inline (drop the constraint, drop the column). RLS unchanged. `lib/reports/slot-map.ts` gains `isGroupAReportSlot` (type predicate), `assertGroupAReportSlot` (lenient cast), `GROUP_B_REPORT_SLOT_IDS` (explicit Group-B list), `DEFAULT_SLOT_BY_SECTION_TYPE`, `defaultSlotForSectionType`, and `reportSlotDefinitionFor`. `lib/reports/types.ts` adds an optional narrowed-union `exhibitSlot?: ... | null` on `ReportSection`. `lib/reports/mappers.ts` routes the DB `exhibit_slot` string through `assertGroupAReportSlot` so a non-allowlisted value is coerced to `null` at the read boundary (defense-in-depth alongside the SQL CHECK). `lib/reports/queries.ts` adds `exhibit_slot` to `SECTION_SELECT`. `lib/reports/actions.ts` updates `initializeReportForEngagement` to seed `exhibit_slot: defaultSlotForSectionType(t)` on each section row; **every other action remains partial-field (`.update({ ... specific fields })`) so the slot is never accidentally dropped**. `components/reports/report-exhibit-slots.tsx` is rewired to take a `slots: ReportExhibitSlot[]` prop driven by persisted values (re-filtered through `isGroupAReportSlot`, deduplicated while preserving order) instead of iterating the static `GROUP_A_REPORT_SLOTS` table; the table becomes the title/description registry. When `slots.length === 0` the renderer mounts a "No persisted exhibit slots configured" notice rather than silently falling back to the static table. `app/app/engagements/[id]/report/page.tsx` derives `persistedSlotsInOrder` from `(report?.sections ?? []).map(s => s.exhibitSlot).filter(isGroupAReportSlot)` and passes it through. **`ReportWorkspace` is untouched.** `npm run lint` clean; `npm run build` clean. **27 routes** (unchanged); `/app/engagements/[id]/report` stays at `6.89 kB / 110 kB` (zero client-JS delta). Expected behavior: newly initialized reports auto-assign the five canonical slots; existing reports are backfilled by the migration; Group-B values are rejected at the SQL boundary and additionally filtered by the application-side predicate if they somehow bypass the CHECK. No proposal, public-scorecard, PDF / export, AI synthesis, API route, package, or chart-primitive file was touched. `docs/17` did not need amendment.

**Phase 1B Report Exhibit Wiring Sprint 1 (2026-05-11) · internal report-slot rendering** is the first sprint that touches the report builder. **It is operator-internal only:** PDF / export, "Send to client" / "Share", proposal surfaces, public scorecard, schema, AI synthesis, and package dependencies are untouched, and Group B (Benchmark Comparison Bars, AI-Savings Waterfall, ROI Bridge) is **not** wired into the report path. `ExecutiveSummaryTwoByTwo` is now prop-driven (`SAMPLE_DATA` moved into `app/app/charts-preview/page.tsx`; exhibit accepts the `ExecutiveSummaryPortfolioProps` shape produced by the Sprint 0B adapter; bubble-size dimension renamed `roi → impactSignal` to enforce canon-safe non-financial framing per `docs/15`; new optional `bubbleSizeLabel` prop lets the preview keep its proof-of-fit "annual ROI" copy while persisted callers default to neutral "business impact"). New `lib/reports/slot-map.ts` exports the authoritative Group-A `ReportSlotDefinition[]` table (5 slots, no DB / React imports, no Group-B rows). New `components/reports/report-exhibit-slots.tsx` is a pure server component that mounts the 5 typed `ChartAdapterResult` envelopes in slot order, renders ready exhibits via `isAdapterReady` narrowing, and renders explicit operator-facing fallback cards for `insufficient_data` / `invalid_data` / `gated` states with deep links to `/app/engagements/[id]/{opportunities,findings,intake,roadmap}`. **Fallback never falls back to sample data.** The report page at `app/app/engagements/[id]/report/page.tsx` is the only modified UI route: it now also fetches `getIntakeRecordForEngagement` (no new query helper added), computes a single `exhibitSlotsGeneratedAt` clock token, runs the five Group-A adapters inline, and renders `<ReportExhibitSlots />` between the existing `ReportWorkspace` and the Boundary Reminder card. Mock (legacy slug) engagements skip the exhibit-slot section entirely so Sprint 1 stays narrowly scoped to real persisted data. **`ReportWorkspace`, `report_sections` schema / persisted body / mutation paths, the action bar, every approve / needs-review / final action are unchanged** — Sprint 1 is display-only. Sprint 0B's `/app/engagements/[id]/chart-diagnostics` page was also updated to spread `{...executiveResult.props}` into the now-parameterized exhibit (the previous `adapterOnlyNote` workaround is removed). `npm run lint` clean; `npm run build` clean. 27 routes; `/app/engagements/[id]/report` stays at `6.89 kB / 110 kB` First Load JS (server-rendered SVG contributes zero client JS); every other route is within 0 KB of the Sprint 0B baseline. Expected adapter statuses against current persisted data: **Risk Quadrant `ready`**; **Executive Summary 2×2 `ready`** (the parameterization unblocked it); **Roadmap Gantt `ready` with two `info` issues** (`schedule_synthesized` + `status_mapping_unavailable`); **Capability Maturity `insufficient_data` · `untagged_findings`** (canon forbids synthesizing tags from prose); **Stakeholder Coverage `insufficient_data` · `no_topic_taxonomy`** (intake responses lack topic tagging today). The two `insufficient_data` slots render the canon-mandated fallback cards; the report page treats this as the honest current state, not a regression. `docs/17` did not need amendment. No schema, migration, API route, package dependency, exhibit primitive, proposal surface, public-scorecard surface, AI synthesis, or PDF / export file was changed.

**Phase 1B Report Exhibit Wiring Sprint 0B (2026-05-11) · adapter scaffolding + diagnostic surface** is the first implementation under the wiring canon. New: `lib/charts/adapters/types.ts` (canonical `ChartAdapterResult<TProps>` envelope, `ReportExhibitSlot` union, factory helpers, freshness helper with 7-day default, source-note helper); five Group-A adapters under `lib/charts/adapters/` (`executive-summary-2x2`, `risk-adjusted-priority-quadrant`, `capability-maturity-heatmap`, `stakeholder-coverage-matrix`, `roadmap-gantt`); operator-only diagnostic route at `app/app/engagements/[id]/chart-diagnostics/page.tsx`. All adapters are **pure functions**: no React, no Supabase / DB-client imports, no app-route imports, no `Date.now()` / `process.env`, no fetching / mutation, no exceptions for ordinary bad data. The `Risk Quadrant` adapter re-uses the exhibit-side `opportunityToRiskQuadrantPoint`. The `Capability Maturity` adapter recognizes that today's `Finding` shape lacks `(capability, dimension, maturityScore)` tagging and returns `insufficient_data` with `untagged_findings` — the canon's "never synthesize tags from prose" rule is enforced at the validation layer. The `Stakeholder Coverage` adapter requires a caller-supplied `responseCountFor(stakeholder, topic)` resolver; without one it returns `insufficient_data` with `no_topic_taxonomy`. The `Roadmap Gantt` adapter synthesizes a deterministic schedule from `phase` + array position and surfaces `schedule_synthesized` + `status_mapping_unavailable` info issues — the canon's "never invent" rule is preserved because the synthesis is disclosed and deterministic. The `Executive Summary 2×2` adapter establishes the prop contract Sprint 1 will lift into the (currently-internal-data) proof-of-fit exhibit. The diagnostic route at `/app/engagements/[id]/chart-diagnostics` is operator-only via `/app/*`, read-only, gated on UUID-persisted engagements, reuses existing `lib/<domain>/queries.ts` helpers without adding new ones, and renders each Group-A adapter envelope (status badge, freshness badge, source summary, issues list, live exhibit when `ready`). **Group B (Benchmark Comparison Bars, AI-Savings Waterfall, ROI Bridge) is explicitly omitted from the diagnostic surface** with a visible omission banner; they remain preview-only at `/app/charts-preview`. Source notes follow the canon-mandated patterns ("Source: Approved opportunities", etc.); no preview / illustrative / sample wording in the adapter or diagnostic code paths. `npm run lint` clean; `npm run build` clean. New route lands at `175 B / 96.1 kB`; every other route is within 0 KB of the Sprint 0A baseline. **`ReportWorkspace`, `report_sections`, proposal surfaces, public scorecard, PDF / export, AI synthesis, schema, package dependencies, exhibits, and primitives are all untouched.**

**Phase 1B Report Exhibit Wiring Canon — Sprint 0A (2026-05-11)** has landed at `docs/17_PHASE_1B_REPORT_EXHIBIT_WIRING_CANON.md`. Documentation-only canon governing how the accepted preview-library exhibits will move from `/app/charts-preview` into persisted SLATE reports. No source code, schema, migration, API, report, proposal, public, PDF, AI, or package files were touched. Locks **Group A** (five non-gated exhibits — Executive Summary 2×2, Risk-Adjusted Priority Quadrant, Capability Maturity Heatmap, Stakeholder Coverage Matrix, Roadmap Gantt with Dependencies — eligible for Sprint 0B adapter scaffolding) vs **Group B** (Benchmark Comparison Bars, AI-Savings Waterfall, ROI Bridge — preview-only until `docs/14` / `docs/15` advance their data gates). Defines a stable five-slot `ReportExhibitSlot` vocabulary (`executive_summary_portfolio`, `findings_risk_priority`, `diagnostic_capability_maturity`, `diagnostic_stakeholder_coverage`, `roadmap_90_day_sequence`), each mapped 1-to-1 to one Group-A exhibit. Fixes the pure-function adapter contract: adapters live under `lib/charts/adapters/`, never fetch / mutate / throw, never import React / DB / app routes, never invent sample data when persisted rows are absent — they return a typed `ChartAdapterResult<TProps>` envelope with `status` (`ready` / `insufficient_data` / `invalid_data` / `gated`), `props`, `issues`, and `sourceSummary` (source + rowCount + generatedAt + freshness). Defines data-freshness rules (7-day default stale threshold; stale data may render on operator diagnostics with a visible warning but never silently in PDF / report export; missing source timestamps treated as stale). Defines per-Group-A fallback rules — every slot has an explicit insufficient-data state with operator-facing copy + a deep link to the editing surface; never falls back to sample / illustrative data. Specifies the future operator-only diagnostic surface at the recommended path `app/app/engagements/[id]/chart-diagnostics/page.tsx` (read-only, Group-A only, reuses existing query helpers, never modifies `report_sections`, never client-facing, never exports PDF). Specifies report-wired source-note patterns (`Source: Approved opportunities · n=<count>`, etc.) plus the per-slot composed strings. Locks the staged wiring sequence — **Sprint 0A (this sprint) canon → Sprint 0B adapter scaffolding + diagnostic surface → Sprint 1 internal report-slot rendering (no PDF, no client-facing send) → Sprint 2 report-section model / constrained rich-text + exhibit references → Sprint 3+ PDF / export and client-facing delivery**. Group B explicitly blocked from wiring until the corresponding canon advances. **Sprint 0A authorizes only Sprint 0B's scope and nothing else.**

**Phase 1B Preview Library Acceptance Audit (2026-05-11)** has landed at `docs/16_PHASE_1B_PREVIEW_LIBRARY_ACCEPTANCE_AUDIT.md`. Formal acceptance of the eight-exhibit preview library before any report / proposal / public-scorecard / PDF wiring begins. Outcome: **Accepted with notes** for downstream report-wiring preparation. Verifies inventory (8 exhibits + 11 primitives + README + preview page + 3 canon docs); architecture compliance (SVG-only, zero canvas, zero `"use client"` in chart code, zero `@visx/*` imports outside `components/charts/`, zero primitive imports outside `components/charts/exhibits/`, no new package deps, static sample data only at the preview-page call site, 0 KB client JS for `/app/charts-preview`); canon / gating compliance (the three Gate-0 exhibits render the exact canonical illustrative source notes byte-for-byte and are confined to the preview route; no firm benchmark / ROI / savings / payback / break-even / financial-return claims appear anywhere outside canon prohibition text). Build + lint clean; every route is within 0 KB of the Sprint 7 baseline. Five polish items recorded in a backlog — none blocks downstream wiring. **No exhibit, primitive, or preview-page source code was changed for the audit.** Exhibits classified as **Group A** (five non-financial / non-benchmark exhibits — eligible for the next report-wiring sprint given persisted data already exists) and **Group B** (three Gate-0 financial / benchmark exhibits — gated on `docs/14` and `docs/15` reaching their next data tier). The audit recommends **Phase 1B Report Exhibit Wiring Sprint 0 — adapter and slot mapping** as the next sprint: author a wiring canon doc, scaffold the five Group-A adapters under `lib/charts/adapters/`, and add an operator-only diagnostic surface — explicitly stopping short of editing `ReportWorkspace`, `report_sections`, or any client-facing surface. Visual artifacts for all eight exhibits land in `artifacts/phase-1b-preview-library/` (gitignored; not committed).

**Phase 1B Exhibit Sprint 7 (2026-05-08) · Gate 0 illustrative only** shipped ROI Bridge at `components/charts/exhibits/roi-bridge.tsx` plus two new generic primitives: `components/charts/primitives/chart-projection-line.tsx` (polyline + optional marker dots; zero ROI / financial wording) and `components/charts/primitives/chart-band-area.tsx` (closed-area band traced upper L→R then lower R→L; zero domain wording). Multi-period (Y1 / Y2 / Y3) modeled-return range that visualizes a SENSITIVITY BAND (low / expected / high) rather than a single deterministic line — the cone-of-uncertainty stays visible at every period. The canon's no-collapsed-band invariant is enforced at the validation layer: `validateRoiBridgePoint` rejects `lowEstimate === expectedValue === highEstimate`, plus `lowEstimate > expectedValue` and `expectedValue > highEstimate`. Y-axis caption reads `MODELED RANGE · %`; per-marker labels are plain percentages — never "ROI", "return", "payback", or "break-even". The exhibit deliberately rejects "guaranteed ROI" / "payback in X months" / "break-even" / "cash-flow positive by" / "will return X%" / "board-ready ROI" framing. Exports `validateAssumptionSet`, `validateRoiBridgePoint`, and `defaultRoiBridgeSourceNote`. Default takeaway ("Illustrative ROI structure only; finance-approved assumptions are required before client-facing return claims."), legend status pill ("Illustrative · Gate 0"), and exact illustrative source note ("Source: Illustrative sample data · not a financial model") all carry the Gate 0 framing deliberately. Financial-assumption types (`FinancialAssumptionSet`, `FinancialConfidence`, `FinancialAssumptionStatus`) are intentionally duplicated from Sprint 6 so each exhibit stays data-shape-pure; both type sets match `docs/15` byte-for-byte and a future shared-types extraction sprint will lift them into `lib/charts/financial-types.ts` once a third financial exhibit exists. Pure server component; rendered as the eighth exhibit on `/app/charts-preview`. **MUST NOT be wired into reports, proposals, public-scorecard, or PDF until a Gate 1 / Gate 2 / Gate 3 assumption set exists.** **The Phase 1B preview library is now complete (eight of eight exhibits).** No package dependencies added; no financial data files; no schema. **`ChartPercentileBand` (Sprint 5), `ChartWaterfallBar` (Sprint 6), and the generic `ChartProjectionLine` + `ChartBandArea` (Sprint 7) primitives all remain Gate 0; the next Phase 1B workstream is wiring these exhibits into real persisted reports — gated on the canons reaching their next data tier.** All `/app/*` routes are auth-protected. Public scorecard submissions persist server-side with internal fit/lead derivation; the public response is type-narrowed to `PublicScoreResult` (no `fit` leak). The submit endpoint runs honeypot, minimum-duration, disposable-domain, and per-email/domain rate limiting; submissions and leads carry typed quality + trust metadata that surfaces only inside `/app/*` (small `LeadTrustChip` on the inbox + lead detail). `/app/leads*` and `/app/engagements*` read real Supabase rows under operator-only RLS via the authenticated server client. `Start AI Opportunity Sprint` creates / reopens a real engagement, idempotent on `linked_lead_id`. Operators mint token-gated stakeholder intake links; stakeholders submit responses on the public `/intake/[token]` route. Operators author real findings tied to stakeholder intake evidence; review actions persist. Operators score opportunities from approved findings (quadrant + priority derived server-side from impact + complexity + risk) and manually sequence selected opportunities into a 30/60/90 roadmap. Operators initialize a 12-section report and 3-option proposal per UUID engagement; section approve / needs-review / final actions and option recommendation persist; implementation credit lands as a bounded commercial lever defaulted to canonical commercial-lever copy. Production export, SOW draft, send-to-client, and e-signature stay behind `LockedActionButton`. **Operators have an operator-only auditable activity trail across every Step 4–8 action plus internal notes on real leads + engagements (soft delete, pin/unpin, edit in place). Logging is best-effort and never blocks the primary action. Public scorecard and stakeholder intake routes never expose notes or activity rows.** **Step 10 wires real binary file storage: stakeholders can attach supporting documents on the public `/intake/[token]` page, operators can upload internal documents on the engagement intake workspace, and operators download via 5-minute signed URLs through `/api/app/assets/[id]/download`. Bucket `engagement-documents` is private (10 MiB cap, 9-MIME allowlist); all access flows through server-only helpers under the existing token + auth boundary — no `storage.objects` policies are added.** **AI Synthesis Step 1 layers operator-triggered draft finding generation on top: `Generate draft findings` on `/app/engagements/[id]/findings` calls a server-only OpenAI adapter (`fetch`-based, no SDK), validates JSON output against allowlists, persists 3–7 candidates as `ai_drafted = true` / `needs_review`, and records a safe-summary `ai_synthesis_runs` row plus an `ai_findings_generated` activity event. Uploaded files are not parsed — only metadata reaches the model. Drafts must pass operator approval before becoming report-ready.** **AI Synthesis Step 1.1 surfaces the synthesis surface from the engagement workflow without overriding the intake-first recommended action: the engagement stage tracker shows a small `Sparkles` next to the synthesis stage, the Findings status panel shows an `AI draft available` badge plus an evidence-aware readiness footnote, and the page-level "Limited evidence" warning gained a `Manage intake first ↗` deep link. AI affordances render only on real persisted UUID engagements when `OPENAI_API_KEY` is configured server-side.** `lib/leads/mock-leads.ts` is retired; `lib/engagements/mock-engagements.ts`, `lib/intake/mock-intake.ts`, `lib/findings/mock-findings.ts`, `lib/opportunities/mock-opportunities.ts`, `lib/roadmap/mock-roadmap.ts`, `lib/reports/mock-reports.ts`, and `lib/proposals/mock-proposals.ts` are retained as fixtures for legacy slug-keyed demo paths only — those paths skip the notes + activity queries and the persisted assets panel entirely.

**Render-prop serialization blocker fix (2026-05-13)** shipped at commit `1099064 — Fix report and proposal workspace action bars`. Removed two latent render-prop closures that crossed the server-to-client component boundary in `components/reports/report-workspace.tsx` and `components/proposals/proposal-workspace.tsx` (Next.js 14 forbids functions as props on `"use client"` components at runtime). The server pages (`app/app/engagements/[id]/report/page.tsx` and `app/app/engagements/[id]/proposal/page.tsx`) now pass JSON-safe primitives (`showActionBar` / `engagementId` / `aiAvailable`) and the workspaces import their respective action-bar components inline. Migration `0012_report_section_exhibit_slot.sql` was applied to the live Supabase project via `scripts/dev/apply-migration.cjs` (HTTP 201) before the refactor landed — the prior live error was PostgREST `42703 column "report_sections.exhibit_slot" does not exist`. Four files changed (+77 / -32). All four `LockedActionButton` instances unchanged; AI gating preserved inside the action-bar components; mock-engagement gate preserved. `npm run lint` clean; build clean (28 routes; `/app/engagements/[id]/proposal` +0.02 kB to `6.55 kB / 110 kB`).

**Sprint 3 walkthrough (2026-05-13)** landed against Sapient Digital persisted UUID engagement `76097653-fedb-42e5-9ef6-e89a0e97f802`. AI Synthesis Steps 4 and 5 fully live-verified against the real `OPENAI_API_KEY`-configured pipeline: case 4-A produced a completed `proposal_option_draft` run (provider=`openai`, model=`gpt-4o-mini`, ~6.5 s) with `pricing_placeholder` / `recommended` / `option_type` / `position` byte-identical pre/post and `scope_summary` rewritten as a richer 3-sentence paragraph; case 5-A produced a completed `roadmap_draft` run (~13 s) inserting 3 planned items with deterministic per-phase positions plus per-item `roadmap_item_created` events + batch `ai_roadmap_items_drafted` summary event. Cases 3-D / 4-E / 5-F verified the mock-fixture AI-button-hiding gate against `atlas-aios-q2`. Case 3-A blocked by the orphan empty `reports` row on the Sapient Digital engagement (cleanup SQL held per operator direction; documented in `artifacts/walkthroughs/sprint-3-print-preview/blocker-fix-notes.md`). 3-B / 3-C / 3-E / 4-D / 4-F / 5-C / 5-E / 5-G skipped (env-churn / no-safe-stub) and static-verified per `docs/18`. 5-D not triggered (model produced 3 items from engagement metadata + proposal-option context even with 0 findings / 0 opportunities, so the natural empty state was insufficient to force `no-items-persisted`). Sprint 3 print preview visual review on `/app/engagements/76097653-…/report/print`: all canonical chrome rendered — internal banner with full canon copy, identity card with both UUIDs (`ENGAGEMENT ID 76097653-…` · `REPORT ID 783b8335-…`), `NO SECTIONS` empty state, `REPORT EXHIBITS · INTERNAL PREVIEW` section with Phase 1B · Sprint 2 badge, `NO EXHIBIT SLOTS CONFIGURED` notice referencing migration `0012`, `GROUP B OMITTED` card naming the three Phase 1B / Sprint 2 exhibits. All `LockedActionButton` controls remained locked throughout. Full notes at `artifacts/walkthroughs/sprint-3-print-preview/walkthrough-notes.md`.

**Phase 1B Client Delivery Canon — Sprint 4A (2026-05-13)** has landed at `docs/19_PHASE_1B_CLIENT_DELIVERY_CANON.md`. **Documentation-only sprint.** No source code, schema, migration, API route, package, AI synthesis, chart primitive, storage policy, or public-link infrastructure was created or modified. No `LockedActionButton` was unlocked. Canon defines a six-surface delivery taxonomy (internal preview · internal generated PDF · client-facing report PDF · client-facing share link · proposal / SOW document · e-signature), a locked-control unlock-prerequisites policy across all five locked controls, a four-gate report readiness ladder (R0 internal preview → R3 client delivery enabled), Group-A vs Group-B exhibit inclusion rules (Group-A eligible after R2 with omit-by-default fallback; Group-B blocked from every client surface until `docs/14` / `docs/15` advance), an export-time claim-safety scan policy reusing `lib/ai/claim-guard.ts`'s 26 patterns, a snapshot / versioning model outline, access / storage / share-link rules, light-theme / print-quality rules, AI-drafted content rules, and the staged Sprint 4 sequence (Sprint 4A canon-only [this sprint] → DB hygiene micro-step → Sprint 4B internal print polish + light-theme Group-A palette → Sprint 4C client-safe report PDF candidate → Sprint 4D controlled client-facing delivery / share → separate sprints for Proposal/SOW canon, Benchmark Gate 1, Financial Gate 1, and explicitly later e-signature / CRM). **Canon authorizes only the DB hygiene micro-step and Sprint 4B's scope; nothing else.**

**DB hygiene micro-step (2026-05-13)** completed. Read-only preflight SQL verified the orphan empty Sapient Digital `reports` row matched on both `id=783b8335-…` AND `engagement_id=76097653-…`, had 0 `report_sections`, no link-row dependents, and was the only `reports` row for that engagement. A single guarded DELETE (matching id + engagement_id + `NOT EXISTS report_sections`) removed exactly 1 row; post-delete count = 0 for the target id and for the engagement. Re-initialization through the `Initialize report` form on `/app/engagements/76097653-…/report` created a new `reports` row (`08bdee36-…`) plus 12 sections at `status='not_started'` with the 5 canonical Group-A `exhibit_slot` values populated correctly (pos 0 `executive_summary` → `executive_summary_portfolio` · pos 3 `readiness_assessment` → `diagnostic_capability_maturity` · pos 5 `stakeholder_synthesis` → `diagnostic_stakeholder_coverage` · pos 7 `priority_recommendations` → `findings_risk_priority` · pos 9 `roadmap` → `roadmap_90_day_sequence`). `docs/18` Step 3-A re-ran live against the Executive Summary section: `ai_synthesis_runs` row `bd8612e9-…` completed (`provider=openai`, `model=gpt-4o-mini`, ~1.55 s) with `input_summary.exhibitSlot=executive_summary_portfolio` and `output_summary.{assumptionsCount: 3, evidenceNotesCount: 3}`; section row demoted to `status='needs_review'`, `exhibit_slot` preserved verbatim, `ai_drafted=true`, `reviewed_by` + `last_reviewed_at` populated; other 11 sections byte-identical pre/post; activity event `ai_report_section_drafted` emitted with full metadata. **All three AI synthesis paths (Step 3 · Step 4 · Step 5) are now live-verified.** During the operation a transient `.next/` cache corruption surfaced (Cannot find module './vendor-chunks/next.js'); cleared `.next/` and restarted dev server — no source change required. Working tree clean except `.claude/` and `artifacts/` (both gitignored).

**Phase 1B Report Exhibit Wiring Sprint 4B (2026-05-13) · internal print polish + light-theme Group-A palette** ships an operator-only light/print-safe rendering path for `/app/engagements/[id]/report/print` per `docs/19` § Proposed Sprint Sequence. **Approach chosen: Option C — CSS-variable override scope** (smallest coherent surface). Every Group-A exhibit + every SLATE UI primitive (badges, cards, text tokens) already routes through `var(--color-*)` CSS custom properties defined at `:root` in `styles/globals.css`, so a single scoped class `.slate-print-light` on the print route's outermost wrapper overrides those variables for the print document subtree and the cascade re-themes embedded SVG charts + chrome with zero prop additions, zero exhibit-file changes, zero chart-primitive changes, zero adapter changes, zero `ReportExhibitSlots` change, and zero new dependency. Three files changed: `styles/globals.css` (+82 lines: `.slate-print-light` scope with light CSS-variable overrides + `@media print { ... }` block forcing pure white + dropping shadows); `app/app/engagements/[id]/report/print/page.tsx` (+3 / -3: three return paths wrapped in `slate-print-light` + `print:bg-white` + `print:p-0`); `components/reports/report-print-document.tsx` (+24 / -2: new `OperatorPrintHint` aside with `print:hidden` for inline Save-as-PDF instructions on screen only, tightened `InternalBanner` border opacity for stronger on-paper contrast, `print:max-w-none` on outer wrapper, `print:break-before-page` on exhibits region). **No exhibit, primitive, adapter, ReportExhibitSlots, AI synthesis, schema, migration, API, package, public-scorecard, proposal, or locked-control file modified.** Light palette: white surfaces, slate-900/700/600/400 text scale, slate-700 status mid-tones (teal · amber · red · blue), subtle drop-shadows on-screen → none under print media. Default dark behavior preserved everywhere outside the wrapper class — spot-verified on `/app/charts-preview`, `/app/engagements/76097653-…/report`, and `/app/engagements/76097653-…/chart-diagnostics`. All four `LockedActionButton` instances + the `Prepare Client Review` chip remain locked verbatim; `Internal preview PDF` ghost button stays distinct from `Export Report Locked` per `docs/19` § Locked Control Policy anti-pattern. Group-B exhibits (Benchmark Comparison Bars / AI-Savings Waterfall / ROI Bridge) not imported on the print route; `GROUP B OMITTED` card renders in light theme. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **28 routes** (unchanged); every route's First Load JS byte-identical to the post-Sprint-4A baseline — confirms the CSS-variable cascade approach added zero client-bundle weight. Walkthrough artifacts at `artifacts/walkthroughs/sprint-4b-print-polish/notes.md` (gitignored).

**Phase 1B Report PDF Candidate Canon — Sprint 4C-A (2026-05-13)** has landed at `docs/20_PHASE_1B_REPORT_PDF_CANDIDATE_CANON.md`. **Documentation / decision-only sprint.** No source code, schema, migration, API route, package, AI synthesis change, chart primitive, storage policy, or unlock of any locked control was made. Canon resolves the three blocker decisions for Sprint 4C implementation: **PDF pipeline (recommended Option A — native browser print-to-PDF + metadata-only snapshot; Option B server-side library deferred to a separate dependency canon); snapshot schema (recommended Option A — new `report_delivery_snapshots` table, columns defined conceptually); storage posture (recommended metadata-only initially; private-bucket binary storage gated by a future canon).** Defines R2 readiness gate (9 mandatory checks; default rule: operator-only candidate may include `needs_review` sections if `draft_watermark=true`, client-ready PDF requires approved/final only); export-time claim-guard scan strategy (26-pattern scan over title + section fields + exhibit takeaways + appendix notes; failure records violations on snapshot and blocks generation); Group-A omit-by-default fallback rule + Group-B excluded entirely with machine-readable confirmation in `omitted_exhibits`; `Export Report` unlock boundary (locked throughout 4C-A; eventually renamed `Generate PDF Candidate` and unlocked only when R2 implementation lands; `docs/19` § Locked Control Policy anti-patterns reaffirmed); snapshot content (identity + per-section snapshot + per-exhibit snapshot + source summary digest + claim-guard result + omitted exhibits + Group-B omission entry + app/commit version; raw prompts/model output/PII excluded); artifact marking (surface designation · timestamp · UUID identity strip · Group-B omission notice · operator identity · draft watermark · stale-data warnings); access/storage rules (no public route, no share token in Sprint 4C, no public bucket, signed-URL TTL ≤ 15 min if storage added later, no embedded private asset URLs); four new activity event types defined for Sprint 4C-B (`report_pdf_candidate_generated` · `_failed` · `_downloaded` · `report_delivery_snapshot_voided`); and the staged implementation plan (single-sprint Sprint 4C-B if Option A approved, or split Sprint 4C-B/C/D if Option B / open decisions deferred). Nine open decisions enumerated for operator approval before implementation begins. **Canon authorizes only the next implementation sprint if accepted; nothing else.**

**Phase 1B Report Exhibit Wiring Sprint 4C-B (2026-05-14) · client-safe report PDF candidate implementation** ships the first implementation under `docs/20`. **Operator-only metadata-only snapshot pipeline.** No PDF library dependency added. No PDF binary stored. No public/share/send/SOW/e-sign unlocked. `Send to Client`, `Prepare SOW Draft`, `Prepare Client Review`, and `Prepare Report` remain locked verbatim. New migration `0013_report_delivery_snapshots.sql` (table + 4 indexes + workspace-scoped operator-full RLS + self-contained `before update` trigger; nullable `artifact_*` columns reserved for future Option B) applied to live Supabase (HTTP 201). New TS layer: `lib/reports/delivery-snapshot-types.ts` + `delivery-snapshot-mappers.ts` + `delivery-snapshot-queries.ts`. New pure-function readiness evaluator `lib/reports/report-pdf-readiness.ts` (9 R2 checks; `included` partition for approved/final + needs_review/drafted-with-watermark; `omitted` for not_started + insufficient/invalid/gated/stale_rejected; always emits canonical `group_b_block` omission entry). New export-time scan `lib/reports/report-pdf-claim-guard.ts` (reuses `lib/ai/claim-guard.ts`'s 26-pattern scanner over title + per-included-section title/summary/draftPreview/evidenceNotes + per-rendered-exhibit sourceSummary + omission appendix; reviewer notes excluded by default). New server actions at `lib/reports/pdf-candidate-actions.ts`: `generateReportPdfCandidateAction` runs the 5 Group-A adapters with the same pipeline as `/report/print`, evaluates readiness, runs claim guard, inserts the snapshot row, logs `report_pdf_candidate_generated` activity event; failure paths log `report_pdf_candidate_failed` with `failureReason: r2_check_failed | stale_acceptance_required | claim_guard_violation` and never insert a snapshot. Companion `voidReportDeliverySnapshotAction` soft-voids and emits `report_delivery_snapshot_voided`. Four new activity event types + 1 new entity type in `lib/activity/types.ts` + timeline tone/labels in `components/activity/activity-timeline.tsx`. New candidate route at `app/app/engagements/[id]/report/pdf-candidate/[snapshotId]/page.tsx` (operator-only, `/app/*` gated, persisted-UUID-only, defense-in-depth `snapshot.engagementId === params.id`, wrapped in Sprint 4B's `.slate-print-light` CSS-variable scope). New component `components/reports/report-pdf-candidate-document.tsx` (operator hint, amber candidate banner, identity strip with UUIDs + operator label, claim-guard strip, red Draft Candidate watermark when `draft_watermark=true`, Sections list from `section_snapshot` jsonb only (snapshot purity preserved — no live row re-query), Report Exhibits list showing `renderedInArtifact=true` slots with source-summary + freshness chips (live SVGs deliberately omitted), Omitted Exhibits appendix with canonical Group-B confirmation + each Group-A omission with adapter reason, footer with snapshot id). New client trigger `components/reports/generate-pdf-candidate-button.tsx` (`useTransition`, success notice with `Open candidate` link + draft-watermark badge, failure notice with violations/blockers/stale slots). `app/app/engagements/[id]/report/page.tsx` replaces `LockedActionButton label="Export Report"` with `<GeneratePdfCandidateButton>` for persisted UUID engagements only; mock paths keep the locked chip. **No Group-B exhibit imported by the new code** (grep verified). `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **29 routes** (was 28 — new `/report/pdf-candidate/[snapshotId]` at `181 B / 96.1 kB`); `/report` grows from `7.5 kB / 111 kB` → `8.85 kB / 112 kB` for the new client button; every other route within 0 KB of the post-Sprint-4B baseline. Live test against `76097653-fedb-42e5-9ef6-e89a0e97f802` produced snapshot `580db034-b3eb-4f6c-bbe6-814b03bcc13e` (status=candidate, delivery_surface=client_pdf_candidate, draft_watermark=true, claim_guard_passed=true, 11 fields/26 patterns/0 violations, included 1 section + 1 exhibit, omitted 5 [Group-B canonical + 4 Group-A insufficient_data]); activity event `report_pdf_candidate_generated` logged with full metadata; candidate route renders with light/print theme inherited from Sprint 4B scope. Walkthrough artifacts at `artifacts/walkthroughs/sprint-4c-pdf-candidate/notes.md`.

**Phase 1B Report PDF Candidate Acceptance Audit — Sprint 4C-C (2026-05-14)** has landed at `docs/21_PHASE_1B_REPORT_PDF_CANDIDATE_ACCEPTANCE_AUDIT.md`. **Audit / read-only sprint.** No source code modified. No PDF library added. No PDF binary produced. No storage bucket added. No client delivery / share / send route added. No locked client-facing control unlocked. **Outcome — Accepted with notes for operator-only candidate use.** Migration / RLS audit verified the canonized 27-column / 5-index / 1-policy / RLS-enabled / 2-CHECK / 1-trigger shape of `report_delivery_snapshots`; `artifact_*` columns confirmed nullable. Happy-path snapshot `580db034-…` from Sprint 4C-B re-probed and still intact (`status='candidate'`, `claim_guard_passed=true`, 11 fields / 26 patterns / 0 violations). Candidate route re-rendered correctly in light/print theme (screenshot `ss_2162h819i`) — operator hint, amber CLIENT-SAFE PDF CANDIDATE banner, identity strip with all 3 UUIDs + GENERATED BY initial, green CLAIM GUARD Passed strip, red Draft Candidate watermark, Sections (1) with Executive Summary card. R2 readiness partitioning, claim-guard rejection path, approved/final R2 path, and `voidReportDeliverySnapshotAction` all code-read verified. **Three live tests deferred to Sprint 4C-D backlog** — live banned-phrase rejection, live approved/final regeneration, live void — due to (a) dev-server instability in this audit session and (b) the auto-mode classifier correctly denying a direct-SQL mutation fallback under the user-spec "stop and ask approval before destructive or risky edits" rule. Group-B audit clean: 4 files only (3 exhibits + charts-preview); zero references in the candidate route. Locked controls audit: 5 instances preserved (`Prepare SOW Draft`, `Send to Client`, `Prepare Report`, `Prepare Client Review`, `Export Report` for mock paths); persisted UUID engagements get `<GeneratePdfCandidateButton>` instead — no anti-patterns from `docs/19` § Locked Control Policy triggered. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean (29 routes; byte-identical sizes vs Sprint 4C-B). `git status` zero source drift. Walkthrough artifacts at `artifacts/walkthroughs/sprint-4c-pdf-candidate-audit/notes.md`.

**Phase 1B Report Exhibit Wiring Sprint 4C-D (2026-05-14) · PDF candidate polish + acceptance backlog closure** shipped. **Operator-only.** No PDF library, no PDF binary storage, no public/share/send/SOW/e-sign unlocked. Five new / extended files: `components/reports/report-pdf-candidates-panel.tsx` (new server-rendered Past Candidates panel with per-row identity strip + status / delivery-surface / draft-watermark / claim-guard / stale-accepted indicators + counts + Open candidate link + per-row Void button); `components/reports/void-pdf-candidate-button.tsx` (new client component — two-step confirm; default reason "Superseded by a newer candidate."; calls `voidReportDeliverySnapshotAction`); extended `components/reports/generate-pdf-candidate-button.tsx` (new `StaleAcceptancePrompt` checkbox-per-slot acceptance flow; threads `acceptedStaleSlots: ReportExhibitSlot[]` into the action); extended `components/reports/report-pdf-candidate-document.tsx` (new `VoidedBanner` red block surfaced above the candidate banner when `status='voided'`; new `StaleAcceptanceNote` amber block listing accepted stale slots); extended `app/app/engagements/[id]/report/page.tsx` (mounts `<ReportPdfCandidatesPanel>` between exhibit slots and boundary reminder, persisted-UUID-only). No migration. **Live tests:** **Approved/final no-watermark test PASSED** — Executive Summary section promoted via the existing section action bar's `Approve section` button; new snapshot `9068f58f-…` generated with `draft_watermark=false`; candidate route rendered without the red watermark; section reverted to `needs_review` via the `Needs review` button (state fully restored). **Void test PASSED end-to-end** — clicked Void on snapshot `580db034-…` via the new panel; default-reason inline form revealed; Confirm void clicked; snapshot row flipped to `status='voided'` with full audit columns populated; `report_delivery_snapshot_voided` activity event `e27ebab1-…` emitted; snapshot row NOT deleted (audit preserved); candidate route shows red `SNAPSHOT VOIDED · DO NOT DELIVER` banner with original content preserved below. **Stale-acceptance UX implemented; live-test deferred** (no naturally stale slots on the test engagement today). **Banned-phrase live test deferred** (auto-mode classifier denied raw SQL section-text mutation, matching Sprint 4C-C precedent; static verification stands). **Reviewer-note opt-in deferred** (5-file coordinated change exceeds polish-sprint scope; reserved for a future sprint). All 5 locked controls remain locked verbatim. Group-B confined to the same 4 files; `docs/14` / `docs/15` gates unchanged. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **29 routes** (unchanged); `/report` grows from `8.85 kB / 112 kB` → `9.98 kB / 113 kB` for the new client void button + extended Generate button; every other route within 0 KB of the Sprint 4C-C baseline. Walkthrough artifacts at `artifacts/walkthroughs/sprint-4c-d-candidate-polish/notes.md`.

**Phase 1B Client Share Delivery Canon — Sprint 4D-A (2026-05-14)** has landed at `docs/22_PHASE_1B_CLIENT_SHARE_DELIVERY_CANON.md`. **Documentation / decision-only sprint.** No source code, schema, migration, API route, package, token-generation pipeline, public route, storage policy, email pipeline, CRM integration, PDF binary, e-signature, or AI synthesis change. `Send to Client`, `Prepare SOW Draft`, `Prepare Client Review`, `Prepare Report` remain locked verbatim. Group-B exhibits remain excluded. Canon defines a seven-surface delivery taxonomy (with client share link as the recommended Sprint 4D-B scope), strict snapshot eligibility rules (non-voided + non-watermarked + claim_guard passing + Group-B absent + every included section `approved`/`final` + empty `acceptedStaleSlots` + age ≤ 14 days; enforced at link creation AND link render), an opaque-random token model (≥ 256 bits, SHA-256 hashed at rest, one-time-display semantics, `/r/[token]` recommended URL shape), a conceptual `report_share_tokens` schema (workspace/engagement/report/snapshot FKs + `token_hash` unique + 3-state `status` CHECK + `audience_label` + optional `recipient_email_hash` + `expires_at` NOT NULL + soft-revoke columns + `access_count` + `last_accessed_at` + `metadata` jsonb + workspace-scoped operator-full RLS), default expiry 14 days / max 30 days / no never-expire / regeneration ≠ link mutation, revocation policy with immediate disable + audit trail preservation + void-cascade, a minimal single-audience model (no client login / portal / comments / approvals / e-signature / SOW acceptance), public route security (server-side lookup only, generic identical rejection page across every failure mode, `noindex,nofollow` + `Cache-Control: no-store`, no client-side Supabase/API access), snapshot-pure content rendering (no live re-query; reviewer notes excluded; first implementation renders exhibits as source-summary cards only — no live SVG; Group-B omission notice non-negotiable; claim-guard shown affirmative-only; internal IDs hidden; required client-visible footer with the four denials — "not a SOW · not a binding quote · not a financial guarantee · not a contract"), four new audit event types (`report_share_token_created` · `_revoked` · `_accessed` · optional `_expired`) with hashed IP+UA logging and per-(tokenId, ipHash) debounce, a `Send to Client` unlock policy that requires all four prior Sprint 4D sprints landed + explicit operator authorization (last control unlocked), Export Report interaction rules (operator-only flow unchanged; token bound to one snapshot for lifetime; snapshot voiding cascades to public access; new candidate ≠ updated share link), storage policy (no PDF binary in first share implementation; future stored binaries require a separate `docs/23` storage canon; no public bucket; signed-URL TTL max 15 min per `docs/19`; route renders HTML never `302`-redirects to storage), rate-limit / abuse controls (per-IP and per-token render limits, generic rejection messages, CAPTCHA deferred), benchmark/financial/commercial claim policy (Group-B excluded; `claim_guard_result.passed=true` required; client footer mandatory; proposal/SOW separate), and the **staged sequence**: Sprint 4D-A canon-only [this sprint] → Sprint 4D-B `report_share_tokens` migration + token service + operator-only Generate Share Link action [no public route yet, Send to Client stays locked] → Sprint 4D-C public `/r/[token]` route with sanitized snapshot render + access logging + rate limiting → Sprint 4D-D share management UI + revoke flow + void-cascade wiring → Sprint 4D-E (optional) Send to Client unlock with separate channel canon → separate sprints for Proposal/SOW canon · Benchmark Gate 1 · Financial Gate 1 · explicitly later e-signature/CRM. Ten **open decisions** enumerated with recommended defaults committed by this canon: URL shape `/r/[token]`, default 14-day expiry, 30-day max, optional recipient email hash, link-creation-only first implementation (no email send), hidden internal IDs in client artifact, hashed IP+UA access logging with server-side pepper, stale-accepted snapshots NOT share-eligible in first implementation, browser print/save allowed (HTML-side), operator label "Client Report Link". **Canon authorizes only the next implementation sprint if accepted.**

**Phase 1B Report Exhibit Wiring Sprint 4D-B (2026-05-14) · share-token foundation** shipped. **Operator-only.** No public `/r/[token]` route added (Sprint 4D-C scope). All five locked client-delivery controls (`Send to Client`, `Prepare SOW Draft`, `Prepare Client Review`, `Prepare Report`, `Export Report` for mock paths) remain locked verbatim. No email send, no CRM push, no SLATE-mediated delivery. Group-B exhibits remain excluded from every share-eligible snapshot. **Migration `0014_report_share_tokens.sql`** — `public.report_share_tokens` (17 columns: workspace/engagement/report/snapshot FKs + `token_hash` unique + `status` CHECK (`active|revoked|expired`) + `audience_label` + optional `recipient_email_hash` + `expires_at` NOT NULL + soft-revoke columns + `access_count` int default 0 + `last_accessed_at` + `metadata` jsonb + `created_by_label` + `updated_at`); three idempotent DO-block-guarded CHECK constraints (`expires_at > created_at` + SHA-256 hex shape on `recipient_email_hash` and `token_hash` defending against accidental raw-email / raw-token persistence); six indexes (unique on `token_hash` + workspace/engagement/report/snapshot/status_expires); self-contained `updated_at` trigger; workspace-scoped operator-full RLS policy mirroring `0013`. Applied to live Supabase (HTTP 201). **New TS layer:** `lib/reports/share-token-types.ts` (`ReportShareToken` shape + `ReportShareTokenStatus` union + `ReportShareEligibility` + `DEFAULT_SHARE_TOKEN_EXPIRY_DAYS=14` / `MAX_SHARE_TOKEN_EXPIRY_DAYS=30` / `MAX_SNAPSHOT_AGE_DAYS_FOR_SHARE=14`); `lib/reports/share-token-mappers.ts` (defensive jsonb validation; unknown status falls through to `"expired"`); `lib/reports/share-token-queries.ts` (`getReportShareTokensForSnapshot` / `getReportShareTokensForReport` / `countActiveShareTokensForSnapshot`). **New pure token service `lib/reports/share-token-service.ts`** — `generateRawShareToken()` uses `crypto.randomBytes(32).toString("base64url")` for 256-bit entropy and a 43-char URL-safe encoding; `hashShareToken(raw)` produces SHA-256 hex lowercase (matches the migration's CHECK contract); `calculateShareTokenExpiry({days?, now?})` defaults to 14 days; `isExpiryWithinPolicy(iso, {maxDays?})` enforces the 30-day max; `hashRecipientEmail(raw)` trims + lowercases before SHA-256. **New pure eligibility evaluator `lib/reports/share-token-eligibility.ts`** — `evaluateReportShareEligibility(snapshot, {now?, maxAgeDays?})` returns `{eligible, reasons[], evaluatedAt}` with ALL applicable reasons (not first-fail). Disqualifies: voided / non-`client_pdf_candidate` / `draft_watermark=true` / claim-guard failed / missing Group-B omission entry / age > 14 days from `generated_at`. (Status `candidate` is the operational status for Sprint 4C/4D — `generated` is reserved for a future sprint when real PDF binaries land; eligibility disqualifies `voided` only.) **New action `lib/reports/share-token-actions.ts`** — `generateShareLinkAction({snapshotId, audienceLabel?, recipientEmail?, expiryDays?})` validates UUID + auth, re-evaluates eligibility server-side (UI gate is defense-in-depth, not the authority), generates raw token + hash, inserts `report_share_tokens` row with `status='active'`, logs `report_share_token_created` activity event with `metadata: {snapshotId, reportId, audienceLabel, recipientHashPresent, expiresAt}` — deliberate key name `recipientHashPresent` (NOT `hasRecipientEmailHash`) because the activity logger sanitizer strips any key matching `/email/i`. **Returns the raw token in the action result exactly once.** Audience-label sanitization strips ASCII control chars (`0x00-0x1F`, `0x7F`) and collapses whitespace runs. **Four new activity event types** in `lib/activity/types.ts`: `report_share_token_created` (tone `brand`) · `report_share_token_revoked` (tone `warning`) · `report_share_token_accessed` (tone `info`) · `report_share_token_expired` (tone `neutral`); plus the new `report_share_token` entity type. Timeline labels added to `components/activity/activity-timeline.tsx`. **New client component `components/reports/generate-share-link-button.tsx`** — three states: (1) ineligible → "Share disabled" neutral chip + bulleted reasons; (2) eligible idle → "Generate share link" secondary button; (3) just-generated → copy-once panel (status-success surface) showing the raw token in a `<code class="select-all">` with Reveal/Hide toggle, Copy button (uses `navigator.clipboard.writeText`, flashes "Copied" for 2.4s), "Public route lands in Sprint 4D-C" warning badge, and expiry timestamp. The raw token lives only in component-local React state — never in URL, never in localStorage / sessionStorage, never re-fetchable. `components/reports/report-pdf-candidates-panel.tsx` extended to evaluate eligibility per row server-side and propagate `shareIneligibilityReasons: ReportShareEligibilityReason[]` into each `SnapshotRow`. **Live verification against `76097653-fedb-42e5-9ef6-e89a0e97f802`:** Past Candidates panel renders the Generate button for snapshot `9068f58f-…` (candidate / client-safe / claim guard passed / draft watermark off) and the Share disabled chip + two reasons for snapshot `580db834-…` (voided + draft watermark). Three Generate Share Link clicks during testing produced three fresh 43-char base64url tokens (each unique); copy-once panel rendered with full control set; three `Share link created` activity events landed in the engagement timeline (no raw token in any entry). `curl http://localhost:3000/r/test-token-noop` → HTTP 404 confirming Sprint 4D-C scope intact. Dev server log clean across the test session. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **25 app routes** (unchanged); `/app/engagements/[id]/report` grows from `11.1 kB / 114 kB` → `11.2 kB / 114 kB` (+0.1 kB for the new client share-link button mounted per row); every other route within 0 KB of the Sprint 4D-A baseline. Walkthrough artifacts at `artifacts/walkthroughs/sprint-4d-b-share-token-service/notes.md`.

**Phase 1B Report Exhibit Wiring Sprint 4D-C (2026-05-14) · Client Report Link MVP** shipped. **First sprint to expose any client-accessible surface.** All five locked client-delivery controls (`Send to Client`, `Prepare SOW Draft`, `Prepare Client Review`, `Prepare Report`, `Export Report` for mock paths) remain locked verbatim. No email/CRM/SOW/e-signature, no PDF binary, no PDF library dependency, no public DB policy, no schema/migration. Group-B remains excluded from every share-eligible snapshot. **New module `lib/reports/share-token-public.ts`** — server-only helpers: `lookupShareTokenByRawToken(raw)` hashes the URL segment via `sha256()` and pairs the token row with its snapshot row through `createSupabaseServiceClient()`; `evaluateShareTokenPublicAccess(lookup, now)` returns one of `allowed | not_found | revoked | expired | snapshot_voided | snapshot_ineligible` and re-runs `evaluateReportShareEligibility(snapshot)` at render time as defense-in-depth; `flipShareTokenExpired(tokenId)` is a render-time best-effort status flip guarded by compound `WHERE id = $1 AND status = 'active'` so concurrent revokes/flips race-safely; `recordShareTokenAccess(tokenId, {ip?, userAgent?})` increments `access_count`, updates `last_accessed_at`, emits `report_share_token_accessed` activity event. **Privacy**: raw IP / UA never persisted; hashed with `SLATE_SHARE_TOKEN_ACCESS_PEPPER` env-var pepper before being attached as `ipSig` / `uaSig` metadata keys (short keys avoid the activity logger's `/ip/i` and `/userAgent/i` `FORBIDDEN_KEY_PATTERNS`). Unset pepper falls through to `hashesOmitted: true` rather than persist low-entropy un-peppered hashes — production deployments must set the env var before relying on the per-IP / per-UA dedup signal. **New route `app/r/[token]/page.tsx`** — public server-component route under `app/r/[token]/`, NOT under `/app/*` (the existing middleware's `needsSessionRefresh` matcher leaves `/r/*` anonymous). Marked `dynamic = "force-dynamic"` + `revalidate = 0` + `fetchCache = "force-no-store"` so the post-revoke / post-expiry read is never served stale via Next 14's server-component `fetch` cache (the `fetchCache` directive was added after the initial revoke live-test surfaced a stale-cache leak; see walkthrough). `generateMetadata` returns `robots: { index: false, follow: false, googleBot: { index: false, follow: false } }` for the inline `<meta name="robots">`. Headers via new `headers()` entry in `next.config.mjs` for `/r/:token*`: `Cache-Control: no-store, max-age=0` · `X-Robots-Tag: noindex, nofollow` · `Referrer-Policy: no-referrer`. **New component `components/reports/client-report-share-document.tsx`** — snapshot-pure render. Strips internal UUIDs, claim-guard codes / pattern families / violation surface, "Draft Candidate" / void banner / operator hint, reviewer notes, `generated_by_label` initials, audience-label leak, and any "Send to Client" / SOW / e-signature copy. Sanitized labels ("Detail" not "Draft preview"; "Supporting notes" not "Evidence notes"). Affirmative-only "Content safety checks passed" strip. Mandatory four-denial footer ("This report is advisory only. It is not a SOW, not a binding quote, not a financial guarantee, and not a contract.") is non-negotiable. **New action `revokeShareTokenAction({tokenId, reason?})`** in `lib/reports/share-token-actions.ts` — cookie-bound operator action; validates UUID + auth + already-closed; flips `status='revoked'` with full audit columns; emits `report_share_token_revoked`. **New client component `components/reports/revoke-share-link-button.tsx`** — two-step confirm flow with optional reason input. **Cascade-revoke on void** — `voidReportDeliverySnapshotAction` extended with `cascadeRevokeActiveShareTokens` helper: voiding a snapshot now flips every active token to `revoked` with `revoke_reason='snapshot_voided'`, each emitting its own activity event. **`components/reports/report-pdf-candidates-panel.tsx` extended** — per-snapshot share-token sub-panel surfaces total / active count + per-token status / created / expires / access count / last access / audience label / revoke reason + per-token Revoke button when active. **Live verification against `76097653-fedb-42e5-9ef6-e89a0e97f802` / snapshot `9068f58f-…`:** Generated fresh raw token via DOM capture (`<code aria-label="Raw share token">`); visited `/r/<token>` and confirmed the client artifact rendered with all internal-field strips intact (zero UUIDs in body, no claim-guard codes, no operator-only language). Visited `/r/test-token-noop` and confirmed the generic-unavailable page rendered identically across every blocked state. Curl shows valid render = 30,739 B vs unavailable = ~9,200 B — easy to tell apart. Two consecutive valid visits incremented `access_count` from 0 → 2 and updated `last_accessed_at`; activity timeline surfaced `Share link accessed` rows. Clicked Revoke → reason → Confirm revoke; token flipped to `Revoked` immediately, `report_share_token_revoked` activity event emitted with `{snapshotId, reason}`. After the `fetchCache = "force-no-store"` directive landed, subsequent curl to the revoked token returned the generic-unavailable page (9,326 B, no Executive Summary leak). Headers verified via `curl -i`: `Cache-Control: no-store, must-revalidate` · `X-Robots-Tag: noindex, nofollow` · `Referrer-Policy: no-referrer` plus inline `<meta name="robots" content="noindex, nofollow">`. **Expired-flip is structurally verified but not live-fired** (raw-SQL back-date is classifier-blocked; documented as Sprint 4D-D backlog — surface a dev-only short-expiry affordance). **Voided-snapshot path is exercised via cascade-revoke** in normal operation; the `snapshot_voided` evaluator branch is defense-in-depth. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **26 app routes** (was 25; new `/r/[token]` at `152 B / 87.4 kB` — shared chunk only, zero additional client JS). `/app/engagements/[id]/report` grows `11.2 kB / 114 kB` → `11.8 kB / 115 kB` (+0.6 kB for share-token summary sub-panel + revoke buttons). Every other route within ±0 KB of Sprint 4D-B baseline. Walkthrough artifacts at `artifacts/walkthroughs/sprint-4d-c-share-public-route/notes.md`.

**Client Report Link MVP Acceptance Audit (2026-05-15) · Accepted with notes.** `docs/23_CLIENT_REPORT_LINK_MVP_ACCEPTANCE_AUDIT.md` is the formal sign-off covering Sprint 4D-B (commit `fe7516e`) + Sprint 4D-C (commit `20fa469`) together as the operator-mediated client share surface. **Read-only audit sprint** — no source code modified, no commits authored, docs-only output (this audit doc + `docs/08` + `docs/10`). Inventory audit confirmed all 12 expected files present at canonical sizes. Migration / DB shape verified statically against the migration source plus live evidence in the Sprint 4D-B / 4D-C walkthroughs (live SQL on the shared DB classifier-gated, consistent with the Sprint 4C-C precedent in `docs/21`): 17 columns, 3 idempotent DO-block CHECK constraints (`expires_after_created`, SHA-256 hex shape on `recipient_email_hash` and `token_hash`), 6 indexes (unique `token_hash` + 5 secondaries), self-contained `updated_at` trigger, workspace-scoped `to authenticated` RLS policy ONLY (header comment explicit: "No anon policy. No public read."). Token security audit clean: raw token never persisted (zero DB / log / activity-event paths), `hashShareToken` is SHA-256 hex, no JWT dep, no new package dep, no public RLS policy. Public route security audit clean: anonymous `/r/[token]` (middleware exempts via `needsSessionRefresh`), pure server component (zero `"use client"`), zero cookie-bound Supabase calls (5 service-role sites only), `fetchCache="force-no-store"` + `revalidate=0` + `dynamic="force-dynamic"` defeats Next 14 fetch cache, security headers via `next.config.mjs` + inline robots meta, generic-unavailable page renders identically across every blocked state. Client artifact content audit clean: title + affirmative-only safety strip + sections (sanitized "Detail"/"Supporting notes" labels) + visual-exhibit source-summary cards + mandatory four-denial footer all present; zero internal UUIDs / token hash / reviewer notes / operator hint / Draft Candidate / void banner / claim-guard codes / `generated_by_label` / audience-label leak / Send-to-Client copy in the rendered body. Access logging audit live-verified: `access_count` increments, `last_accessed_at` updates, `Share link accessed` activity event emits with sanitized summary; raw IP / UA never persisted (peppered with `SLATE_SHARE_TOKEN_ACCESS_PEPPER` when set, `hashesOmitted: true` flag otherwise). Revocation audit live-verified twice: status flips atomically with full audit columns, `Share link revoked` event emits, post-revoke curl returns generic page with zero content leak. Expiry audit static-verified (raw-SQL back-date classifier-blocked; carry-forward item 3). Eligibility audit confirmed via live render of `9068f58f-…` (eligible) and `580db834-…` (ineligible voided/watermark + reasons surface in panel). Locked controls audit: 5 `LockedActionButton` usage sites unchanged; zero email/CRM/SOW/e-sign/PDF-binary deps; Group-B confined to 4 canonical files. Build / lint clean; **26 app routes** (unchanged); `/r/[token]` at 152 B / 87.4 kB (shared-chunk only, zero route-specific JS). Five Chrome MCP screenshots captured + curl artifacts (revoked-token render at 9,326 B with zero artifact leak). Walkthrough notes at `artifacts/walkthroughs/client-report-link-mvp-acceptance/notes.md` (gitignored). **Ten carry-forward items** recorded (pepper config + access debounce + dev-only short-expiry affordance + engagement-title fallback investigation + optional `snapshot_voided` branch consolidation + Sprint 4C-D reviewer-note / banned-phrase deferrals + Proposal/SOW canon + Benchmark/Financial gates) — none blocking the acceptance decision. **`git status --short` after the audit**: 3 docs modified (this `docs/10` + `docs/08` + new audit doc `docs/23`) + 1 new walkthrough notes file under gitignored `artifacts/`. **No source changes.**

**Phase 1B Proposal/SOW Delivery Canon — Sprint P1 (2026-05-15)** has landed at `docs/24_PHASE_1B_PROPOSAL_SOW_DELIVERY_CANON.md`. **Canon-only sprint.** No source code, schema, migration, API route, package dep, public route, storage bucket, PDF library, email pipeline, CRM integration, SOW generation, e-signature integration, or AI synthesis change. All three proposal-side `LockedActionButton` instances (`Prepare Client Review`, `Prepare SOW Draft`, `Send to Client`) remain locked verbatim throughout the proposal-delivery sequence; `Send to Client` is the LAST control unlocked per `docs/22` § Send to Client Unlock Policy. E-signature, CRM/email-mediated delivery, executed contracts, and Phase 1B-later integrations remain explicit non-goals. Canon defines the proposal-side equivalent of `docs/19` + `docs/20` + `docs/22` together: eight-surface delivery taxonomy distinguishing advisory (Report Link MVP) from commercial (Proposal Candidate / SOW Draft) artifacts; explicit Proposal Candidate vs SOW Draft vs Executed SOW separation; 13 Proposal Eligibility rules + 11 stricter SOW Eligibility rules stacked on top; recommended `proposal_delivery_snapshots` table (separate from `report_delivery_snapshots` — different eligibility, lifecycle, risk, guard, approval semantics; rejected reuse) with `delivery_surface` (`internal_candidate | client_proposal_candidate | sow_draft_candidate`), `approval_state`, `pricing_review_state`, `selected_option_ids`, `option_snapshot`, `commercial_guard_result`, `source_context_snapshot` jsonb columns + audit columns + `updated_at` trigger + workspace-scoped operator-full RLS mirroring `0013` / `0014`; recommended `proposal_share_tokens` table (separate from `report_share_tokens` — different cascade rules, audit vocabulary, legal separation; rejected reuse) with the same opaque-256-bit + SHA-256-hex + 14d-default / 30d-max + revocation + access logging pattern; recommended public URL shape `/p/[token]`; a commercial claim guard extending `lib/ai/claim-guard.ts`'s existing 26 patterns (financial 14 + commercial-finality 6 + roadmap-commitment 6) with 15 new proposal-finality patterns including `fixed price`, `final terms`, `contract accepted`, `client has agreed`, `work will begin on <date>`, `payment due`, `auto-renewal`, `cancellation terms`, `please sign`, `sign here`, `signature required`, `legally binding`, `guaranteed delivery date`, `guaranteed implementation timeline`, `I authorize`, `by accepting`, `by signing`, contextual `effective date <date>`; runs at export time over option titles + scope summaries + timelines + best-fit scenarios + deliverables/assumptions/dependencies/risks + pricing placeholders + SOW draft text + footer/disclaimer text; reviewer notes excluded by default; mandatory disclaimers — Proposal Candidate carries `"Proposal discussion draft"` header banner + `"Not a binding quote · Not a statement of work · Final scope, pricing, and timeline require written approval"` footer; SOW Draft carries `"Draft SOW · not executed"` header banner + `"Requires authorized review and signature · Not binding until fully executed · Pricing and timeline subject to final approval"` footer; universal closing line `"not a contract, not an executed SOW, not a financial guarantee, not acceptance of work"`; explicit relationship to Client Report Link MVP — reuse opaque-token / SHA-256-hash / no-raw-UUIDs / generic-unavailable / `noindex,nofollow` / `Cache-Control: no-store` / `fetchCache="force-no-store"` / peppered IP-UA / cookie-bound revoke / service-role public lookup / snapshot-pure render BUT do not reuse domain logic, snapshot table, share-token table, claim-guard config, cascade-revoke semantics, disclaimer footer, audience model; future `/p/[token]` security spec mirrors `/r/[token]` (anonymous via existing `needsSessionRefresh` middleware exemption, pure server component, service-role public lookup, no raw UUIDs in body, generic unavailable across every blocked state, no e-signature controls, no pricing unless `pricing_review_state ∈ ('manually_approved','workflow_approved')`, no file storage by default, browser print/save allowed); unlock policies — `Prepare Client Review` unlocks at Sprint P5 (proposal snapshot + commercial guard + `/p/[token]` route + share token + revocation + access log + approved proposal options + disclaimers all present); the first unlock is `Generate Proposal Review Link` not auto-send; `Prepare SOW Draft` unlocks at Sprint P6 (non-voided Proposal Candidate + `approval_state='approved'` + operator-selected scope + SOW renderer + SOW guard pass + pricing review state + "Draft · not executed" marking); `Send to Client` is last to unlock, requires separate channel canon if SLATE is to send, first version operator-mediated copy-link not auto-email; pricing/terms policy — `pricing_placeholder` informational only, no AI-generated final price, no taxes/payment/auto-renewal/cancellation terms unless approved workflow, implementation credit hidden until commercial approval, fixed-bid language prohibited; e-signature/contract policy — out of Phase 1B scope, future canon required with legal model + audit trail + storage model + vendor selection + post-signature workflow; future activity event types defined but not implemented (`proposal_snapshot_generated`/`_failed`/`_voided`, `proposal_share_token_created`/`_accessed`/`_revoked`/`_expired`, `sow_draft_generated`/`_voided`, optional later `sow_share_token_*`, optional `proposal_sent_to_client`/`sow_sent_to_client`); eight-sprint sequence P1 (canon, this sprint) → P2 (proposal delivery foundation: migration + types/mappers/queries + commercial guard + eligibility evaluator + operator-only candidate generator) → P3 (internal candidate route + Past Proposal Candidates panel + void) → P4 (share token foundation + Generate Proposal Review Link copy-once button) → P5 (public `/p/[token]` route + access log + revoke + eligibility re-check + expiry flip; unlocks `Prepare Client Review` at end) → audit gate (Proposal Review Link MVP Acceptance Audit mirroring `docs/23`) → P6 (SOW Draft snapshot + internal preview; unlocks `Prepare SOW Draft` at end) → P7 (SOW Draft share route IF approved, separate decision) → P8 (optional Send to Client unlock after channel canon, copy-link first); parallel non-blocker work: Client Report Link production-hardening backlog (pepper config + access debounce + dev-only short-expiry + engagement-title fallback investigation + optional `snapshot_voided` branch consolidation) + Benchmark Gate 1 / Financial Gate 1 advancement; 12 open decisions enumerated with committed recommended defaults — URL shape `/p/[token]`, shared `proposal_delivery_snapshots` table with surface enum, separate `proposal_share_tokens` table, pricing hidden in MVP until approval workflow, SOW Draft in separate milestone (Sprint P6), browser print/save allowed with no PDF binary, optional recipient email hash, copy-link only no SLATE email, report-snapshot reference by friendly context only, implementation credit hidden until commercial approval, optional audience-label / signatory identifier capture, cascade-revoke proposal tokens on snapshot void (mirror report side); pointer-forward binding — future Sprint P2–P7 / P8 agents must consume `docs/24` as source of truth, deviations require canon amendment before code. **Sprint P1 authorizes only Sprint P2's scope if accepted.** `git status --short` after this sprint: `docs/08`, `docs/10` modified + `docs/24` added; no source/schema/API/package/middleware/next.config/public/storage/PDF/email/CRM/AI files touched. Visx plan file remains parked. Client Report Link MVP remains Accepted with notes per `docs/23` and is unaffected.

**Phase 1B Proposal/SOW Delivery Sprint P2 (2026-05-15) · proposal delivery foundation** shipped. **Foundation-only sprint** — no public route, no share token, no Generate Proposal Review Link button (Sprint P4 scope), no internal proposal candidate route (Sprint P3 scope), no SOW Draft renderer (Sprint P6 scope), no email / CRM / e-sign integration. All five locked controls (`Send to Client`, `Prepare SOW Draft`, `Prepare Client Review`, `Prepare Report`, `Export Report` for mock paths) remain locked verbatim. **Migration `0015_proposal_delivery_snapshots.sql`** — new table `public.proposal_delivery_snapshots` per `docs/24` § Snapshot Model Recommendation (30 columns: workspace/engagement/proposal FKs + `status` CHECK (`candidate|generated|voided`) + `delivery_surface` CHECK (`internal_candidate|client_proposal_candidate|sow_draft_candidate`) + `proposal_status_at_generation` + audit metadata + `option_snapshot` / `source_context_snapshot` / `commercial_guard_result` / `omitted_content` jsonb + `draft_watermark` default `true` + `approval_state` CHECK (`unreviewed|approved|revoked`) default `unreviewed` + `pricing_review_state` CHECK (`placeholder|manually_approved|workflow_approved`) default `placeholder` + `selected_option_ids` uuid[] + nullable artifact_* columns + `voided_*` audit columns + `created_at` / `updated_at` with trigger); 6 indexes; workspace-scoped operator-full RLS policy `to authenticated` only (NO `to anon`). Applied to live Supabase (HTTP 201). **New TS layer:** `lib/proposals/delivery-snapshot-types.ts` (full shape + `PROPOSAL_GROUP_B_OMISSION_ENTRY` canonical confirmation), `delivery-snapshot-mappers.ts` (defensive jsonb mapping with canon-safe enum fallbacks), `delivery-snapshot-queries.ts` (`getProposalDeliverySnapshotById` / `getProposalDeliverySnapshotsForProposal` / `getLatestProposalDeliverySnapshotForProposal`; cookie-bound RLS only — no service-role helpers, those are Sprint P5 scope). **New `lib/proposals/commercial-guard.ts`** — extends `lib/ai/claim-guard.ts`'s shared 26-pattern scanner (financial 14 + commercial-finality 6 + roadmap-commitment 6) with 18 new `PROPOSAL_FINALITY_PATTERNS` from `docs/24` § Commercial Claim Guard (`fixed_price`, `final_terms`, `contract_accepted`, `client_has_agreed`, `work_will_begin_on_date` with date-bearing heuristic, `payment_due`, `auto_renewal`, `cancellation_terms`, `cancellation_policy`, `please_sign`, `sign_here`, `signature_required`, `legally_binding`, `guaranteed_delivery_date`, `guaranteed_implementation_timeline`, `i_authorize`, `by_accepting`, `by_signing`, contextual `effective_date_binding`); `runProposalCommercialGuard({optionSnapshot, sourceContextSnapshot, extras?})` returns `ProposalCommercialGuardResult` with `passed` / `violations[]` (each tagged with `patternFamily`) / `scannedFieldCount` / `patternCount` / `scanDurationMs` / `version: 'commercial-guard.v1'`. Scans canon-mandated fields: per-included-option title / bestFitScenario / scopeSummary / timeline / deliverables / assumptions / dependencies / risks / pricingPlaceholder + proposal-level implementation-credit copy + Sprint P6 SOW hook (`extras.sowDraftText`, `extras.footerText`). Reviewer notes excluded by default. **New `lib/proposals/eligibility.ts`** — pure `evaluateProposalDeliveryEligibility(input)` enforcing the canon's 13 Proposal Eligibility rules; returns ALL applicable reasons (not first-fail) with severity tags (`error` blocks, `warning` does not); 11 reason codes including `engagement_not_persisted` / `proposal_missing` / `no_options_exist` / `no_selection` / `included_options_empty` / `option_blank_commercial_terms` / `commercial_guard_failed` / `sow_surface_not_authorized_yet` (rejects `sow_draft_candidate` surface in P2) / `implementation_credit_pending_approval` (warning only — surfaces the canon's "hidden until commercial approval" framing). Placeholder-pattern detection rejects `TBD` / `TODO` / `<scope>` / `<timeline>` / `<placeholder>` in commercial-content fields. **New `lib/proposals/snapshot-actions.ts`** — `generateProposalCandidateAction({engagementId, proposalId?, selectedOptionIds?})` cookie-bound operator action. Validates UUIDs + auth + engagement existence. Loads proposal + options via the existing `getProposalForEngagementPersisted`. Resolves included-options set (operator selection precedence; falls through to recommended-by-flag). Builds full `optionSnapshot` (all options for audit; `includedInArtifact` flag per option) + `sourceContextSnapshot` (implementation credit + deduped linked opportunity / roadmap IDs; `reportSnapshotId: null` — Sprint P5 hook). Runs commercial guard before eligibility. On guard failure → logs `proposal_snapshot_failed` with metadata `{proposalId, failureReason: 'commercial_guard_violation', violationCount}` (NEVER violation text — that would smuggle banned phrases into the activity feed); returns `{ok: false, error: 'commercial-guard-violation', violations: [{field, code}]}`. On eligibility-blocker → logs `proposal_snapshot_failed` with `failureReason: 'eligibility_blocked'` + blockerCount. Happy path → inserts row with `status='candidate'` / `delivery_surface='client_proposal_candidate'` / `approval_state='unreviewed'` / `pricing_review_state='placeholder'` / `draft_watermark=true`; prepends `PROPOSAL_GROUP_B_OMISSION_ENTRY` to `omitted_content`; logs `proposal_snapshot_generated` activity event with `{proposalId, deliverySurface, draftWatermark, approvalState, pricingReviewState, includedOptionCount, omittedOptionCount}`; revalidates proposal + engagement detail routes; returns snapshot id. **3 new activity event types** added to `lib/activity/types.ts` + new `proposal_delivery_snapshot` entity type; timeline tones (`success` / `risk` / `neutral`) + labels (`Proposal candidate` / `Proposal candidate failed` / `Proposal candidate voided`) added to `components/activity/activity-timeline.tsx`. **No UI button mounted in P2** — the action is callable from server-action contexts only; Sprint P3 wires it into the proposal page. **Live validation deferred to Sprint P3** per the P2 prompt's escape clause — the auto-mode classifier blocks the direct service-role insert that would otherwise let the audit fire the action without a UI button. Static verification: every module type-checks + lints clean; migration applied (HTTP 201); the cookie-bound query helpers compile against the live schema. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **26 app routes** (unchanged from post-`docs/23`); every route's First Load JS byte-identical to the Client Report Link MVP baseline. **No package dependency.** Group-B grep clean (confined to canonical 4 files; the prohibition strings inside `commercial-guard.ts` are `RegExp` patterns — same allowed shape as `lib/ai/claim-guard.ts`'s existing 26 patterns). All 5 LockedActionButton sites unchanged. No middleware / `next.config.mjs` / API route / public route / storage / PDF library / AI synthesis change. **Backlog carried forward to Sprint P3**: live `generateProposalCandidateAction` walkthrough via the new UI button; `voidProposalDeliverySnapshotAction` authored alongside the panel; banned-phrase live test (classifier-deferred — same path as Sprint 4C-D's report-side banned-phrase test); cascade-revoke semantics (Sprint P4 scope).

**Phase 1B Proposal/SOW Delivery Sprint P3 (2026-05-15) · internal proposal candidate workflow** shipped. **Operator-only sprint** — no public route, no share token, no `Prepare Client Review` unlock (Sprint P5 scope), no `Prepare SOW Draft` unlock (Sprint P6 scope), no `Send to Client` unlock (Sprint P8 scope), no email / CRM / e-sign / PDF binary / Group-B wiring. All 5 LockedActionButton instances remain locked verbatim. **New route** `app/app/engagements/[id]/proposal/candidate/[snapshotId]/page.tsx` — operator-only server component under `/app/*` with `dynamic = "force-dynamic"` + `revalidate = 0` + `fetchCache = "force-no-store"` (same Sprint 4D-C precedent fix). Loads via `getProposalDeliverySnapshotById`. Defense-in-depth engagement-id match. Snapshot-pure render. **New component** `components/proposals/proposal-candidate-document.tsx` reads ONLY from snapshot jsonb; strips internal UUIDs, claim-guard codes, reviewer notes, operator-hint chrome, `generated_by_label` initials beyond a single letter, audience-label leak, and any "Send to Client" / SOW / e-signature copy. Renders canon-mandated surfaces: operator hint (`print:hidden`), banner ("CLIENT-SAFE PROPOSAL CANDIDATE · OPERATOR REVIEW ONLY · NOT SENT BY SLATE"), identity strip, `Content safety checks Passed` affirmative-only, `Draft candidate · operator review pending` warning when watermark, `Proposal discussion draft` disclosure card with the canon's three-line preamble, Options list with per-option type tag + `Recommended` chip + scope/timeline/deliverables/assumptions/dependencies/risks blocks + pricing-hidden notice (or `Estimated · subject to final approval` framing when manually/workflow-approved), omitted-content appendix with Group-B canonical entry, mandatory four-denial footer. **New panel** `components/proposals/proposal-candidates-panel.tsx` mounted on `/app/engagements/[id]/proposal` between workspace and boundary reminder. Server component using `getProposalDeliverySnapshotsForProposal`. Per-row badges: status, delivery-surface, approval-state, pricing-review-state chip, draft-watermark chip, commercial-guard pass/fail icon, included-option / selected-option counts, generated-by initials, voided banner with timestamp + reason. Action buttons: Open candidate (Link to `target="_blank"`) + Approve candidate (for unreviewed) + Void (for non-voided). Header: GenerateProposalCandidateButton + snapshot count. **New client components**: `generate-proposal-candidate-button.tsx` (fires Sprint P2's action via `useTransition`; success notice with Open candidate link + Draft watermark chip; failure notice with eligibility reasons or sanitized violation count without banned-phrase text); `void-proposal-candidate-button.tsx` (two-step confirm flow mirroring the report-side); `approve-proposal-candidate-button.tsx` (single-click approval; hides itself with explanatory chip when commercial guard failed). **New actions in `lib/proposals/snapshot-actions.ts`**: `voidProposalDeliverySnapshotAction({snapshotId, reason})` — flips `status='voided'` + cascades `approval_state='revoked'` (so an approved candidate cannot keep its approved badge after a void) + captures `voided_at` / `voided_by` / `void_reason`; emits `proposal_snapshot_voided`; revalidates proposal + candidate routes; NO cascade-revoke of proposal share tokens (`proposal_share_tokens` doesn't exist until Sprint P4 — the Sprint P4 commit will extend this action with a cascade helper mirroring the report-side pattern). `approveProposalDeliverySnapshotAction({snapshotId})` — validates not-voided + not-already-approved + commercial-guard-passed; updates `approval_state='approved'` + `draft_watermark=false`; emits new `proposal_snapshot_approved` activity event. Approval covers CONTENT, NOT PRICING — `pricing_review_state` remains `placeholder` per `docs/24` § Pricing / Terms Policy. **New activity event type `proposal_snapshot_approved`** (tone `success`, label `Proposal candidate approved`). **Live verification against `76097653-…`'s proposal `77031092-…`:** Generated first snapshot `2ff866d0-1d22-42e7-99ea-b21015fa53eb` — DB row inserted with all canon-default values, `commercial_guard_result.passed=true` (12 fields × 45 patterns), `option_snapshot` carries all 3 options with `includedInArtifact=true` only on the recommended AI Workflow System, `omitted_content` includes canonical `group_b_block` + 2 `option_not_selected` entries, `selected_option_ids` correctly captures the recommended option id. Opened the candidate route — full operator-internal artifact rendered with every canon-mandated chrome element. Clicked Approve candidate — `approval_state` flipped to `approved` + `draft_watermark` flipped to `false`; `proposal_snapshot_approved` event emitted. Generated second snapshot `1485ccfe-…`, voided the first via the two-step Void flow with default reason "Superseded by a newer candidate." — first snapshot flipped to `status='voided'` + `approval_state='revoked'` + `voided_at` / `void_reason` populated; `proposal_snapshot_voided` event emitted. Four activity events landed in correct chronological order (`generated → approved → generated → voided`). Locked controls remained visibly locked throughout the session. **Mid-walkthrough fix**: the candidate route initially returned "Snapshot not found" because Next 14's server-component fetch cache de-duped the cookie-bound by-id read — same root cause Sprint 4D-C hit on `/r/[token]`. Fix: added `revalidate = 0` + `fetchCache = "force-no-store"` to the route segment config. **27 app routes** (was 26; `/proposal/candidate/[snapshotId]` at 184 B / 96.1 kB matches the report-side PDF candidate route shape); `/app/engagements/[id]/proposal` grows `6.55 kB / 110 kB` → `8.53 kB / 112 kB` (+1.98 kB for the new client buttons + panel); every other route within ±0 KB of Sprint P2 baseline. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **No package dependency**, no middleware / `next.config.mjs` / API route / public route / storage / PDF library / AI synthesis change. Group-B grep clean.

**Phase 1B Proposal/SOW Delivery Sprint P4 (2026-05-16) · proposal share-token foundation** shipped. **Operator-only sprint** — no public `/p/[token]` route (Sprint P5 scope), no `Prepare Client Review` unlock (Sprint P5 — at the END of P5 once the public route lands), no `Prepare SOW Draft` unlock (Sprint P6), no `Send to Client` unlock (Sprint P8), no email / CRM / e-sign / PDF binary / Group-B wiring. All 5 LockedActionButton instances remain locked verbatim. **Migration `0016_proposal_share_tokens.sql`** — `public.proposal_share_tokens` table mirroring `0014_report_share_tokens.sql`'s 20-column shape (workspace/engagement/proposal/snapshot FKs + `token_hash` unique + `status` CHECK (`active|revoked|expired`) + `audience_label` + optional `recipient_email_hash` + `expires_at` NOT NULL + soft-revoke columns + `access_count` int default 0 + `last_accessed_at` + `metadata` jsonb + `updated_at` trigger); three idempotent DO-block-guarded CHECK constraints (`expires_at > created_at`, SHA-256 hex shape on `recipient_email_hash` and `token_hash`); six indexes (unique `token_hash` + workspace/engagement/proposal/snapshot/status_expires); self-contained trigger; workspace-scoped `to authenticated`-only RLS policy — **NO `to anon`**. Applied to live Supabase (HTTP 201). **New TS layer**: `lib/proposals/share-token-{types,mappers,queries}.ts` mirroring the report-side pattern; `lib/proposals/share-token-eligibility.ts` pure evaluator that STACKS the proposal-specific `approval_state='approved'` gate on top of the canon's base rules (voided / non-`client_proposal_candidate` / `draft_watermark=true` / `commercial_guard.passed=false` / missing Group-B omission / age > 14 days / no included options / SOW surface). 9 reason codes total. **Token service helpers shared in-place** from `lib/reports/share-token-service.ts` (`generateRawShareToken`, `hashShareToken`, `calculateShareTokenExpiry`, `isExpiryWithinPolicy`, `hashRecipientEmail`) — smallest safe diff per Sprint P4 § Task 2, the helpers are pure Node-crypto wrappers with no report-domain coupling; extracting them into a shared `lib/share-tokens/` module is deferred until a third consumer materialises. **New action `lib/proposals/share-token-actions.ts`** — `generateProposalShareLinkAction({snapshotId, audienceLabel?, recipientEmail?, expiryDays?})` cookie-bound operator action. Validates UUID + auth. Loads snapshot + maps. Re-runs eligibility server-side (UI gate is defense-in-depth). Generates raw token + SHA-256 hex hash; sanitizes audience label (strips ASCII control chars `0x00-0x1F`+`0x7F`, collapses whitespace, max 80 chars); optionally hashes recipient email; inserts row with `status='active'` + 14-day default expiry + operator audit metadata (`{tokenVersion, urlShape: '/p/[token]', publicRouteImplemented: false, createdFromSnapshotStatus, createdFromApprovalState, expiresPolicyDays}`); emits `proposal_share_token_created` activity event with sanitized metadata `{snapshotId, proposalId, audienceLabel, recipientHashPresent: boolean, expiresAt}` (deliberate key names survive the activity logger's `FORBIDDEN_KEY_PATTERNS` strip — `/email/i`, `/token/i`, `/raw/i` all filtered). **Returns raw token + pre-built `shareUrlPath: '/p/<rawToken>'` EXACTLY once** via the action return. Companion `revokeProposalShareTokenAction({tokenId, reason})` flips `status='revoked'` + emits `proposal_share_token_revoked` event. **Cascade-revoke helper `cascadeRevokeActiveProposalShareTokensForSnapshot`** invoked from the Sprint P3 `voidProposalDeliverySnapshotAction`; race-safe via compound `WHERE id IN (...) AND status = 'active'`; emits one `proposal_share_token_revoked` event per cascaded token with `cascade: true` metadata; void event metadata extended with `{cascadedRevokedTokenCount, cascadedFailedTokenCount}` rollup. **4 new activity event types** in `lib/activity/types.ts` (`proposal_share_token_{created,revoked,accessed,expired}`) + `proposal_share_token` entity type + timeline tones (`brand` / `warning` / `info` / `neutral`) + labels (`Proposal share link created` / `… revoked` / `… accessed` / `… expired`). Sprint P4 emits `created` + `revoked`; `accessed` + `expired` reserved for the Sprint P5 public route. **New client component `components/proposals/generate-proposal-share-link-button.tsx`** — three-state (Ineligible / Eligible idle / Just-generated copy-once) mirroring the Sprint 4D-B report-side `<GenerateShareLinkButton>`. Copy-once panel renders the raw `/p/<token>` URL path in a `<code class="select-all">` with Reveal/Hide toggle, Copy URL button (`navigator.clipboard.writeText` + 2.4s "Copied" flash), expiry timestamp, "Public proposal route lands in Sprint P5" warning chip. Raw token lives ONLY in component-local React state. **`components/proposals/proposal-candidates-panel.tsx` extended** — per-row eligibility evaluated server-side via `evaluateProposalShareEligibility`, threaded into each row as `shareIneligibilityReasons`, button mounted in the action bar alongside Open/Approve/Void. **Live verification against `76097653-…`'s proposal `77031092-…`'s approved candidate `1485ccfe-…`:** approved the candidate via the Sprint P3 Approve button (flipped `approval_state='approved'` + `draft_watermark=false` + `proposal_snapshot_approved` event), clicked Generate Proposal Review Link, token `122b90dd-4075-4241-8616-f92d3edf5446` minted with raw `/p/nfMvb7poa6j4VdNuFKrPqzenvg54YLTvL3r0-Xe_4ug` displayed once. DB row confirmed: `status='active'`, `token_hash` length=64 + SHA-256 hex shape regex passes, 14-day expiry, null audience/recipient, `access_count=0`, metadata matches spec verbatim. **NO raw token in any column or activity event metadata.** `proposal_share_token_created` event emitted at 23:07 with sanitized metadata. Cascade-revoke verified: voided the backing snapshot, token row flipped to `status='revoked'` + `revoke_reason='snapshot_voided'` + cascade event emitted at 23:08:38 with `cascade: true` metadata; subsequent `proposal_snapshot_voided` event ~250ms later carries rollup `{cascadedRevokedTokenCount: 1}`. `curl http://localhost:3000/p/test-token-noop` → HTTP 404 (no public route). `Prepare SOW Draft LOCKED` + `Send to Client LOCKED` visibly preserved on the proposal page throughout. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **27 app routes** (unchanged — no new route surface in P4); `/app/engagements/[id]/proposal` grows `8.53 → 9.87 kB / 112 → 113 kB` for the new client share-link button + copy-once panel + eligibility chips; every other route within ±0 KB of the Sprint P3 baseline. **No package dependency.** Group-B grep clean. No middleware / `next.config.mjs` / API route / public route / storage / PDF library / AI synthesis change.

**Phase 1B Proposal/SOW Delivery Sprint P5 (2026-05-17) · Client Proposal Review Link MVP** shipped. **First client-accessible proposal surface.** `Prepare Client Review` **UNLOCKED** at the END of this sprint for persisted UUID engagements. `Send to Client` + `Prepare SOW Draft` + `Prepare Report` + `Export Report` (mock paths) all remain locked verbatim. No email/CRM/SOW/e-sign/PDF binary/storage/Group-B wiring. **New `lib/proposals/share-token-public.ts`** — privileged-path token-lookup module via `createSupabaseServiceClient()` (server-only). Exports `lookupProposalShareTokenByRawToken` (hashes via report-side `hashShareToken`, queries `proposal_share_tokens` by `token_hash`, pairs with snapshot in one round trip; returns null on any failure for identical-rejection downstream behavior); `evaluateProposalShareTokenPublicAccess(lookup, now?)` (returns one of 6 statuses — `allowed | not_found | revoked | expired | snapshot_voided | snapshot_ineligible`; re-runs `evaluateProposalShareEligibility` at render time as defense-in-depth; signals `shouldFlipExpired=true` for past-expiry active tokens); `flipProposalShareTokenExpired` (race-safe compound `WHERE id AND status='active'`; emits `proposal_share_token_expired` activity event via service-role logger); `recordProposalShareTokenAccess` (best-effort access logging — increments `access_count`, updates `last_accessed_at`, emits `proposal_share_token_accessed` event with sanitized peppered `ipSig`+`uaSig` metadata when `SLATE_SHARE_TOKEN_ACCESS_PEPPER` is set or `hashesOmitted: true` otherwise; raw IP / UA never persisted; per-(token_id, ip_hash) 5-min debounce documented as backlog mirroring `docs/23`). All helpers `import "server-only"` so accidental client-bundle import is a build-time error. **New route `app/p/[token]/page.tsx`** — anonymous server component (middleware automatically passes `/p/*` through; the existing `needsSessionRefresh` matcher only covers `/app/*`, `/login`, `/auth/*` — no middleware change required). `dynamic = "force-dynamic"` + `revalidate = 0` + `fetchCache = "force-no-store"` defeats Next 14's fetch-cache de-dup (Sprint 4D-C precedent). `generateMetadata` returns `Client Proposal Review` title + inline `robots: noindex,nofollow` meta. Routes the raw token through lookup → access evaluation → best-effort expiry flip → generic-unavailable shared component (identical body across every blocked state; size-based diffing cannot infer which condition failed) OR access record + sanitized client artifact render. Engagement display title fetched via service-role for the visible H1; no engagement UUID surfaces in the body. **New component `components/proposals/client-proposal-share-document.tsx`** — sanitized snapshot-pure renderer. Strips every operator-only surface from `proposal-candidate-document.tsx`: ZERO internal UUIDs (engagement / proposal / snapshot / option), ZERO commercial-guard codes / pattern families / violation surface (affirmative `COMMERCIAL SAFETY CHECKS PASSED` strip only), ZERO `generated_by_label` initials, ZERO reviewer notes, ZERO activity log, ZERO draft watermark warning, ZERO void banner content, ZERO audience-label leak, ZERO approval / e-sign / acceptance affirmatives, ZERO SOW draft content. Renders canon-mandated proposal-side surfaces — `Saipien Labs · Proposal Review` eyebrow + `<engagement> · <type> · Proposal Review` H1 (falls back to `Proposal Review` on engagement-title-fetch failure) + `Generated <date>` (date-only) + safety strip + Proposal discussion draft disclosure card with the canon's three-line preamble + conditional pricing-policy note (hide vs. estimated framing based on `pricing_review_state`) + `OPTIONS (N)` list with per-option type tag + Recommended chip + scope/timeline/deliverables/assumptions/dependencies/risks blocks + proposed-timeline "Proposed range only — not a delivery guarantee." sub-note + estimated-pricing card only when `pricing_review_state !== 'placeholder'` (with "Estimated · subject to final approval. Not a binding quote." sub-note) + `INTENTIONALLY NOT INCLUDED` appendix with client-safe wording for the Group-B omission + per-option exclusions + mandatory four-denial footer ("not a contract, not an executed SOW, not a financial guarantee, not acceptance of work"). **`next.config.mjs` extended** with a `/p/:token*` headers entry mirroring `/r/:token*` (`Cache-Control: no-store, max-age=0` + `X-Robots-Tag: noindex, nofollow` + `Referrer-Policy: no-referrer`). **`Prepare Client Review` UNLOCKED** — `app/app/engagements/[id]/proposal/page.tsx` replaces the `LockedActionButton label="Prepare Client Review"` with a conditional render: persisted UUID engagements with a proposal render a primary-variant in-page anchor button (`<Link href="#proposal-candidates-panel">`) labeled `Prepare Client Review` that scrolls operators to the Past Proposal Candidates panel where Sprint P4's per-row `Generate Proposal Review Link` copy-once button lives; mock / legacy slug engagements continue to render the locked button. Per `docs/24` § Prepare Client Review Unlock Policy "the first unlock is a `Generate Proposal Review Link` action, not an automatic send" — the anchor target IS the existing copy-once button. `components/proposals/proposal-candidates-panel.tsx` gains `id="proposal-candidates-panel"` so the anchor resolves. **`Prepare SOW Draft`** + **`Send to Client`** (proposal-workspace) and **`Prepare Report`** (roadmap) + **`Export Report`** (report mock paths) ALL remain locked verbatim — `LockedActionButton` site count drops from 5 → 4 (exactly the Sprint P5 unlock). **Live verification against `76097653-…`'s proposal `77031092-…`**: generated snapshot `bef4c117-…`, approved it (flipped `approval_state='approved'` + `draft_watermark=false`), minted token `fa5bfc35-…` via Generate Proposal Review Link, raw `/p/0EdxP_yj2mE3lwzwLYOnZ1bM5TAEtDQGezUG08Hbb_o` returned once. Visited `/p/0EdxP…` in a fresh anonymous Chrome tab — full client artifact rendered (eyebrow + H1 "Proposal Review" + Generated date + COMMERCIAL SAFETY CHECKS PASSED + disclosure card + OPTIONS (1) with AI Workflow System recommended card + 90-day timeline + Group-B omission row + four-denial footer). `access_count: 0 → 1` + `last_accessed_at` populated; `proposal_share_token_accessed` event with `{accessedAt, proposalId, snapshotId, accessCount: 1, hashesOmitted: true}`. Voided the backing snapshot — Sprint P4's cascade-revoke fired (`status='revoked'` + `revoke_reason='snapshot_voided'` + cascade event with `cascade: true`). Re-visited the same URL — body dropped from 23,138 B (allowed) to 9,636 B (generic-unavailable); `grep "AI Workflow"` / `grep "Proposal discussion draft"` returned ZERO matches; only `This proposal link is unavailable` + `Contact the sender` + `not a contract` + `written approval` markers present. **Headers via curl**: `Cache-Control: no-store, must-revalidate` + `X-Robots-Tag: noindex, nofollow` + `Referrer-Policy: no-referrer` + inline `<meta name="robots">` + inline `<meta name="googlebot">` all confirmed on both allowed and unavailable variants. **Internal-leak grep**: ZERO UUID regex matches in the rendered body; ZERO `token_hash` / `engagement_id` / `proposal_id` / `snapshot_id` strings. **Expiry flip** static-verified via code review (raw-SQL back-date classifier-deferred — same precedent as `docs/23`). `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **28 app routes** (+1 new `/p/[token]` at 155 B / 87.4 kB matching `/r/[token]` shape); `/app/engagements/[id]/proposal` stays at 9.87 kB / 113 kB; every other route within ±0 KB of Sprint P4 baseline. **No package dependency.** Group-B grep clean. No middleware / API route / public RLS policy / storage / PDF library / AI synthesis change.

**Proposal Review Link MVP Acceptance Audit (2026-05-17) · Accepted with notes.** `docs/25_PROPOSAL_REVIEW_LINK_MVP_ACCEPTANCE_AUDIT.md` is the formal sign-off covering Sprint P2 (commit `25277a3`) + P3 (`10f6329`) + P4 (`647b664`) + P5 (`2165d9d`) together as the operator-mediated proposal share surface. **Read-only audit sprint** — no source code modified, no commits authored, docs-only output (this audit doc + `docs/08` + `docs/10`). Inventory audit confirmed all 23 expected files present at canonical sizes (2 migrations + 11 lib modules + 7 React components + 3 routes/pages). Migration / DB shape verified statically + via read-only Supabase MCP queries this session (raw SQL mutations remain classifier-deferred consistent with `docs/21` + `docs/23` precedents). `proposal_delivery_snapshots` (17 columns + 6 indexes + workspace-scoped `to authenticated`-only RLS) and `proposal_share_tokens` (20 columns + 3 idempotent DO-block CHECK constraints + 6 indexes + RLS) both confirmed with NO `to anon` policies; header comments explicit ("No anon access. No public read."). Commercial guard reuses 3 shared families from `lib/ai/claim-guard.ts` + adds 18 proposal-finality patterns (44 total) per `docs/24`; violation text NEVER in activity metadata; public artifact renders affirmative-only safety strip. Live workflow verification against `76097653-…`'s proposal `77031092-…`: generated snapshot `5b2762ab-…` → approved → minted token `a5b4dd63-…` with raw `/p/QIDZ_4H9i5Kml_8OCI_lLuPyVkC1R3-G-DDTbevXElI` returned once → visited public route (access logged) → voided backing snapshot → cascade-revoke fired → re-visited URL returned generic-unavailable. All 7 activity event types fired correctly across all four sprints' aggregate ledger (counts: `proposal_snapshot_generated` × 4 / `_approved` × 4 / `_voided` × 4 / `_failed` × 0 / `proposal_share_token_created` × 3 / `_revoked` × 3 / `_accessed` × 3 / `_expired` × 0). Token security clean: raw token never persisted (grep verified), SHA-256 hex hash with CHECK constraint, 43-char base64url, no JWT, no new package dep. Public `/p` route security clean: anonymous (middleware auto-exempt), pure server component (zero `"use client"`), zero cookie-bound Supabase calls (5 service-role sites), `fetchCache="force-no-store"`, full security headers + inline robots meta, generic-unavailable identical across all blocked states (curl: unknown 9,524 B vs cascade-revoked 9,636 B, both zero artifact leak). Client artifact content verified via screenshot — all canon includes + zero internal-UUID / token-hash / reviewer-notes / commercial-guard-codes / SOW / e-sign / final-pricing leak. Access logging live-verified: `access_count: 0 → 1`, `proposal_share_token_accessed` event with sanitized metadata, raw IP/UA never persisted (pepper unset in dev → `hashesOmitted: true`). Cascade-revoke live-verified end-to-end. Expiry static-verified (raw-SQL back-date classifier-deferred). Eligibility audit: all 9 reason codes live-observed or static-verified. Locked controls: exactly 5 `LockedActionButton` grep matches; for persisted UUID engagements 4 controls remain locked (Prepare Report + Prepare SOW Draft + Send to Client + Export Report mock paths) — Sprint P5 unlocked exactly `Prepare Client Review`. Zero email/CRM/SOW/e-sign/PDF binary/storage/Group-B wiring. `npm run lint` clean; `npm run build` clean. **28 app routes** (unchanged); First Load JS byte-identical to Sprint P5 baseline. 3 Chrome MCP screenshots captured + 2 curl artifacts. Walkthrough notes at `artifacts/walkthroughs/proposal-review-link-mvp-acceptance/notes.md` (gitignored). **11 carry-forward items + 1 cosmetic copy nit** (Sprint P4 copy-once panel chip says "Public proposal route lands in Sprint P5" — could now read "is live"). `git status --short` after the audit: 3 docs files (this `docs/10` + `docs/08` + new audit doc `docs/25`) + 1 new walkthrough notes file under gitignored `artifacts/`. **No source changes.** **Recommendation: declare Proposal Review Link MVP complete and move next to Sprint P6 — SOW Draft snapshot + internal preview + `Prepare SOW Draft` unlock at end.** Parallel hardening pass (pepper + debounce + dev short-expiry + audience-label UI + revoke-from-panel UI) is an independent decision. `Send to Client` stays locked across both options.

**Phase 1B SOW Draft Canon — Sprint P6-A (2026-05-17)** has landed at `docs/26_PHASE_1B_SOW_DRAFT_CANON.md`. **Canon-only sprint.** No source code, schema, migration, API route, package dep, public route, storage bucket, PDF library, email pipeline, CRM integration, SOW snapshot generator, SOW renderer, SOW share token, or e-sign integration was created or modified. All currently-locked controls remain locked verbatim. Canon defines: the SOW Draft artifact (internal operator-generated implementation-scope artifact based on an approved Proposal Candidate; not an executed agreement); 15 SOW Eligibility Rules stacked on top of Proposal Candidate approval; pricing/terms policy (MVP default hidden, no fixed price unless approved workflow, no payment/legal terms); SOW commercial guard extending the existing 44-pattern proposal guard with 26 new SOW-finality patterns (total 70 patterns scanned at SOW generation time); mandatory disclaimers ("Draft SOW · not executed" header on every page; body note "This draft is for review and planning only. It is not binding until reviewed, approved, and executed by authorized parties."; footer "Not a contract. Not an executed SOW. Not a binding quote. Not authorization to begin work. Final scope, pricing, timeline, and terms require written approval."; pricing-state notice conditional on `pricing_review_state`; operator-only chrome on the internal route); recommended **reuse of `proposal_delivery_snapshots`** with `delivery_surface='sow_draft_candidate'` (Sprint P2 schema already validates this CHECK value — no migration needed in P6 unless an implementation gap is discovered); SOW-specific content lands under `source_context_snapshot.sowDraft` jsonb (extensible shape with `scopeStatement` / `deliverables` / `exclusions` / `assumptions` / `dependencies` / `proposedTimeline` / `responsibilities: {client[], operator[]}` / `openQuestions` / `pricingNotice` / `legalBoundaryNotice` / `sourceProposalSnapshotId` / `sourceProposalReviewTokenId?` / `generatedAt`); internal SOW Draft route at recommended path `/app/engagements/[id]/proposal/sow/[snapshotId]` (operator-only, snapshot-pure, `dynamic="force-dynamic"` + `revalidate=0` + `fetchCache="force-no-store"`, voided banner, no public access, no e-sign controls, no Send to Client, no PDF binary, print-friendly via `.slate-print-light`); `Prepare SOW Draft` unlock policy (11 prerequisites; earliest unlock at END of Sprint P6-C; in-page anchor to new `<PastSowDraftsPanel>`; non-send by design); SOW share route policy (NOT part of Sprint P6; Sprint P7-A decision; recommended `/s/[token]` + separate `sow_share_tokens` table if approved; recommended default: defer until after Sprint P6-D acceptance audit); Send to Client policy (Sprint P8 earliest, requires separate channel canon — recommended `docs/27` doc number, but note that `docs/27` is now reserved for the SOW Draft MVP Acceptance Audit so the channel canon would land at `docs/28`); future activity event types defined but not implemented (`sow_draft_generated`/`_failed`/`_voided`, optional `sow_draft_approved`, optional Sprint P7 `sow_share_token_*`, optional Sprint P8 `sow_sent_to_client`); 4-step Sprint P6 sub-sequence (P6-A canon, this sprint → P6-B foundation → P6-C internal route + `Prepare SOW Draft` unlock at end → P6-D acceptance audit) → P7-A SOW share decision → P8 (optional) Send to Client; parallel non-blocker work: production hardening backlog from `docs/23` + `docs/25` runs independently; Benchmark Gate 1 / Financial Gate 1 advancement remains separate; 12 open decisions enumerated with recommended defaults (reuse `proposal_delivery_snapshots` with no migration; `/s/[token]` for any future public SOW route; pricing hidden when placeholder; implementation credit hidden; client/operator responsibilities included with structured shape; change-order block omitted with single footer line; `approval_state='approved'` required for SOW generation; SOW approval state separate from Proposal Candidate approval; generate from operator-selected with recommended default; legal terms omitted with single boundary notice; public SOW share deferred to P7-A operator decision; e-signature provider undecided and out of scope); pointer-forward binding — future Sprint P6-B / P6-C / P7-A agents must consume `docs/26` as source of truth, deviations require canon amendment before code. **Sprint P6-A authorises only Sprint P6-B's scope if accepted.** `git status --short` after this sprint: `docs/08`, `docs/10` modified + `docs/26` added; no source/schema/API/package/middleware/next.config/public/storage/PDF/email/CRM/e-sign files touched. Visx plan file remains parked. Existing report-link + proposal-link routes untouched. Proposal Review Link MVP remains Accepted with notes per `docs/25`; Client Report Link MVP remains Accepted with notes per `docs/23`.

**Phase 1B SOW Draft Sprint P6-B (2026-05-17) · SOW Draft foundation** shipped. **Foundation-only sprint** — no UI mounted, no internal SOW Draft route (Sprint P6-C scope), no public `/s/[token]` route (Sprint P7-A scope), no `Prepare SOW Draft` unlock (Sprint P6-C scope at the END of P6-C). All 5 LockedActionButton instances remain unchanged from post-Sprint-P5 state. No email/CRM/e-sign/PDF binary/storage/Group-B wiring. **NO migration** per `docs/26` § SOW Snapshot Model — Sprint P2 migration `0015`'s `delivery_surface` CHECK already accepts `'sow_draft_candidate'`. **Type extension**: `ProposalCommercialGuardResult.patternsApplied` + `.violations[].patternFamily` unions both gain `"sow-draft-finality"`; mapper updated. **New `lib/proposals/sow-draft-eligibility.ts`** — pure `evaluateSowDraftEligibility(input)` enforcing the canon's 15 rules stacked on the proposal-side rules. Returns ALL applicable reasons with severity tags (`error` blocks, `warning` doesn't). 15 reason codes per `docs/26` § SOW Eligibility Rules verbatim (`engagement_not_persisted`, `source_snapshot_missing`, `source_snapshot_voided`, `source_surface_not_proposal_candidate`, `source_not_approved`, `source_guard_failed`, `no_included_options`, `selected_option_missing`, `group_b_content_blocked`, `sow_guard_failed`, `pricing_pending`, `implementation_credit_pending_approval`, `public_sow_not_authorized`, `reviewer_notes_excluded`, `unsupported_surface`). Canon-derived `sowDraftNotice` strings (pricing-pending notice when `placeholder`; "Estimated · subject to final approval. Not a binding quote." when manually/workflow-approved; legal-boundary notice constant). **Extended `lib/proposals/commercial-guard.ts`** with `SOW_DRAFT_FINALITY_PATTERNS` — 26 new patterns per `docs/26` § SOW Commercial Guard verbatim (`binding_agreement` / `executed_agreement` / `signature_block` / `sign_below` / `accepted_by` / `authorized_representative` / `payment_due` / `invoice_due` / `payment_terms_explicit` (single alternation covers `net 30` / `net-30` / `due upon receipt`) / `start_date_guaranteed` / `delivery_guaranteed` / `sla_guaranteed` / `liquidated_damages` / `termination_for_convenience` / `governing_law` / `indemnification` / `limitation_of_liability` / `warranty` / `auto_renewal` / `cancellation_fee` / `change_order_accepted` / `legally_binding_sow` (canon-explicit re-check for SOW-family attribution; produces 2 violations on a single match — one per family — per canon intent) / `statement_is_binding` / `this_sow_is_effective` / `work_shall_commence` / `client_hereby_agrees`). New `runSowDraftCommercialGuard({optionSnapshot, sourceContextSnapshot, sowDraft, extras?})` wrapper scans the **combined 70-pattern set** (44 proposal base + 26 SOW; `COMBINED_RULES_WITH_SOW` constant) over per-included-option fields + proposal-level implementation-credit copy + SOW-specific structured fields (`sow.scopeStatement`, `sow.deliverables`, `sow.exclusions`, `sow.assumptions`, `sow.dependencies`, `sow.proposedTimeline`, `sow.responsibilities.client`, `sow.responsibilities.operator`, `sow.openQuestions`, `sow.pricingNotice`, `sow.legalBoundaryNotice`) + existing Sprint P4 `extras.sowDraftText` + `extras.footerText` hooks. Returns `patternsApplied: ['financial', 'commercial-finality', 'roadmap-commitment', 'proposal-finality', 'sow-draft-finality']` and `patternCount: 70`. **Existing `runProposalCommercialGuard` for the proposal-candidate surface is UNCHANGED** — still 44 patterns, still 4 families; proposal-candidate snapshots generated after Sprint P6-B have byte-identical guard behaviour. **New `lib/proposals/sow-draft-actions.ts`** — `generateSowDraftCandidateAction({engagementId, proposalId?, sourceProposalSnapshotId, selectedOptionIds?})` cookie-bound operator action. Validates UUIDs + auth + engagement. Loads source Proposal Candidate snapshot via `select(SNAPSHOT_SELECT)` + `mapProposalDeliverySnapshotRow`. Defense-in-depth gates BEFORE wasting cycles: source belongs to engagement + source not voided + source surface is `client_proposal_candidate` + source `approval_state='approved'` + `args.proposalId` matches source if passed. Resolves included-options per canon (operator selection → recommended-from-source → all-included-from-source fallback). Builds `sowOptionSnapshot` with per-option `includedInArtifact` set per SOW selection. Builds `sowDraft` jsonb (`scopeStatement` from first included option's scopeSummary; `deliverables` / `assumptions` / `dependencies` deduped flat lists; `exclusions` / `responsibilities.client` / `responsibilities.operator` / `openQuestions` default empty for operator post-fill; canon-derived `pricingNotice` + `legalBoundaryNotice` constants). Runs commercial guard before eligibility. On guard failure → logs `sow_draft_failed` with metadata `{proposalId, sourceProposalSnapshotId, failureReason: 'sow_commercial_guard_violation', violationCount}` (NEVER violation text); returns `{ok: false, error: 'commercial-guard-violation', violations: [{field, code}]}`. On eligibility blocker → logs `sow_draft_failed` with `failureReason: 'sow_eligibility_blocked'`. Happy path → inserts row with `status='candidate'` + `delivery_surface='sow_draft_candidate'` + `approval_state='unreviewed'` + `pricing_review_state` copied from source + `draft_watermark=true`; prepends `PROPOSAL_GROUP_B_OMISSION_ENTRY` + per-option SOW-omission entries to `omitted_content`; logs `sow_draft_generated` activity event with sanitized metadata; revalidates proposal + engagement detail routes; returns snapshot id. **3 new activity event types** added to `lib/activity/types.ts` (`sow_draft_generated` tone `success`, `sow_draft_failed` tone `risk`, `sow_draft_voided` tone `neutral`) + timeline tones/labels (`SOW Draft generated` / `… failed` / `… voided`). The Sprint P6-B action emits `sow_draft_generated` + `sow_draft_failed`; `sow_draft_voided` is reserved for Sprint P6-C's possible swap of the existing `voidProposalDeliverySnapshotAction`'s event type when `delivery_surface='sow_draft_candidate'`. **No UI mounted in P6-B** — the action is callable from server-action contexts only; Sprint P6-C will wire it into the proposal page. **Live validation deferred to Sprint P6-C** per the Sprint P2 → P3 precedent (no safe operator-mediated invocation path without a UI button). Static verification: every module type-checks + lints clean; the new types compose correctly with the existing jsonb mappers; the SOW guard's pattern union math is exact (44 + 26 = 70); the proposal-candidate guard's existing behaviour is provably unchanged because `COMBINED_RULES` and `runProposalCommercialGuard` were not modified. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **28 app routes** (unchanged from Sprint P5); every route's First Load JS byte-identical to the Sprint P5 baseline (no client-bundle delta — all new modules are server-only). **No package dependency.** Group-B grep clean (`commercial-guard.ts`'s prohibition strings are `RegExp` patterns, the canonical allowed shape). All 5 LockedActionButton sites unchanged. No middleware / `next.config.mjs` / API route / public route / storage / PDF library / AI synthesis change.

**Phase 1B SOW Draft Sprint P6-C (2026-05-17) · internal SOW Draft route + Past SOW Drafts panel + `Prepare SOW Draft` unlocked at end** shipped. **First operator-visible SOW Draft surface.** No public route (no `app/s/` directory; no `sow_share_tokens` migration), no `Send to Client` unlock, no email/CRM/e-sign/PDF binary/storage/Group-B wiring. **New operator-only route** `app/app/engagements/[id]/proposal/sow/[snapshotId]/page.tsx` (server component, `/app/*`-gated middleware, persisted-UUID-only, `dynamic="force-dynamic"` + `revalidate=0` + `fetchCache="force-no-store"` defeats Next 14 fetch cache per Sprint 4D-C precedent, defense-in-depth `snapshot.engagementId === params.id` AND `snapshot.deliverySurface === 'sow_draft_candidate'` mismatch → `notFound()`, `.slate-print-light` palette wrapper, on-screen-only back link to proposal page). **New snapshot-pure renderer** `components/proposals/sow-draft-document.tsx` reads ONLY from `option_snapshot` / `source_context_snapshot.sowDraft` / `commercial_guard_result` / `omitted_content` jsonb; narrows the SOW jsonb via a structural `readSowDraftFromSnapshot(snapshot)` helper so the base proposal type stays untouched. Renders canon-mandated surfaces per `docs/26` § Required Markings / Disclaimers verbatim — operator-only banner ("Operator-only SOW Draft · NOT SENT BY SLATE · No client delivery"), voided banner when applicable, generated-state banner ("Operator-only SOW Draft · operator-approved | operator review only · not sent by SLATE"), identity header "Draft SOW · not executed" with body note "This draft is for review and planning only. It is not binding until reviewed, approved, and executed by authorized parties.", SOW safety strip (affirmative `Passed` or sanitized violation count — NEVER banned-phrase text), draft-watermark warning when present, disclosure card with canon-derived pricing notice (`pricing_review_state`-conditional via `sowDraft.pricingNotice` with sensible fallback), dedicated legal-boundary notice card (via `sowDraft.legalBoundaryNotice` with fallback), SOW scope + deliverables + assumptions + dependencies + exclusions section, client/operator responsibilities section (renders even when empty with placeholder hint), optional open-questions section, per-option detail carry-forward (preserves audit trail; pricing hidden when `placeholder`), intentionally-not-included appendix (Group-B canonical + per-option SOW omissions), mandatory four-line footer ("Not a contract. Not an executed SOW. Not a binding quote. Not authorization to begin work. Final scope, pricing, timeline, and terms require written approval and execution by authorized parties."). **ZERO internal UUIDs / commercial-guard codes / e-sign / sign / accept / agree controls / final pricing while placeholder.** **New panel** `components/proposals/past-sow-drafts-panel.tsx` (server component using existing `getProposalDeliverySnapshotsForProposal`; filters returned list to `delivery_surface === 'sow_draft_candidate'` AFTER also using the full list to find the latest non-voided + approved `client_proposal_candidate` snapshot as the SOW source; per-row badges: status / `SOW Draft` outline / approval / draft-watermark / commercial-guard pass-fail icon + counts / included-option count / pricing-review label / proposal-status-at-generation / generated-by initials / voided banner with timestamp + reason; per-row actions: Open SOW Draft (`target="_blank"` Link) + Void (when not voided); header: Generate SOW Draft + draft count; `id="past-sow-drafts-panel"` anchor for the workspace unlock to scroll to). **New client component** `components/proposals/generate-sow-draft-button.tsx` (fires Sprint P6-B's `generateSowDraftCandidateAction` via `useTransition`; disabled with help string when no eligible source Proposal Candidate exists; success notice with Open SOW Draft link + Draft watermark chip + included/omitted counts + approval/pricing review state; failure notice with translated reason — including 14 specific error codes mapped to operator-friendly strings — and either eligibility messages or sanitized `{field, code}` violation list — NEVER banned-phrase text). **New client component** `components/proposals/void-sow-draft-button.tsx` (thin wrapper mirroring `void-proposal-candidate-button.tsx`; two-step confirm with SOW-specific default reason "Superseded by a newer SOW Draft."; calls shared `voidProposalDeliverySnapshotAction`). **`voidProposalDeliverySnapshotAction` extended** in `lib/proposals/snapshot-actions.ts` — fetches `delivery_surface` in the row select (was previously omitted) + branches the emitted activity event on it: `sow_draft_candidate` rows now emit `sow_draft_voided` (title "SOW Draft voided", summary "Operator marked an earlier SOW Draft snapshot stale.") instead of `proposal_snapshot_voided`; non-SOW rows unchanged at `proposal_snapshot_voided`; metadata payload extended with `deliverySurface`; the route revalidation also branches between `/proposal/sow/[snapshotId]` and `/proposal/candidate/[snapshotId]` so the operator's open tab re-renders with the voided banner in either lane. **`Prepare SOW Draft` UNLOCKED at end of Sprint P6-C** — `components/proposals/proposal-workspace.tsx` accepts a new JSON-safe `isPersisted: boolean` prop (default `false`; documented identically to the existing `engagementId` JSON-safety comment); replaces the `LockedActionButton label="Prepare SOW Draft"` at `proposal-workspace.tsx:375` with a primary in-page anchor button (`<Link href="#past-sow-drafts-panel">Prepare SOW Draft</Link>` with `FileSignature` icon) for persisted UUID engagements; mock / legacy slug engagements keep the locked button. Adjacent footnote copy updated to drop "SOW draft" from the locked list for persisted engagements while keeping "Send and signature stay locked behind a later commercial sprint." `app/app/engagements/[id]/proposal/page.tsx` imports the new panel, threads `isPersisted` to `<ProposalWorkspace>`, and mounts `<PastSowDraftsPanel>` after `<ProposalCandidatesPanel>` and before the boundary reminder when `proposal && isPersisted`. **`Send to Client` (proposal-workspace), `Prepare Report` (roadmap), `Export Report` (report mock paths) ALL remain locked verbatim** — `LockedActionButton` site count unchanged at 4 for persisted engagements; Sprint P6-C unlocked exactly `Prepare SOW Draft`. **Live validation against `76097653-…`'s proposal `77031092-…` was deferred** — read-only SQL against the shared production Supabase remains classifier-blocked, matching the `docs/21` / `docs/23` precedent. The audit-gate sprint (`docs/27`) is where the operator-driven end-to-end live walkthrough lands (Generate fresh Proposal Candidate → approve → Generate SOW Draft → open route → verify all canon chrome + zero internal leak → void → verify `sow_draft_voided` event emission + post-void render). Static verification complete: every module type-checks + lints clean; the renderer composes correctly against the existing snapshot jsonb shapes; the `voidProposalDeliverySnapshotAction` event-type branch is exact (`row.delivery_surface === 'sow_draft_candidate'` → `sow_draft_voided` / else → `proposal_snapshot_voided`); the new route + panel + button + workspace unlock compose end-to-end. **Public route absence verified by inventory** — no new `app/s/` directory; no SOW share token table; no service-role public lookup helper. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **29 app routes** (+1 new `/app/engagements/[id]/proposal/sow/[snapshotId]` at 187 B / 96.2 kB — byte-identical shape to `/app/engagements/[id]/proposal/candidate/[snapshotId]`); `/app/engagements/[id]/proposal` grows `9.87 kB / 113 kB` → `10.5 kB / 114 kB` (+0.6 kB / +1 kB for the new panel + Generate/Void buttons + workspace unlock); every other route within ±0 KB of the Sprint P6-B baseline. **No package dependency.** Group-B grep clean. No middleware / `next.config.mjs` / API route / public route / storage / PDF library / AI synthesis change.

**SOW Draft MVP Acceptance Audit (2026-05-17) · Accepted with notes.** `docs/27_SOW_DRAFT_MVP_ACCEPTANCE_AUDIT.md` is the formal sign-off covering Sprint P6-A (commit `c68fd81` canon) + P6-B (`2128e0b` foundation) + P6-C (`ad29320` operator surface + unlock) together as the operator-only SOW Draft surface. **Read-only audit sprint** — no source code modified, no commits authored, docs-only output (`docs/27` + `docs/08` + `docs/10`). Inventory audit confirmed all 8 SOW-specific files + 8 modified support files present at canonical sizes (route + panel + 2 client buttons + renderer + eligibility evaluator + SOW action + canon doc; commercial-guard + types + mappers + snapshot-actions + activity types + activity-timeline + proposal page + proposal-workspace). Canon / boundary audit clean against `docs/26` (SOW Draft internal + non-binding; reuses `proposal_delivery_snapshots` with `delivery_surface='sow_draft_candidate'`; no migration; no public `/s` or `/sow` route; no SOW share-token table; no e-signature / email / CRM / PDF binary / Group-B wiring; `Send to Client` LOCKED). Commercial guard pattern math: FINANCIAL_CLAIM_PATTERNS 14 ✓, COMMERCIAL_FINALITY_PATTERNS 6 ✓, ROADMAP_COMMITMENT_PATTERNS 6 ✓, SOW_DRAFT_FINALITY_PATTERNS 26 ✓; **PROPOSAL_FINALITY_PATTERNS 19 vs canon-claimed 18 — one-off documentation discrepancy** flagged as audit note (runtime `commercial_guard_result.patternCount` reads 45/71 for proposal/SOW snapshots vs canon-cited 44/70; not a runtime defect; doc reconciliation needed in `docs/24` / `docs/26` / `docs/25` or by trimming one redundant proposal-finality pattern). Activity-event metadata verified to NEVER include violation text (counts only) + result-payload `violations` only carries `{field, code}`. Eligibility evaluator implements 14 of 15 union members in the body (15th `unsupported_surface` declared but unreachable in production — action layer pre-rejects with `error: 'source-surface-not-proposal-candidate'`; flagged as cosmetic audit note). All-reasons return semantic verified (no first-fail short-circuit; only `source_snapshot_missing` early-returns because without a source there is nothing else to evaluate — canon-correct). SOW route defense-in-depth verified at `app/app/engagements/[id]/proposal/sow/[snapshotId]/page.tsx:116-127`: `dynamic="force-dynamic"` + `revalidate=0` + `fetchCache="force-no-store"` + `snapshot.engagementId === params.id` mismatch → `notFound()` + `snapshot.deliverySurface === 'sow_draft_candidate'` mismatch → `notFound()`. Renderer content-inclusion audit (`components/proposals/sow-draft-document.tsx`): all 21 canon-mandated SOW chrome elements grep-matched in source (operator banner "NOT SENT BY SLATE" / "Draft SOW · not executed" header / body note "It is not binding until reviewed, approved, and executed by authorized parties" / generated date + selected option count + status/approval/pricing chips / scope statement / deliverables / exclusions / assumptions / dependencies / proposed timeline / client+operator responsibilities / open questions / pricing notice / legal boundary notice / intentionally-omitted appendix + Group-B canonical / SOW content safety affirmative strip / mandatory four-line footer). Renderer content-exclusion audit: zero final-pricing while `pricing_review_state='placeholder'` (`OptionCard.showPricing = pricingReviewState !== 'placeholder'` guard); zero e-sign / sign / accept / agree / send-to-client / public-share / share-token surfaces; legal terms named ONLY inside the canon-required boundary notice that explains why they're absent (governing law / indemnification / liability / warranty / termination disclosed as deliberately omitted, not surfaced as content). **Internal-route content-scope clarification**: the strict ZERO-internal-UUID rule applies to the future public `/s/[token]` SOW share route (Sprint P7-A scope), NOT to this operator-only internal route — internal references like option ids, generated-by initials, draft watermark badge, operator-banner text are intentionally rendered for operator audit. Past SOW Drafts panel verified mounted at `app/app/engagements/[id]/proposal/page.tsx:269-278` between `<ProposalCandidatesPanel>` and boundary reminder for persisted engagements; `id="past-sow-drafts-panel"` anchor present at `past-sow-drafts-panel.tsx:116`; latest non-voided + approved `client_proposal_candidate` auto-resolved (`past-sow-drafts-panel.tsx:100-110`) as SOW source; `Generate SOW Draft` disabled with help string when none (`generate-sow-draft-button.tsx:58-95`). `Prepare SOW Draft` UNLOCKED audit: `proposal-workspace.tsx:389-415` renders the in-page anchor button ONLY for persisted UUID engagements (`isPersisted ? <Link href="#past-sow-drafts-panel"> : <LockedActionButton>`); no email, no public route, no share token mint, no PDF binary, no Send to Client. Locked-control matrix: 5 `LockedActionButton` mount sites total. For persisted engagements 3 actively render (`Send to Client` at `proposal-workspace.tsx:416-420`, `Prepare Report` at `roadmap/page.tsx:122`, `Export Report` at `report/page.tsx:279`); mock paths render 5 (adds `Prepare SOW Draft` at line 410-414 + `Prepare Client Review` at `proposal/page.tsx:143-146`). Public-route absence verified by inventory (no `app/s/` directory; no `supabase/migrations/*sow*` file; no `lib/proposals/sow-share-*` modules; no `lib/proposals/sow-public-*` modules; no service-role SOW helpers). Build + lint clean; **29 app routes** (unchanged from Sprint P6-C); reduced-cardinality route table: `/app/engagements/[id]/proposal` 10.5 kB / 114 kB, `/proposal/sow/[snapshotId]` 187 B / 96.2 kB (byte-identical to `/proposal/candidate/[snapshotId]`), `/p/[token]` 155 B / 87.4 kB, `/r/[token]` 155 B / 87.4 kB, `/scorecard/results` 10.7 kB / 119 kB — all byte-identical to Sprint P6-B baseline except the proposal page (+0.6 kB / +1 kB for the Sprint P6-C panel + buttons + workspace unlock). **Live walkthrough deferred** to operator session against `76097653-fedb-42e5-9ef6-e89a0e97f802` — read-only SQL against shared production Supabase remains classifier-blocked, mirroring `docs/21` / `docs/23` / `docs/25` precedent. Walkthrough notes template at `artifacts/walkthroughs/sow-draft-mvp-acceptance/notes.md` (gitignored) — 14-step canonical operator walkthrough enumerated (Generate Proposal Candidate → Approve → Generate SOW Draft → verify DB row + activity event → Open route + verify all canon chrome + zero prohibited surface → Void → verify `sow_draft_voided` event + post-void banner render + `Prepare SOW Draft` anchor-only behaviour + `Send to Client` LOCKED). Operator captures 5 canonical screenshots (`proposal-page-prepare-sow-draft-unlocked.png` / `past-sow-drafts-panel.png` / `internal-sow-draft-route.png` / `internal-sow-draft-voided.png` / `activity-log-sow-generated-voided.png`) into the gitignored artifacts folder post-audit. **12 carry-forward items + 3 audit notes** recorded — none blocks acceptance: (1) Sprint P7-A SOW share decision canon (recommended defer); (2) SOW share tokens if P7-A approves; (3) SOW approval flow if separate state needed per `docs/26` § Open Decisions item 8; (4) pricing approval workflow; (5) implementation-credit approval workflow; (6) e-signature provider decision; (7) Send to Client channel canon at `docs/28`; (8) production hardening backlog from `docs/23` + `docs/25` (SLATE_SHARE_TOKEN_ACCESS_PEPPER + debounce + dev short-expiry + audience-label UI + revoke-from-panel UI + engagement-title fallback); (9) pattern-count documentation reconciliation (Audit note 1); (10) `unsupported_surface` union member cleanup (Audit note 2); (11) optional filter of Past Proposal Candidates panel to hide SOW Drafts explicitly; (12) operator-driven live walkthrough confirmation. `git status --short` after the audit: 3 docs files modified (this `docs/10` + `docs/08` + new audit doc `docs/27`) + 1 new walkthrough notes file under gitignored `artifacts/`. **No source changes.** **Recommendation: declare SOW Draft MVP complete and move next to Sprint P7-A — SOW share route decision canon (recommended default: defer)** OR move directly to the production-hardening backlog from `docs/23` + `docs/25`. `Send to Client` remains LOCKED across both options.

**Production Hardening Sprint H1 (2026-05-18) · report + proposal share-link hardening** shipped. Implements the carry-forward backlog shared by Client Report Link MVP (`docs/23`) + Proposal Review Link MVP (`docs/25`) acceptances. **No schema, no migration, no public route, no new package dependency, no `Send to Client` unlock, no email/CRM/e-sign/PDF binary/storage/Group-B wiring, no SOW share token, no SOW public route.** **New shared module** `lib/share-tokens/access-signature.ts` (`server-only`; pure Node-crypto wrappers): `getShareTokenAccessPepperStatus()` returns `{configured, envName: 'SLATE_SHARE_TOKEN_ACCESS_PEPPER'}`; `shouldOmitAccessHashes()` is the convenience predicate; `hashAccessFingerprint({ip, userAgent})` returns `{ipHash, uaHash, combinedSig, hashesOmitted}` where each hash is `sha256(pepper + ':' + value)` truncated to 128 bits of hex (32 chars) when pepper is set OR all-null + `hashesOmitted: true` when pepper is unset. The combined `(ip, ua)` signature is computed only when both components are supplied so debounce keys don't collide across NAT'd offices or one viewer's two browsers. Explicit production-recommendation comment that the pepper must be configured before relying on IP/UA fingerprinting. **Both `recordShareTokenAccess` (report) + `recordProposalShareTokenAccess` (proposal) refactored** to import the shared helper — eliminates the two `sanitizeAccessMetadata` + `peppered` helpers that had drifted in parallel. The activity-event metadata key shape (`ipSig`, `uaSig`, `hashesOmitted`) is now the helper's return contract; callers must not rename downstream because `lib/activity/log.ts`'s `FORBIDDEN_KEY_PATTERNS` strip would catch `ip` / `userAgent` / etc. **5-minute access-log debounce** added to both surfaces: read existing `metadata` jsonb + `last_accessed_at` per token; when the combined `(ip, ua)` peppered signature is available AND `metadata.lastAccessSig` matches AND `metadata.lastAccessSigAt` is within `5 * 60 * 1000` ms, SLATE skips the `access_count` bump + skips the activity event + leaves `last_accessed_at` unchanged (the previous bump captured the window start). When no signature is available (pepper unset OR missing IP/UA), debounce is bypassed and every access is logged (safe-degrade — production should set the pepper). `access_count` is a soft counter (canon explicitly does not treat it as a security boundary) so under-counting during a debounce window is an accepted MVP trade. No migration: the `metadata` jsonb column on both `report_share_tokens` (0014) + `proposal_share_tokens` (0016) already accepts the `lastAccessSig` / `lastAccessSigAt` shape. **Dev-only short-expiry affordance** added to both `generateShareLinkAction` + `generateProposalShareLinkAction` via new `resolveShareTokenExpiry` / `resolveProposalShareTokenExpiry` helpers: when `expiryMinutes` is passed AND `NODE_ENV !== "production"` OR `SLATE_SHARE_TOKEN_ALLOW_DEV_EXPIRY === "true"`, expiry resolves to `now + minutes * 60s` (capped at 30 days expressed in minutes); production callers passing `expiryMinutes` get the parameter silently ignored — the 14d default applies. Action-level support only per Sprint H1 prompt's "if adding UI is too much, add action-level support only and document" guidance — the dev select would land in a future Operator Toolbelt sprint if appetite materializes. **Audience-label + recipient-email UI threading** added to both `generate-share-link-button.tsx` + `generate-proposal-share-link-button.tsx` via a new `<details>`-collapsed `<OperatorTrackingFields>` block above the Generate button. Two inputs (audience label, 80 chars; recipient email, 254 chars). Operator-facing copy reads exactly: "Optional. Used for operator tracking only. SLATE does not email this recipient. The email is hashed at rest; SLATE never stores the raw address." Raw email never reaches the DB OR the activity feed — the server action calls the existing `hashRecipientEmail(rawEmail)` helper (SHA-256 hex on lowercase-trimmed input, identical shape to `token_hash`'s CHECK constraint). The activity event metadata already carried `recipientHashPresent: Boolean(recipientEmailHash)` from Sprint 4D-B / P4 — never the hash bytes, never anything matching the activity logger's strip patterns. **Revoke-from-panel UI** for proposal share tokens: new `components/proposals/revoke-proposal-share-link-button.tsx` (thin client mirror of `components/reports/revoke-share-link-button.tsx`; two-step confirm flow — initial Revoke chip → reason input + Confirm/Cancel; calls existing `revokeProposalShareTokenAction` which was already in place from Sprint P4). New `ShareTokenSummary` + `ShareTokenRow` sub-components added to `components/proposals/proposal-candidates-panel.tsx`: fetches `getProposalShareTokensForProposal(proposalId)` alongside the snapshot query via `Promise.all`; groups tokens by `snapshotId` into a `Map<string, ProposalShareToken[]>`; per-row renders status badge (active/revoked/expired with `displayStatus = active + isPastExpiry ? expired : status` correction) + created/expires timestamps + access count + last access timestamp + audience label + "Recipient hashed at rest" indicator with `title` tooltip when `recipientEmailHash` is present + revoke reason when status='revoked'; active tokens get the `<RevokeProposalShareLinkButton>` rendered beside the row. Identical "Recipient hashed at rest" indicator added to the existing report-side `ShareTokenSummary` for visual symmetry. **Stale Sprint copy cleanup**: `generate-share-link-button.tsx` chip text "Public route lands in Sprint 4D-C" (warning tone) → "Public route is live" (success tone); `generate-proposal-share-link-button.tsx` chip text "Public proposal route lands in Sprint P5" (warning tone) → "Public proposal route is live" (success tone). **Engagement-title fallback fixed** in both `app/r/[token]/page.tsx` + `app/p/[token]/page.tsx` `resolveClientReportTitle` / `resolveClientProposalTitle`: previous code selected `engagements.company_name`, a column that **does not exist** on the `engagements` table — canon: company name lives on `accounts.name`, joined via `engagements.account_id` (confirmed by `lib/engagements/mappers.ts:458` which maps `accountName` → TS `companyName`). The mismatch meant every public route was falling through to the generic "Client Report" / "Proposal Review" titles. Fix: query `engagements.name`, `engagements.engagement_type`, `engagements.account_id`, then secondary query `accounts.name` for the company name. Three-tier fallback chain: account name → engagement name → generic title — so even an orphaned engagement-id surfaces something non-generic before falling through. Both queries run under the service-role client; no UUIDs reach the rendered DOM. **All Sprint H1 non-goals preserved verbatim**: zero new public route (no `app/s/` or `app/sow/` directory — verified by `find app -type d -name s -o -type d -name sow`); zero new SOW share tokens (no `sow_share_tokens` migration); zero `Send to Client` unlock (`proposal-workspace.tsx:417` `LockedActionButton label="Send to Client"` unchanged); zero email/CRM integration; zero e-signature; zero PDF binary/storage; zero new package dependency (verified by `git diff --name-only package.json package-lock.json` → empty); zero token-format / token-hash semantics changes; zero AI synthesis change; zero Group-B wiring (grep clean for `BenchmarkComparisonBars` / `AISavingsWaterfall` / `RoiBridge` imports outside `components/charts/` + `/app/charts-preview`); zero Benchmark Gate 1 / Financial Gate 1 advancement; zero migration changes (`git diff --name-only supabase/` → empty); Visx plan file untouched. **Live validation deferred** to operator session against `76097653-fedb-42e5-9ef6-e89a0e97f802` — read-only SQL against shared production Supabase remains classifier-blocked, matching `docs/21` / `docs/23` / `docs/25` / `docs/27` precedent. Static verification complete: every module type-checks + lints clean; debounce + dev-expiry + audience-label + recipient-email + revoke-from-panel + title-fix compose end-to-end. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **29 app routes** (unchanged from Sprint P6-C — no new route added; the hardening was orthogonal to the route surface). Reduced-cardinality route table: `/r/[token]` 155 B / 87.4 kB (unchanged), `/p/[token]` 155 B / 87.4 kB (unchanged), `/app/engagements/[id]/proposal` `10.5 kB / 114 kB` → `11.4 kB / 115 kB` (+0.9 kB / +1 kB for the new operator-tracking fields + share-token summary + revoke button), `/app/engagements/[id]/report` `11.7 kB / 115 kB` → `12.1 kB / 115 kB` (+0.4 kB / +0 kB for the operator-tracking fields + recipient-hashed indicator), `/app/engagements/[id]/proposal/sow/[snapshotId]` 187 B / 96.2 kB (unchanged), `/scorecard/results` 10.7 kB / 119 kB (unchanged). **No carry-forward audit notes**: every backlog item from `docs/23` § Carry-forward + `docs/25` § Carry-forward + `docs/27` § Known Backlog items 8 + 9 + 11 either landed in this sprint OR was explicitly recommended-default-deferred. Remaining open items inherit from `docs/27` § Known Backlog (Sprint P7-A SOW share canon, SOW approval flow, pricing/implementation-credit approval workflows, e-signature provider, channel canon for Send to Client, pattern-count documentation reconciliation, `unsupported_surface` eligibility cleanup). `git status --short` after the sprint: 10 modified source files + 2 new source files (`lib/share-tokens/access-signature.ts`, `components/proposals/revoke-proposal-share-link-button.tsx`) + `docs/08` + `docs/10` modified.

**Sprint P7-A — SOW Share Route Decision Canon (2026-05-18) · Defer.** `docs/28_PHASE_1B_SOW_SHARE_ROUTE_DECISION_CANON.md` is the formal decision authored after `docs/27` accepted the SOW Draft MVP. **Documentation-only sprint** — no source code, no schema, no migration, no public route, no `sow_share_tokens` table, no new package dependency, no API route, no middleware / `next.config.mjs` / RLS change, no email / CRM / e-signature / PDF / storage, no `Send to Client` unlock, no AI synthesis change, no Group-B wiring. Decision: **DEFER public SOW share route until after the Send to Client channel canon (`docs/29`) is authored.** Three valid outcomes were considered (ship now → build `/s/[token]` in Sprint P7-B; defer-until-channel → postpone until `docs/29` lands; defer-permanently → conclude SOW path is internal-only forever). Sprint P7-A canonically picks **defer-until-channel** as the recommended default. Canon answers 10 specific questions with hard + soft bindings: (1) public SOW link before Send to Client? **No / defer** — sequencing risk (public SOW would constrain channel canon rather than channel canon constraining SOW), acceptance risk (cumulative weight of scope + deliverables + pricing notice + legal-boundary notice reads as "we're signing this" even with explicit non-binding copy), e-signature confusion (single largest reason — see Q9 below); (2) URL shape if shipped later? **`/s/[token]`** (soft binding — short single-character path, visually distinct from `/r/` and `/p/`, low context leak in pasted URLs); alternatives `/sow/[token]` (more explicit but leaks "SOW" to URL logs) and `/p/[token]` with surface switch (rejected — separation by surface keeps privacy / eligibility / claim-guard / cascade semantics cleanly separated); (3) separate `sow_share_tokens` table? **Yes** (soft — separation wins on reasoning clarity + legal-separation criterion: an auditor reading the schema cold must be able to point at one table and say "these are SOW share artifacts"); (4) extra disclaimers? Hard verbatim copy specified in `docs/28` § 4 — header banner "Draft SOW · not executed · not authorisation to begin work"; body note "This draft is for review and planning only. It is not a contract, not an executed SOW, and not authorisation to begin work. Final scope, pricing, timeline, and terms require written approval and execution by authorised parties."; canon-required pricing notices for both `placeholder` and `manually_approved`/`workflow_approved` states; mandatory legal-boundary notice; closing four-line footer; **and a new public-only disclaimer ("This document is for review by the named recipient. SLATE provides no execution or signature workflow on this page. Any approval, signature, or commencement of work happens through your separate contract process.") layered on top** — explicitly disclaims execution/signature workflow at the surface level so a forwarded link cannot be interpreted as "the place where the SOW is signed"; (5) approval state required before sharing? Hard — SOW-side `approval_state='approved'` PLUS source Proposal Candidate also `approved` (defense-in-depth) PLUS commercial guard `passed=true` PLUS within 14d default expiry PLUS **explicit operator confirmation dialog beyond normal flow** ("This will create a public link to the SOW Draft. The recipient can view the document until you revoke the link. Continue?" — the SOW lane is the only lane that warrants this extra friction because it carries the highest legal weight) PLUS **audience label MANDATORY on mint** (operator audit log requirement); hard prerequisite — Sprint P6-C carry-forward item `approveSowDraftSnapshotAction` MUST ship before any Sprint P7-B (per `docs/26` § Open Decisions item 8); without SOW-side approval the eligibility evaluator can never return eligible; (6) pricing on public surface? Hidden when `pricing_review_state='placeholder'` (already canon); "Estimated · subject to final approval. Not a binding quote." when manually/workflow-approved; **forbidden across all states** — payment terms / invoice schedule / currency conversion / discount math / automatic totals — SOW commercial guard's 26 SOW-finality patterns already enforce these at generation time; no additional pattern needed; (7) recipient/signatory identity? Audience label MANDATORY (unlike report/proposal where it's optional — operationally a SOW link in the wild without an audience label is indistinguishable from a leak); recipient email OPTIONAL (hashed-at-rest only via existing `hashRecipientEmail`); **signatory identity NEVER COLLECTED** — asking for signatory identity on a non-signature page invites recipient to interpret page as signature page; no name field, no role field, no signature-block scaffold; (8) what happens when SOW is voided? Cascade-revoke mirrors Sprint P4 pattern (revoke all active SOW share tokens, `revoke_reason='snapshot_voided'`, emit one `sow_share_token_revoked` per cascade with `cascade: true` metadata, rollup count into parent `sow_draft_voided` event's metadata, generic-unavailable post-cascade with identical response shape — size-based diffing must not reveal which condition fired); SOW-specific additional rule — voiding source Proposal Candidate MUST also cascade-revoke descendant SOW share tokens (coherency requirement; without this, voided proposal leaves descendant SOW shares active); (9) does public SOW sharing create e-signature confusion? **YES — single largest reason for the deferral recommendation.** Every other SOW-shaped page recipients have seen in their professional life (DocuSign / PandaDoc / HelloSign / Adobe Sign / Conga / Ironclad) ends with a signature block. Absence of signature block on SLATE surface is informative to careful readers but every other signal on the page primes recipient to look for one; cognitive frame "review surface" is fragile in a way the report (advisory) and proposal-review (commercial discussion) surfaces are not. Four mitigations available (mandatory public-only disclaimer / draft-watermark stripe / removing all action-feeling UI / `noindex,nofollow`); **even with all four mitigations the surface still ships before `docs/29` defines SLATE's relationship to execution path** — a clean answer requires `docs/29` first (channel canon may decide SLATE never hosts client-facing SOW content at all and only provides operator-side "copy link to your DocuSign" affordances; in that world `/s/[token]` is dead code from day one); (10) defer until channel canon? **YES.** Doc-number reservation chain locked: `docs/27` (SOW Draft Acceptance, LANDED) → `docs/28` (this — DEFER decision) → `docs/29` (Send to Client channel canon, RECOMMENDED NEXT SPRINT) → operator decision point branching on channel canon outcome (ship `/s/[token]` → Sprint P7-B implementation; OR operator-only external-platform share → no `/s` route; OR defer Send to Client further → SOW share also stays deferred) → `docs/30` (SOW Share Link MVP Acceptance Audit only if Sprint P7-B is later approved + implemented + acceptance-audited) → `docs/31` (reserved post-channel-canon execution slot). Decision matrix in `docs/28` enumerates 12 hard-bound vs soft-bound parameters with explicit "reconsidered if/when" triggers. Non-goals binding: no `app/s/[token]/page.tsx`, no `sow_share_tokens` migration, no `lib/proposals/sow-share-*` modules, no `components/proposals/generate-sow-share-link-button.tsx`, no `approveSowDraftSnapshotAction` (deferred to a future Sprint P6-E or Sprint P7-B-prereq depending on activation path), no Send to Client unlock, no email/CRM/e-sign/PDF/storage/package-dep/middleware/RLS changes. Send to Client remains LOCKED across every option in the canon's decision matrix until `docs/29` explicitly unlocks it. `git status --short` after the sprint: 3 docs files (this `docs/10` + `docs/08` + new `docs/28`); zero source / schema / migration / config / package / middleware / API / public-route / storage / PDF / email / CRM / e-sign / AI file touched. **Sprint P7-A authorises only the deferral decision + sequencing — not implementation of any SOW share code.** Reversing the deferral before `docs/29` exists requires explicit operator authorisation AND canon amendment to `docs/28`.

**Sprint C1 — Send to Client Channel Canon (2026-05-18) · Option A.** `docs/29_PHASE_1B_SEND_TO_CLIENT_CHANNEL_CANON.md` is the formal channel-boundary decision authored after `docs/28` deferred the SOW share decision. **Documentation-only sprint** — no source code, no schema, no migration, no public route, no `sow_share_tokens` table, no new package dependency, no API route, no middleware / `next.config.mjs` / RLS change, no email / CRM / e-signature / PDF / storage, no `Send to Client` unlock, no AI synthesis change, no Group-B wiring. Decision: **Option A — copy-link / operator-mediated only for the first `Send to Client` unlock.** SLATE itself does not send email, push to CRM, open `mailto:` links, invoke send providers, collect from-addresses/subject lines/bodies, or track delivered/bounced/opened at the email layer. The operator copies a link surfaced by SLATE and delivers it through their own pre-existing channel (email client, CRM, Slack, in-person). Send to Client = **operator confirms intent + SLATE records audit event + operator copies the link**. Four valid outcomes were considered (A copy-link only, B email sent by SLATE, C CRM-mediated, D hybrid); Sprint C1 canonically picks A. The strongest argument is the historical pattern from Sprints P5 + P6-C — the first unlock for `Prepare Client Review` AND `Prepare SOW Draft` was a non-send action by design (in-page anchor to operator-mediated copy-once panel), and the same posture is canonically the safest default for `Send to Client`. Canon answers 20+ specific questions with hard + soft bindings: **(1) Meaning** — operator-mediated handoff via operator's own channel; SLATE never sends email, never pushes to CRM, never opens `mailto:`, never invokes send providers, never tracks transport-layer signals. **(2) Eligible artifacts** — report share link `/r/[token]` YES; Proposal Review Link `/p/[token]` YES; SOW Draft DEFERRED (per `docs/28` § 10 — separate Sprint decision conditional on docs/28 amendment + Sprint P7-B); operator-only routes (engagement detail / findings / opportunities / roadmap / internal PDF candidate / internal SOW Draft) NO. **(3) Audience model** — audience-label MANDATORY (max 80 chars; unlike raw mint where it's optional — operationally a sent link without audience label is indistinguishable from a leak); recipient-email OPTIONAL (max 254 chars, hashed via existing `hashRecipientEmail`, never raw); linkage to single existing share token (NOT mint+send — separation preserves copy-once-mint guarantee + forces conscious pairing); single recipient per event; no persistent recipient directory. **(4) Audit events** — two `*_sent_to_client` event types reserved (tone `brand`, labels "Report/Proposal sent to client") with metadata `{tokenId, snapshotId, reportId|proposalId, audienceLabel, recipientHashPresent: boolean, sentAt}`; two `*_send_failed` types reserved for sanitized failures; re-send to same audience allowed (each separate audit entry); re-send to different audience allowed (new audit entry with new label); revoked/expired token rejected at action layer (emits failure event, NOT success event); zero raw URL / raw token / raw email in metadata. **(5) Delivery confirmation** — operator confirms → SLATE records audit event + `metadata.lastSentToClientAt` ISO timestamp + `metadata.sendCount` atomic increment (NO schema migration — both `report_share_tokens` + `proposal_share_tokens` already carry `metadata` jsonb); zero transport visibility (no delivered/bounced/opened/replied tracking); recipient-access correlation via existing public-route access events on same `tokenId`. **(6) E-signature** — HARD out-of-scope; no signature collection, no `mailto:` with acceptance-pre-populated body, no Accept/Sign-here/I-agree UI, no DocuSign/PandaDoc/HelloSign/Adobe-Sign/Conga/Ironclad linkage; commercial-guard pattern families (71 patterns runtime) already prohibit dangerous language at generation time. **(7) Contract/execution** — HARD; SLATE never delivers contracts/executable-agreements/SOWs/binding-offers/quotes-with-price-commitments; existing canon-required disclaimer footers preserved; SOW Drafts explicitly excluded from first Send to Client unlock; future executable-agreement workflow requires separate canon (≥ `docs/32`). **(8) SLATE sends email?** NO. Three rationales: compliance surface area (SPF/DKIM/DMARC alignment, bounce-handling, list-unsubscribe RFC 8058, CAN-SPAM/CASL/GDPR — operator's existing email client already inside compliance envelope, SLATE's send pipeline would not be); identity-confusion risk (recipient's relationship is with operator's firm not SLATE — SLATE-branded mail muddies that); operator workflow simplicity (existing client one paste away — marginal time savings small, surface-area cost large). **(9) CRM integration?** NO. Two rationales: operator's CRM is system of record (per-CRM build-out creates per-customer integration debt — each CRM has its own auth/contact-model/custom-field-shape/rate-limit posture); loose coupling beats tight coupling at Phase 1B maturity (operator pastes SLATE URL into CRM activity log as one-line entry — works against every CRM without per-CRM authoring). **(10) Failure/revoke** — thin audit layer; doesn't modify token lifecycle; existing cascade-revoke/expiry-flip/void semantics remain authoritative; `metadata.lastSentToClientAt` preserved through revoke; operator responsible for notifying recipient that link no longer active. **(11) Token expiry/reuse** — NO change to existing policy (14d default + 30d max + Sprint H1 dev-only minutes); Send to Client doesn't change `expires_at`/`status`/`access_count`/`last_accessed_at`; re-use across multiple sends allowed. **(12) Operator confirmation** — two-step confirmation modal (mirrors Sprint P4 + Sprint H1 two-step patterns); modal renders token state + audience-label-required + recipient-email-optional fields + copy-once URL + canon-verbatim disclaimer; on confirm, re-runs eligibility, updates metadata, hashes recipient, updates audience_label, emits sanitized event, revalidates page. **(13) Disclaimers** — canon-verbatim copy specified in `docs/29` § 13 for both report-side ("Marking this report as sent records the operator's intent in SLATE's audit log. SLATE does not deliver this link by email, CRM, or any other channel. After confirming, copy the URL above and deliver it through your own channel (email client, CRM, or in person). SLATE does not track recipient delivery beyond access events on the SLATE public route.") AND proposal-side (same prefix + four-denial commercial-discussion suffix); operators may not abbreviate/paraphrase/restructure without amending canon; disclaimers are operator-facing (rendered inside modal) NOT on public surface. **(14) PII/privacy** — raw recipient email NEVER persisted; audience label operator-facing only; new "no raw URL in audit fields" rule — activity event metadata MUST NOT include `/r/<token>` or `/p/<token>` URL even though operator just copied it (token hash + send counters sufficient audit signal); RLS unchanged. **(15) Channel security** — zero new public surfaces; zero new outbound network requests (no fetch to send provider/CRM/analytics); zero new credentials/secrets (no `SLATE_EMAIL_API_KEY`, no `SLATE_CRM_CLIENT_SECRET`); existing `SLATE_SHARE_TOKEN_ACCESS_PEPPER` + Supabase service-role keys remain only sensitive env vars; CSP/Referrer-Policy/X-Robots-Tag unchanged on public routes. **(16) Unlock prerequisites** — 8 hard items: `docs/29` accepted; audience-label-mandatory policy validated; recipient-email hashing path validated (Sprint H1 end-to-end); two-step modal pattern proven (Sprint P4/H1 precedents); activity-event taxonomy reserved (this canon); no conflicting unlock state; no SOW share dependency; lint+build clean. **(17) Sprint sequence** — `docs/29` accepted → Sprint C2-A foundation (action layer + event types + recipient hashing path validation + modal scaffolding, no UI button rendered, mirrors Sprint P2→P3 foundation-first pattern) → Sprint C2-B operator surface (per-token Send to Client modal + per-row button + revalidation + audit-event emission; **at END of C2-B: per-token `Send to Client` UNLOCKED** for report + proposal lanes only — `proposal-workspace.tsx:417` per-option lock REMAINS LOCKED per § 18) → `docs/30` Send to Client MVP Acceptance Audit (operator + send-failed + revoke-after-send live walkthrough) → post-acceptance: `docs/28` SOW share re-opens; per-CRM canon (`docs/31`+); email-send canon (`docs/31`+); e-signature canon (`docs/32`+) — operator chooses next direction. **(18) `proposal-workspace.tsx:417` per-option `Send to Client` button** — REMAINS LOCKED across Sprint C2 series; targets proposal-option-level "send whole proposal" semantic that doesn't map cleanly to share-token-mediated delivery (operator might want to send only specific options/snapshots/audiences); future canon may re-author (recommended slot `docs/32`+); serves as visible marker that operators trained on historic UI must use per-token flow. **(19) Decision matrix** — 16 hard-bound items + 12 open-decision items with recommended defaults enumerated (toast vs banner, default audience-label pre-population, per-row "Sent N times" chip, confirmation toast wording, cooldown/debounce between confirmations, per-token sendCount ceiling, required audit visibility on panel, send-failure metadata shape, empty-state copy, expired-token Send rejection, admin "all sends across workspaces" view slot). **(20) Non-goals binding** — no `app/api/send-to-client/*` route; no `lib/share-tokens/send-to-client.ts` action module (Sprint C2-A scope); no `components/{reports,proposals}/send-to-client-button.tsx` (Sprint C2-B scope); no activity event types added yet (Sprint C2-A scope); no `proposal-workspace.tsx:417` unlock; no email/CRM/e-sign integration; no PDF/storage; no package dep; no schema/migration/RLS change; no `middleware.ts` / `next.config.mjs` / `package.json` change; no AI synthesis change; no Group-B wiring; no public SOW route; no SOW share token. Lock matrix after future C2-B (Option A path): per-token Send to Client UNLOCKED (report + proposal panels) | per-option Send to Client at `proposal-workspace.tsx:417` LOCKED | Prepare Report LOCKED | Export Report mock paths LOCKED | Prepare SOW Draft mock paths LOCKED (persisted unlocked via P6-C anchor) | Prepare Client Review mock paths LOCKED (persisted unlocked via P5 anchor); total `LockedActionButton` mount sites unchanged at 5 — per-token send buttons are NEW client components not replacements. `git status --short` after sprint: 3 docs files (this `docs/10` + `docs/08` + new `docs/29`); zero source / schema / migration / config / package / middleware / API / public-route / storage / PDF / email / CRM / e-sign / AI file touched. **Sprint C1 authorises only the channel-boundary decision + Option A sequencing — not implementation of any Send to Client code.** Reversing Option A to Option B/C/D requires explicit operator authorisation AND canon amendment to `docs/29`.

**Sprint C2-A — Send to Client foundation (2026-05-18) · landed.** Code-change sprint per `docs/29` § 17 + § 23 + the Sprint C2-A prompt. Action layer + activity event types + recipient hashing path validation + reusable confirm-modal scaffold. **NO UI mount.** `LockedActionButton label="Send to Client"` at `proposal-workspace.tsx:417` remains LOCKED verbatim; zero imports of `markReportLinkSentToClientAction` / `markProposalLinkSentToClientAction` / `SendToClientConfirmModal` outside their own modules (verified by grep). No schema, no migration, no API route, no public route, no email/CRM/e-signature, no PDF/storage, no package dependency, no middleware / `next.config.mjs` / RLS change, no AI synthesis change, no Group-B wiring, no SOW share token, no public SOW route. **New shared module set** under `lib/client-delivery/`. `send-to-client-types.ts` — `SendToClientArtifactKind` union locked to `'report' | 'proposal'` (SOW intentionally absent from the union so consumers cannot type-check a SOW mark-sent call without a canon amendment); `SendToClientChannel` union locked to `'operator_mediated_copy_link'` (single entry, expansion requires new canon); 12-entry `SendToClientEligibilityReasonCode` union; `SendToClientPolicyInput` + `SendToClientTokenInput` narrowed shapes so the policy module stays domain-agnostic; `SEND_TO_CLIENT_METADATA_KEYS` constant set (`lastSentToClientAt` / `sendCount` / `lastSentChannel`); canon-verbatim `SEND_TO_CLIENT_DISCLAIMERS` for both report + proposal per `docs/29` § 13. `send-to-client-policy.ts` — `evaluateSendToClientEligibility` pure evaluator returning ALL applicable reasons (no first-fail short-circuit, mirrors proposal-share + SOW-draft evaluators); gates artifact-kind support, token existence, `token.status === 'revoked' | 'expired'` (including computed-expired when `expires_at` has passed even if status is still `active`), snapshot `status === 'voided'`, snapshot `eligibleForShare === false`, audience label mandatory + 80-char cap. `recipient.ts` — `normalizeAudienceLabel` (strips ASCII control chars `[\x00-\x1F\x7F]`, collapses whitespace runs, trims, caps at 80); `validateAudienceLabel` (returns `audience_label_missing` / `audience_label_too_long` failure codes); `validateRecipientEmailForHashing` (permissive `local@domain.tld` shape check, 254-char cap, returns `{normalizedForHashing: string | null}` so the caller hashes once and discards). **New `lib/reports/send-to-client-actions.ts`** with `markReportLinkSentToClientAction({shareTokenId, audienceConfirmation, recipientEmail?, operatorConfirmed: true})` cookie-bound operator action. Pipeline: UUID validation → `operatorConfirmed === true` strict check (modal cannot be bypassed) → audience label validation (mandatory per `docs/29` § 3) → recipient email validation (optional shape-check) → `supabase.auth.getUser()` → `report_share_tokens` row fetch via cookie-bound RLS → `report_delivery_snapshots` row fetch → `evaluateReportShareEligibility` AS DEFENSE-IN-DEPTH (snapshot voided since mint MUST NOT be sendable) → `evaluateSendToClientEligibility` against the policy-narrowed shape → on failure emit `report_share_token_send_failed` with sanitized `{shareTokenId, snapshotId, reportId, failureReason}` (only first-blocking code on the event; full reasons array stays in the action return for the UI) — **NEVER violation text, NEVER raw token, NEVER raw URL, NEVER raw email** — on happy path update token `metadata` jsonb with `lastSentToClientAt` ISO + `sendCount` atomic increment + `lastSentChannel='operator_mediated_copy_link'` AND update `audience_label` + `recipient_email_hash` columns if the operator edited them at confirm time (via existing `hashRecipientEmail` SHA-256 helper — raw email never reaches the DB or activity feed). **No schema change** — the `metadata` jsonb column already exists on `report_share_tokens` from migration 0014. Logs `report_share_token_sent_to_client` activity event with sanitized `{shareTokenId, snapshotId, reportId, audienceLabel, hasRecipientEmailHash: boolean, sentAt, sendCount, channel}`. Revalidates `/app/engagements/{id}/report`. Returns `{ok: true, status: 'sent', artifactKind: 'report', channel, tokenId, sentAt, sendCount, hasRecipientEmailHash, audienceLabel}` — **no raw token / URL / email in the return either** (the operator's clipboard holds the URL from the prior copy-once flow). **New `lib/proposals/send-to-client-actions.ts`** with `markProposalLinkSentToClientAction` — byte-similar mirror against `proposal_share_tokens` + `proposal_delivery_snapshots`; emits `proposal_share_token_sent_to_client` / `proposal_share_token_send_failed`; carries `proposalId` not `reportId` in event metadata; revalidates `/app/engagements/{id}/proposal`. **4 new activity event types** added to `lib/activity/types.ts` + `components/activity/activity-timeline.tsx`: `report_share_token_sent_to_client` (tone `brand`, label "Report link marked sent"); `report_share_token_send_failed` (tone `risk`, label "Report link send mark failed"); `proposal_share_token_sent_to_client` (tone `brand`, label "Proposal link marked sent"); `proposal_share_token_send_failed` (tone `risk`, label "Proposal link send mark failed"). **Labels deliberately use "marked sent" not "sent"** to avoid implying email-transport delivery per `docs/29` § 1 — the canon is explicit that SLATE never sends; the operator copies the link and delivers through their own channel. **New `components/client-delivery/send-to-client-confirm-modal.tsx`** reusable client component exported for Sprint C2-B; **NOT mounted on any panel in C2-A** (verified via `grep -rn "markReportLinkSentToClientAction\|markProposalLinkSentToClientAction\|SendToClientConfirmModal" app/ components/` returning zero matches outside the foundation modules themselves). Two-step confirm UX per `docs/29` § 12: heading + canon-verbatim disclaimer (`SEND_TO_CLIENT_DISCLAIMERS[artifactKind]`) + optional URL recopy panel with reveal-hide toggle + mandatory audience-label input + optional recipient-email input + three operator-acknowledgement checkboxes ("I copied the link" / "I delivered it through my own approved channel" / "I understand SLATE is only recording the handoff") + Confirm/Cancel pair + eligibility-blocking notice when `eligibilityReasons` contains errors + sanitized failure notice on action error. Modal is domain-agnostic — does not know report vs. proposal action details; parent threads the action via `onConfirm: (input) => Promise<SendToClientResult>` callback. Resets transient state every time the modal opens. The Confirm button stays disabled until audience-label non-empty AND all three checkboxes true AND no blocking eligibility reasons AND not pending. Raw URL is operator-clipboard only (the modal `onCopy` writes to `navigator.clipboard.writeText` only; never persisted by the modal, never reaches activity metadata). **No raw token / URL / email anywhere in the SLATE audit surface** — verified by static review of every metadata builder in both actions + the modal. **SOW Drafts are NOT eligible** — the `SendToClientArtifactKind` union excludes SOW. **Per-option `Send to Client` at `proposal-workspace.tsx:417` remains LOCKED** verbatim per `docs/29` § 18 hard binding (lock matrix unchanged: 5 `LockedActionButton` mount sites total, 3 actively rendering for persisted UUID engagements — `Send to Client` at proposal-workspace, `Prepare Report` at roadmap, `Export Report` at report for mock paths). All Sprint C2-A non-goals preserved: no `app/api/send-to-client/*` route; no email pipeline (no SendGrid / Postmark / Resend / Amazon SES / etc.); no CRM integration (no HubSpot / Salesforce / Pipedrive / Attio / Close / Affinity webhook + auth surfaces); no `mailto:` link generation; no e-signature integration (no DocuSign / PandaDoc / HelloSign / Adobe Sign / Conga / Ironclad linkage); no public SOW route; no SOW share tokens; no schema/migration (`git diff --name-only supabase/` empty); no package dependency (`git diff --name-only package.json package-lock.json` empty); no token-format / token-hash semantics change; no public report/proposal renderer modification; no AI synthesis change; no middleware / `next.config.mjs` change; no RLS policy change; no Group-B wiring (grep clean for `BenchmarkComparisonBars` / `AISavingsWaterfall` / `RoiBridge` imports outside `components/charts/` + `/app/charts-preview`); no parked-Visx-plan-file touch. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **29 app routes** (unchanged from Sprint H1). Every route's First Load JS byte-identical to Sprint H1 baseline because the modal is not yet imported by any route: `/r/[token]` 155 B / 87.4 kB; `/p/[token]` 155 B / 87.4 kB; `/app/engagements/[id]/report` 12.1 kB / 115 kB; `/app/engagements/[id]/proposal` 11.4 kB / 115 kB; `/app/engagements/[id]/proposal/sow/[snapshotId]` 187 B / 96.2 kB; `/scorecard/results` 10.7 kB / 119 kB. **Live action validation deferred to Sprint C2-B** when the modal is mounted on the report + proposal panels — Sprint P2 → P3 + P6-B → P6-C precedent (no safe operator-mediated invocation path without a UI button; auto-mode classifier blocks raw shared-prod queries). `git status --short` after the sprint: 2 source files modified (`lib/activity/types.ts` + `components/activity/activity-timeline.tsx`) + 2 new source directories (`lib/client-delivery/` containing types/policy/recipient + `components/client-delivery/` containing the modal scaffold) + 2 new lib files (`lib/reports/send-to-client-actions.ts` + `lib/proposals/send-to-client-actions.ts`) + `docs/08` + `docs/10` modified.

**Sprint C2-B — operator-mediated Send to Client workflow (2026-05-19) · landed.** Code-change sprint per `docs/29` § 17 + the Sprint C2-B prompt. **Per-token `Mark sent to client` UNLOCKED** for persisted UUID engagements on the report + proposal lanes only. The Sprint C2-A foundation is mounted: the canon-verbatim `<SendToClientConfirmModal>` opens from per-token buttons in the candidate panels and fires the cookie-bound `markReportLinkSentToClientAction` / `markProposalLinkSentToClientAction` server actions on operator confirmation. **The historic `LockedActionButton label="Send to Client"` at `proposal-workspace.tsx:417` REMAINS LOCKED verbatim** per `docs/29` § 18 hard binding — that per-option "send whole proposal" semantic doesn't map cleanly to share-token-mediated delivery; the per-token mark-sent flow is a separate surface in the share-token panel, not a replacement for the historic per-option control. **No schema, no migration, no API route, no public route, no new package dependency, no email/CRM/e-signature, no PDF/storage, no middleware / `next.config.mjs` / RLS change, no AI synthesis change, no Group-B wiring, no SOW share token, no public SOW route.** **New `components/reports/send-report-link-to-client-button.tsx`** — client component with two visual states per active share-token row: (a) audience label already on the token → primary `Mark sent to client` secondary-variant button with `Send` icon that opens `<SendToClientConfirmModal>` with `artifactKind='report'` + threaded `tokenId` + `initialAudienceLabel={trimmedAudience}` + `hasRecipientEmailHash={Boolean(token.recipientEmailHash)}` + **`shareUrlPath={null}`** (canon-correct: SLATE never stored the raw token after the mint-once copy flow, so the modal renders without the URL recopy panel — operator re-mints via the existing Generate Share Link if a fresh URL is needed); (b) audience label missing → disabled neutral chip "Mark sent disabled" + operator-facing help "Audience label required before marking sent." (matches `docs/29` § 3 + § 12 audience-label-mandatory rule by gating BEFORE the modal opens rather than rejecting at confirm time, which would surface a half-filled flow). On confirm the modal calls `markReportLinkSentToClientAction({shareTokenId, audienceConfirmation, recipientEmail, operatorConfirmed: true})`; on success the modal auto-closes after the success chip displays for 800ms and `revalidatePath('/app/engagements/{id}/report')` fires server-side. **New `components/proposals/send-proposal-link-to-client-button.tsx`** — byte-similar mirror against the proposal action with `artifactKind='proposal'`. **Both buttons mounted** beside the existing `<RevokeShareLinkButton>` / `<RevokeProposalShareLinkButton>` in the per-row action strip inside the `ShareTokenRow` server components in `components/reports/report-pdf-candidates-panel.tsx` + `components/proposals/proposal-candidates-panel.tsx`. The action strip became `<div className="flex flex-wrap items-start gap-2 pt-1">` to wrap properly when both buttons render side-by-side. Render-gate is `displayStatus === 'active'` (the existing `isPastExpiry` correction is preserved: tokens whose `expires_at` has passed but whose status is still `active` render as `expired` and therefore do NOT see the send button). **Send-history metadata display** added to both `ShareTokenRow` server components: each component now reads `metadata.sendCount` + `metadata.lastSentToClientAt` + `metadata.lastSentChannel` via three pure module-local reader helpers (`readSendCount` / `readLastSentAt` / `readLastSentChannel`) with defensive type guards (`typeof v === 'number' && Number.isFinite(v)` for the count, ISO-string + `Number.isFinite(new Date(v).getTime())` for the timestamp, `typeof v === 'string' && v.length > 0` for the channel — every reader yields a safe default rather than crashing the panel on a malformed metadata blob). When `sendCount > 0` an additional metadata strip renders inline between the existing access-metadata strip and the action strip: "Marked sent by operator N time(s)" + "Last marked <timestamp>" + "Channel: Operator-mediated copy-link" — copy deliberately uses **"Marked sent by operator" not "Email sent" / "Sent to recipient"** per `docs/29` § 1 + § 4 label policy (SLATE never sends transport). The "Channel" pill has a `title` tooltip ("SLATE recorded the operator's intent to deliver the link. SLATE did not send email or push to CRM.") so the label's meaning is unambiguous to operators new to the canon framing. **Eligibility UI** matches `docs/29` § 5 + § 11: per-token buttons render only when token `displayStatus === 'active'` (revoked + expired + past-expiry-but-still-status-active tokens all hide the button); when audience-label missing the disabled-chip variant renders inline; the action layer re-runs `evaluateReportShareEligibility` / `evaluateProposalShareEligibility` PLUS `evaluateSendToClientEligibility` at confirm time as defense-in-depth so a stale UI cannot bypass server-side gating. SOW tokens never see this button because there is no SOW share-token table and the `SendToClientArtifactKind` union at `lib/client-delivery/send-to-client-types.ts` excludes SOW intentionally. **All Sprint C2-B non-goals preserved**: no `mailto:` link generation (grep clean across the diff for `mailto:`, `nodemailer`, `@sendgrid`, `postmark`, `resend`); no DocuSign / PandaDoc / HelloSign / Adobe Sign / Conga / Ironclad linkage; no public SOW route; no SOW share tokens; no schema/migration (`git diff --name-only supabase/` empty); no package dependency (`git diff --name-only package.json package-lock.json` empty); no token-format / token-hash semantics change; no public report/proposal renderer modification; no AI synthesis change; no middleware / `next.config.mjs` change; no RLS policy change; no PDF/storage. **No raw URL / token / email in audit metadata** verified by static review: the buttons pass `shareUrlPath={null}` (SLATE never stored the raw token); the modal's `onCopy` only writes `shareUrlPath` to `navigator.clipboard.writeText` when present (always null in C2-B → button never appears anyway); the C2-A actions never include raw values in token `metadata.*` or activity event metadata; recipient email if newly provided is hashed via `hashRecipientEmail` server-side and only the SHA-256 hex hash is persisted on the token row. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **29 app routes** (unchanged from Sprint C2-A — no new route surface added). Route deltas: `/app/engagements/[id]/proposal` `11.4 kB / 115 kB` → `9.36 kB / 118 kB` (per-route size dropped because `<SendToClientConfirmModal>` is now in a shared chunk across `/report` + `/proposal`; First Load JS picked up the +3 kB shared cost); `/app/engagements/[id]/report` `12.1 kB / 115 kB` → `10 kB / 119 kB` (same shared-chunk effect); `/r/[token]` byte-identical at 155 B / 87.4 kB; `/p/[token]` byte-identical at 155 B / 87.4 kB; `/app/engagements/[id]/proposal/sow/[snapshotId]` byte-identical at 187 B / 96.2 kB; `/scorecard/results` byte-identical at 10.7 kB / 119 kB. **Live action validation deferred** to operator session against `76097653-fedb-42e5-9ef6-e89a0e97f802` — read-only SQL against shared production Supabase remains classifier-blocked per the `docs/21` / `docs/23` / `docs/25` / `docs/27` precedent. The canonical operator walkthrough now becomes the next sprint's audit step: mint a fresh report or proposal share token with audience label → click `Mark sent to client` → verify modal opens with canon-verbatim disclaimer + 3 operator-acknowledgement checkboxes + audience field pre-populated + recipient email field empty + Confirm-send-disabled-until-all-checks-true → fill checks + confirm → verify token `metadata.lastSentToClientAt` populated + `metadata.sendCount = 1` + `metadata.lastSentChannel = 'operator_mediated_copy_link'` + `report_share_token_sent_to_client` / `proposal_share_token_sent_to_client` activity event emitted with sanitized `{shareTokenId, snapshotId, reportId|proposalId, audienceLabel, hasRecipientEmailHash: boolean, sentAt, sendCount, channel}` metadata + ZERO raw URL/token/email anywhere; revoke the token → verify the `Mark sent to client` button disappears (only `displayStatus === 'active'` rows show it); attempt to mark a revoked token sent via stale UI → action returns `not-eligible` with `failureReason='token_revoked'` and emits sanitized `*_send_failed` event; the panel's send-history strip surfaces "Marked sent by operator 1 time(s)" + "Last marked <timestamp>" + "Channel: Operator-mediated copy-link" on the still-active token row after the first successful confirmation. `git status --short` after the sprint: 2 source files modified (`components/reports/report-pdf-candidates-panel.tsx` + `components/proposals/proposal-candidates-panel.tsx`) + 2 new client components (`components/reports/send-report-link-to-client-button.tsx` + `components/proposals/send-proposal-link-to-client-button.tsx`) + `docs/08` + `docs/10` modified. **No source-tree footprint outside the 4 component files + 2 docs files.**

**Send to Client MVP Acceptance Audit (2026-05-19) · Accepted with notes.** `docs/30_SEND_TO_CLIENT_MVP_ACCEPTANCE_AUDIT.md` is the formal sign-off covering Sprint C1 (commit `8320e93` canon) + C2-A (`768c92b` foundation) + C2-B (`8fb8093` operator surface + per-token unlock) together as the operator-mediated Send to Client MVP. **Read-only audit sprint** — no source code modified, no commits authored, docs-only output (`docs/30` + `docs/08` + `docs/10`). Inventory audit confirmed all 13 audit-target files present at canonical sizes: `docs/29_PHASE_1B_SEND_TO_CLIENT_CHANNEL_CANON.md` (44,650 B); 3 `lib/client-delivery/` modules (`send-to-client-types.ts` 7,600 B + `send-to-client-policy.ts` 5,770 B + `recipient.ts` 5,178 B); 2 mark-sent actions (`lib/reports/send-to-client-actions.ts` 12,315 B + `lib/proposals/send-to-client-actions.ts` 10,525 B); modal scaffold (16,940 B); 2 per-token buttons (`components/reports/send-report-link-to-client-button.tsx` 3,429 B + `components/proposals/send-proposal-link-to-client-button.tsx` 2,383 B); 2 modified panels (`report-pdf-candidates-panel.tsx` 19,204 B + `proposal-candidates-panel.tsx` 21,409 B); 2 modified activity files (`lib/activity/types.ts` 2,873 B + `components/activity/activity-timeline.tsx` 8,656 B). Canon / boundary audit clean against `docs/29` § 19 decision matrix — all 16 hard-bound items verified: Option A copy-link-only channel as a single-entry `SendToClientChannel = 'operator_mediated_copy_link'` union; SLATE does NOT send email / push CRM / open mailto / invoke send providers / track transport delivery (zero `mailto:` / `nodemailer` / `@sendgrid` / `postmark` / `resend` / `docusign` / `pandadoc` / `hellosign` / `adobe sign` / `conga` / `ironclad` / `smtp` / `salesforce` / `hubspot` / `pipedrive` references in any `lib/**/*.{ts,tsx}` or `components/**/*.{ts,tsx}`); report + proposal links only; SOW excluded at the type level (`SendToClientArtifactKind = "report" | "proposal"` — consumers cannot type-check a SOW mark-sent call without a canon amendment); audience-label mandatory; recipient-email optional + hashed-only via existing `hashRecipientEmail` SHA-256 helper; zero raw URL / token / email in audit metadata; operator-confirmation strict-check (`operatorConfirmed === true` in both actions); token expiry / revoke semantics inherited from existing actions (no token-lifecycle modification by Send to Client); top-level `Send to Client` at `proposal-workspace.tsx:417` LOCKED verbatim (UNCHANGED — verified by `Grep`). Policy + type audit clean: `evaluateSendToClientEligibility` returns ALL applicable reasons (no first-fail short-circuit; only `token_not_found` early-returns because without a token there's nothing else to evaluate); 12-entry `SendToClientEligibilityReasonCode` union covers artifact-kind / token-not-found / token-revoked / token-expired (incl. computed-expired when `expires_at` has passed even if status='active') / audience-missing / audience-too-long / recipient-email-invalid-shape / recipient-email-too-long / snapshot-voided / snapshot-ineligible / public-route-not-implemented / channel-not-authorised; `SUPPORTED_ARTIFACT_KINDS` set hard-codes `new Set(['report', 'proposal'])` as defense-in-depth at the policy layer; `SEND_TO_CLIENT_METADATA_KEYS` constant locks the three jsonb keys (`lastSentToClientAt` / `sendCount` / `lastSentChannel`); `SEND_TO_CLIENT_DISCLAIMERS` constant carries canon-verbatim `docs/29` § 13 copy for both report + proposal lanes. Recipient validation clean: `normalizeAudienceLabel` strips ASCII control chars, collapses whitespace runs, trims, caps at 80; `validateAudienceLabel` returns `audience_label_missing` / `audience_label_too_long` codes; `validateRecipientEmailForHashing` returns `{ok: true, normalizedForHashing: null}` for empty input (canon-allowed); the local validator does NOT hash — the action layer calls `hashRecipientEmail(normalizedForHashing)` exactly once and discards. Action layer audit clean for both `markReportLinkSentToClientAction` + `markProposalLinkSentToClientAction`: UUID validation → `operatorConfirmed === true` strict check → `validateAudienceLabel` (mandatory per `docs/29` § 3) → `validateRecipientEmailForHashing` (optional shape-check) → `supabase.auth.getUser()` (cookie-bound) → share-token row fetch via existing RLS-gated query → delivery-snapshot row fetch → `evaluateReportShareEligibility` / `evaluateProposalShareEligibility` AS DEFENSE-IN-DEPTH (snapshots voided since mint MUST NOT be sendable) → `evaluateSendToClientEligibility` policy gate → on failure emit `report_share_token_send_failed` / `proposal_share_token_send_failed` with sanitized `{shareTokenId, snapshotId, reportId|proposalId, failureReason}` (only the first-blocking code surfaces on the event; full reasons array stays in the action return for the UI; NEVER violation text, NEVER raw token / URL / email) → on happy path update token `metadata` jsonb with `lastSentToClientAt` ISO + `sendCount` atomic increment + `lastSentChannel='operator_mediated_copy_link'` AND update `audience_label` always + `recipient_email_hash` only on operator-edited new input (via `hashRecipientEmail` — raw email never reaches DB or activity feed) — **no schema change**: the `metadata` jsonb column already exists from migrations 0014 + 0016 — log `*_sent_to_client` activity event with sanitized `{shareTokenId, snapshotId, reportId|proposalId, audienceLabel, hasRecipientEmailHash: boolean, sentAt, sendCount, channel}` → revalidate the appropriate engagement page → return `{ok: true, status: 'sent', artifactKind, channel, tokenId, sentAt, sendCount, hasRecipientEmailHash, audienceLabel}` — no raw token / URL / email in the return either (the operator's clipboard holds the URL from the prior copy-once mint flow). Modal audit clean: two-step confirm UX (open + confirm phases); mandatory audience-label input via `audienceFilled = audienceLabel.trim().length > 0`; optional `email`-type recipient input with 254-char cap; three operator-acknowledgement checkboxes ("I copied the link" / "I delivered it through my own approved channel" / "I understand SLATE is only recording the handoff (no email / CRM / e-signature delivery)"); `confirmDisabled = pending || !audienceFilled || !allChecks || hasBlockingReason` (4-clause gate); canon-verbatim disclaimer from `SEND_TO_CLIENT_DISCLAIMERS[artifactKind]`; URL recopy panel hidden when `shareUrlPath` is null (which is the C2-B default — SLATE never stored the raw token after mint-once copy flow). Report + proposal panel audit clean for both `ShareTokenRow` server components: `Mark sent to client` button renders only inside the `displayStatus === 'active'` action strip beside the existing `RevokeShareLinkButton` / `RevokeProposalShareLinkButton`; audience-missing path → disabled neutral chip + "Audience label required before marking sent." help string; revoked + expired + computed-past-expiry tokens never see the send button (the existing `displayStatus` correction is preserved verbatim); defensive metadata readers (`readSendCount` / `readLastSentAt` / `readLastSentChannel`) with type-guard fallbacks (typeof + Number.isFinite + ISO-string check) surface the canonical Send-history strip when `metadata.sendCount > 0`: "Marked sent by operator N time(s)" + "Last marked <timestamp>" + "Channel: Operator-mediated copy-link" with `title` tooltip ("SLATE recorded the operator's intent to deliver the link. SLATE did not send email or push to CRM."); copy deliberately uses "Marked sent by operator" not "Email sent" / "Sent to recipient" per `docs/29` § 1 + § 4 label policy. No raw token / URL / email rendered anywhere. Revoke button still works (unchanged from Sprint H1). Public `/r/[token]` + `/p/[token]` route behavior unchanged (155 B / 87.4 kB byte-identical). Locked-control matrix verified: 5 `LockedActionButton` mount sites total; 3 actively rendering for persisted UUID engagements (top-level `Send to Client` at `proposal-workspace.tsx:417` + `Prepare Report` at `roadmap/page.tsx:122` + `Export Report` at `report/page.tsx:279`); zero new mounts; the per-token mark-sent buttons are NEW client components, not replacements for any `LockedActionButton`. Sprint C2-B unlocked exactly the per-token `Mark sent to client` flow on the report + proposal share-token panels. Public-route + SOW + transport absence verified by inventory: no `app/s/` directory, no `app/sow/`, no `*sow_share*` migration, no `lib/proposals/sow-share-*` modules, no `lib/email/*`, no `lib/crm/*`, no `lib/e-signature/*`, no new email/CRM/e-sign provider packages in `package.json`. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **29 app routes** (unchanged from Sprint C2-B — the audit added zero routes). Reduced-cardinality route table all byte-identical to Sprint C2-B baseline: `/app/engagements/[id]/report` 10 kB / 119 kB; `/app/engagements/[id]/proposal` 9.36 kB / 118 kB; `/r/[token]` 155 B / 87.4 kB; `/p/[token]` 155 B / 87.4 kB; `/app/engagements/[id]/proposal/sow/[snapshotId]` 187 B / 96.2 kB; `/scorecard/results` 10.7 kB / 119 kB. **Live walkthrough deferred** to operator session against `76097653-fedb-42e5-9ef6-e89a0e97f802` — read-only SQL against shared production Supabase remains classifier-blocked, mirroring `docs/21` / `docs/23` / `docs/25` / `docs/27` precedent. The audit doc's § Operator Live Walkthrough enumerates 11 canonical report-path steps + 9 canonical proposal-path steps + the top-level / SOW / transport-absence checks that the operator confirms post-acceptance; walkthrough notes template at `artifacts/walkthroughs/send-to-client-mvp-acceptance/notes.md` (gitignored) carries the same checklist + 7 canonical screenshot filenames (`report-token-mark-sent-button.png` / `report-mark-sent-modal.png` / `report-token-send-history.png` / `proposal-token-mark-sent-button.png` / `proposal-mark-sent-modal.png` / `proposal-token-send-history.png` / `locked-top-level-send-to-client.png`). **12 carry-forward items + 3 audit notes** recorded — none blocks the acceptance decision. The three audit notes: (1) operator-driven live walkthrough deferral mirrors the established `docs/21` / `docs/23` / `docs/25` / `docs/27` pattern; (2) `SEND_TO_CLIENT_DISCLAIMERS` strings live in `lib/client-delivery/send-to-client-types.ts` and are the single source of truth — a future code change that edits these strings without amending `docs/29` § 13 would silently drift the modal text from canon (recommend a CI lint or doc-test that pins the constants to the canon strings); (3) URL recopy panel intentionally hidden in C2-B because SLATE never stored the raw token after the mint-once copy flow (recommend updating `docs/29` § 12 step 1 to remove the recopy-panel expectation OR add a future affordance that stores a peppered "URL hint" the operator can decrypt locally). 9 remaining carry-forward items inherit from `docs/29` § 20 + `docs/27` audit notes: email-send canon authoring optional (`docs/31`+); per-CRM canon authoring optional (`docs/31`+); e-signature canon authoring optional (`docs/32`+); public SOW share re-opens (default still **defer** per `docs/28`); transport delivery analytics intentionally absent; optional richer share-token management dashboard; optional Send to Client event export/reporting; pattern-count documentation reconciliation from `docs/27` Audit note 1; `unsupported_surface` eligibility union member cleanup from `docs/27` Audit note 2. `git status --short` after the audit: 3 docs files modified (this `docs/10` + `docs/08` + new `docs/30`) + 1 new walkthrough notes file under gitignored `artifacts/`. **No source changes.** Sprint C1 channel canon remains accepted per `docs/29`. Sprint P7-A SOW share decision remains DEFERRED per `docs/28`. SOW Draft MVP remains Accepted with notes per `docs/27`. Proposal Review Link MVP remains Accepted with notes per `docs/25`. Client Report Link MVP remains Accepted with notes per `docs/23`. **Send to Client MVP declared complete at the operator-mediated copy-link level** pending operator-driven live walkthrough confirmation. **Recommendation: do not expand channels (email / CRM / e-signature) immediately. Run a broader Phase 1B Delivery Engine / production readiness audit first** that consolidates share-link + delivery-snapshot + SOW Draft + Send to Client work landed across Sprints 4D-B through C2-B into a single readiness verdict for client-facing usage. If the operator chooses to expand a channel first, `docs/29` § 17 post-acceptance fork authorises four independent decisions (SOW share re-open; per-CRM canon; email-send canon; e-signature canon) with `Send to Client` remaining at the current operator-mediated copy-link posture across every option.

**Phase 1B Delivery Engine Production Readiness Audit (2026-05-19) · Ready with conditions.** `docs/31_PHASE_1B_DELIVERY_ENGINE_PRODUCTION_READINESS_AUDIT.md` is the consolidating read-only audit covering the full Phase 1B Delivery Engine workstream from Sprint 4D-B through C2-B: Report Exhibit Wiring (Group-A); Report PDF Candidate (`docs/21`); Client Report Link MVP (`docs/23`); Proposal Candidate + Proposal Review Link MVP (`docs/25`); Internal SOW Draft MVP (`docs/27`); SOW Share Route Decision DEFER (`docs/28`); Production Hardening H1 (`1b2bbb6`); Send to Client Channel Canon (`docs/29`) + MVP (`docs/30`). **Docs-only sprint** — no source code modified; output is `docs/31` + `docs/08` + `docs/10`. Inventory verified: 16 canonical docs `docs/14`-`docs/30` all present at canonical sizes; 5 migrations 0012-0016 all present; 10-commit chronology matches the audit-scope range. **Milestone ledger** records 9 milestones across the workstream with canon doc + implementation commits + acceptance audit doc + outcome — all 9 either accepted-with-notes (5) or shipped (1) or canon-only (3); zero blocked. **Current approved capability** enumerated for operators (generate/revoke/mark-sent across report + proposal + SOW lanes) and for clients (open `/r/[token]` + `/p/[token]` anonymously; receive NO SLATE email/CRM/e-sign; cannot access SOW publicly; see generic-unavailable identically across every blocked state). **Explicitly not approved / still locked** documented: public SOW share route; SOW share tokens; SLATE-sent email; CRM push; e-signature; executed contracts; payment terms; final pricing workflow; Benchmark Gate 1 / Financial Gate 1 client claims; Group-B exhibits in client artifacts; automated transport delivery analytics; top-level per-option `Send to Client` at `proposal-workspace.tsx:417`. Architecture / boundary audit clean: public routes confined to `/r/[token]` + `/p/[token]` only (zero `/s/` or `/sow/` directories — verified by Glob); no `*sow_share*` migration; no `lib/{email,crm,e-signature}/` modules; zero `mailto:` / `nodemailer` / `@sendgrid` / `postmark` / `@resend` / `docusign` / `pandadoc` / `hellosign` / `adobe sign` / `conga` / `ironclad` / `smtp.` / `salesforce` / `hubspot` / `pipedrive` references across the entire source tree (verified by Grep); no PDF binary / storage dependency added across the entire Phase 1B Delivery Engine workstream; no new package dependency added; service-role lookup confined to `lib/{reports,proposals}/share-token-public.ts` + `app/{r,p}/[token]/page.tsx` + engagement title fetch + service-role activity logger; no raw token / hash / email / IP / UA in any persistence path; Group-B imports confined to `app/app/charts-preview/page.tsx` + `components/charts/exhibits/{benchmark-comparison-bars,ai-savings-waterfall,roi-bridge}.tsx` + `components/charts/README.md` (zero matches in `lib/`). Data model readiness clean: 5 migration table (0012 column add + 0013/0014/0015/0016 share-token + delivery-snapshot tables) — RLS enabled workspace-scoped `to authenticated` only (NO `to anon` policies anywhere); status CHECKs + delivery_surface CHECKs + approval_state CHECKs + pricing_review_state CHECKs present per migration; `updated_at` triggers self-contained; unique `token_hash` + SHA-256-hex shape CHECKs + `expires_at > created_at` CHECKs; `metadata` jsonb columns evolve additively across Sprint H1 (debounce signatures) + Sprint C2-A (send tracking) without schema migrations; snapshot tables preserve immutable jsonb artifact payloads; voided states preserve audit trail; no hard deletes. Public route security audit clean for both `/r` + `/p`: no app shell; no client Supabase; service-role lookup by SHA-256 hash; generic-unavailable identical-shape across all 5 blocked states (not_found / revoked / expired / snapshot_voided / snapshot_ineligible); `noindex,nofollow` + inline robots meta; `Cache-Control: no-store, max-age=0` + `X-Robots-Tag` + `Referrer-Policy: no-referrer` via `next.config.mjs`; no internal UUIDs / raw token / hash / reviewer notes / activity log / guard internals / Group-B client data in rendered DOM; access logging safe with pepper + no-pepper paths via Sprint H1 shared helper at `lib/share-tokens/access-signature.ts` (`hashesOmitted: true` flag when pepper unset); 5-min access debounce documented + implemented on both lanes. Artifact content audit clean: report client artifact carries advisory disclaimer footer + Group-B omitted + no SOW/contract language + no final financial claims; proposal client artifact carries four-denial commercial-discussion footer + pricing hidden when placeholder + no SOW/e-sign/acceptance controls + Group-B omitted; SOW Draft is internal-only with "Draft SOW · not executed" header + mandatory four-line footer + no public route + no e-signature + no final pricing while placeholder. Claim guard / commercial safety audit clean: AI synthesis 26-pattern claim guard at `lib/ai/claim-guard.ts` (FINANCIAL 14 + COMMERCIAL_FINALITY 6 + ROADMAP_COMMITMENT 6) in place; runtime commercial guard scans 45 patterns on proposal-candidate snapshots + 71 patterns on SOW Draft snapshots; known pattern-count documentation discrepancy from `docs/27` Audit note 1 (canon claims 44/70; runtime is 45/71 — one-off in `PROPOSAL_FINALITY_PATTERNS`); violation text NEVER stored in activity metadata (only `{failureReason, violationCount}`); public client views render affirmative-only safety strips; live banned-phrase mutation tests remain backlog; Benchmark Gate 1 / Financial Gate 1 still block client-facing claims. Access / revocation / expiry / Send-to-Client audit clean: H1 shared access-signature helper degrades gracefully when pepper unset; 5-min debounce via token `metadata.lastAccessSig` + `metadata.lastAccessSigAt`; dev-only `expiryMinutes` honored only when `NODE_ENV !== "production"` OR `SLATE_SHARE_TOKEN_ALLOW_DEV_EXPIRY === "true"`; report + proposal revoke flows two-step-confirm; cascade-revoke on voided proposal snapshots emits per-token events with `cascade: true`; Send-to-Client mark-sent updates `metadata.lastSentToClientAt` + `sendCount` + `lastSentChannel='operator_mediated_copy_link'`; sanitized activity events `{shareTokenId, snapshotId, reportId|proposalId, audienceLabel, hasRecipientEmailHash: boolean, sentAt, sendCount, channel}` — zero raw URL/token/email; transport delivery analytics INTENTIONALLY ABSENT per `docs/29` § 5. Locked-control matrix verified at **5 `LockedActionButton` mount sites** (canonical, unchanged): `proposal-workspace.tsx:410` `Prepare SOW Draft` (mock-only) + `proposal-workspace.tsx:416` `Send to Client` (per-option top-level, always LOCKED per `docs/29` § 18 hard binding) + `roadmap/page.tsx:122` `Prepare Report` + `report/page.tsx:279` `Export Report` mock + `proposal/page.tsx:143` `Prepare Client Review` mock-only; per-token `Mark sent to client` UNLOCKED on report + proposal panels (Sprint C2-B). Operator live walkthrough status: **deferred** across all four lanes (report link, proposal link, SOW Draft, Send to Client) per the established `docs/21` / `23` / `25` / `27` / `30` Phase 1B precedent — auto-mode classifier blocks shared-prod queries; walkthrough notes templates pre-staged in 4 gitignored `artifacts/walkthroughs/*/notes.md` files with canonical step-by-step + screenshot filenames. **This is accepted-with-notes status, not full production certification.** Production environment checklist documented: `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` + `SUPABASE_SERVICE_ROLE_KEY` required; `OPENAI_API_KEY` optional for AI drafting; migrations 0012-0016 verified applied; `SLATE_SHARE_TOKEN_ACCESS_PEPPER` RECOMMENDED before production (without it the audit-log fingerprints degrade to `hashesOmitted: true` per H1 safe-degrade); `SLATE_SHARE_TOKEN_ALLOW_DEV_EXPIRY` NOT required (dev-only); email/CRM/e-sign secrets NOT required (no such surface exists); RLS verified `to authenticated`-only in deployed Supabase; no public SOW route deployed. **12-item pre-client checklist** enumerated: (1) staging walkthrough report link path; (2) staging walkthrough proposal link path; (3) staging walkthrough SOW internal draft path; (4) Send to Client mark-sent walkthrough; (5) `SLATE_SHARE_TOKEN_ACCESS_PEPPER` configured; (6) migrations 0012-0016 applied; (7) NO `to anon` policies on share-token tables; (8) no raw email/token/URL in activity logs; (9) public `/r` and `/p` security headers via curl; (10) revoke/expired/generic-unavailable behavior identical-shape; (11) top-level `Send to Client` at `proposal-workspace.tsx:417` remains LOCKED; (12) public SOW route absent (`curl /s/test` + `curl /sow/test` return 404). **16-item risk register** recorded with severity + mitigation + remaining action; **zero blocking risks** — every recorded risk is canon-aligned-by-design, addressed by existing mitigation, or carries a documented future cleanup (live walkthrough deferral / pepper not configured / disclaimers drift / pattern-count doc discrepancy / `unsupported_surface` cleanup / URL recopy panel expectation drift / operator misuse of copy-link / public-route fallback title / access-count under-count / lack of transport analytics / no email/CRM by design / no e-sig by design / final pricing workflow absent / Benchmark+Financial gates not advanced / Group-B excluded / SOW share deferred). **Acceptance verdict: READY WITH CONDITIONS** — Phase 1B Delivery Engine approved for **controlled client use** under 5 conditions: (1) complete operator staging walkthrough across all four lanes; (2) configure `SLATE_SHARE_TOKEN_ACCESS_PEPPER` in production; (3) verify migrations 0012-0016 applied + RLS posture in target Supabase (workspace-scoped `to authenticated`-only; NO `to anon`); (4) review canon-verbatim disclaimer copy in `lib/client-delivery/send-to-client-types.ts` against `docs/29` § 13 (string-pinning recommendation from `docs/30` Audit note 2); (5) keep SOW public sharing + all alternative transports locked until corresponding canons are authored. `npm run lint` clean; `NEXT_TELEMETRY_DISABLED=1 npm run build` clean. **29 app routes** (unchanged from Sprint C2-B — the audit added zero routes). Every route's First Load JS byte-identical to Sprint C2-B baseline. `git status --short` after the audit: 3 docs files modified (this `docs/10` + `docs/08` + new `docs/31`); zero source files touched. Send to Client MVP remains Accepted with notes per `docs/30`. SOW Share Route Decision remains DEFERRED per `docs/28`. SOW Draft MVP remains Accepted with notes per `docs/27`. Proposal Review Link MVP remains Accepted with notes per `docs/25`. Client Report Link MVP remains Accepted with notes per `docs/23`.

**Phase 1B Delivery Engine Staging Walkthrough sign-off scaffold (2026-05-19) · static verification complete · operator-driven staging walkthrough pending.** `docs/32_PHASE_1B_DELIVERY_ENGINE_STAGING_WALKTHROUGH.md` is the canonical sign-off surface executing the 12 readiness conditions from `docs/31` § Required Pre-Client Checklist + 5 conditions from `docs/31` § Acceptance Decision. **Honest scope-bounded outcome:** the audit agent cannot run live SQL or `curl` against a deployed staging environment under the auto-mode classifier (same precedent as `docs/21` / `docs/23` / `docs/25` / `docs/27` / `docs/30` Phase 1B audits); this sprint produces the **sign-off scaffold** with **5 of 12 conditions statically verified** + **7 of 12 reserved for operator confirmation slots** that the operator fills inline post-staging-execution. Docs-only sprint — no source code modified; output is `docs/32` + `docs/08` + `docs/10`. **This-session environment captured in `docs/32` § 0**: head commit `05146f5` (Add Phase 1B delivery engine readiness audit); branch `persistence/step-0-1-auth-shell`; working tree clean except gitignored `.claude/` + `artifacts/`; `npm run lint` ✅ no warnings/errors; `NEXT_TELEMETRY_DISABLED=1 npm run build` ✅ compiled successfully + 14/14 static pages; 29 app routes (unchanged from Sprint C2-B baseline); reduced-cardinality route table all byte-identical to baseline (`/r/[token]` 155 B / 87.4 kB; `/p/[token]` 155 B / 87.4 kB; `/app/engagements/[id]/report` 10 kB / 119 kB; `/app/engagements/[id]/proposal` 9.36 kB / 118 kB; `/app/engagements/[id]/proposal/sow/[snapshotId]` 187 B / 96.2 kB; `/scorecard/results` 10.7 kB / 119 kB). **Operator-filled environment slot reserved in `docs/32`** for: staging host URL; staging Supabase project ref (recommended a branch/fork of `hhglrcvsmwaheikdvijw`, NOT production); deployed commit SHA (should match a remote build of `persistence/step-0-1-auth-shell` ≥ `05146f5`); browser used for client-side walkthrough; date/time walkthrough completed; `NODE_ENV` value (must be `production` for the deployed staging build). **Statically verified (✅) conditions this session**: (7) NO `to anon` policies on `report_share_tokens` + `proposal_share_tokens` — migrations 0014 + 0016 declare workspace-scoped `to authenticated` only; verified by source grep; (8) no raw email/token/URL in activity-log metadata builders — code-path review across `markReportLinkSentToClientAction` + `markProposalLinkSentToClientAction` + every `*_share_token_*` emitter confirms only sanitized `{shareTokenId, snapshotId, reportId|proposalId, audienceLabel, hasRecipientEmailHash: boolean, sentAt, sendCount, channel}` shape; (11) top-level `Send to Client` at `proposal-workspace.tsx:417` remains LOCKED — `Grep` confirms `LockedActionButton label="Send to Client"` unchanged + the 5-mount-site canonical inventory holds (proposal-workspace:410 Prepare SOW Draft mock-only; proposal-workspace:416 Send to Client per-option always locked; roadmap:122 Prepare Report; report:279 Export Report mock; proposal/page:143 Prepare Client Review mock-only); (12) public SOW route absent — `Glob app/s/**` + `Glob app/sow/**` both return zero files; no `*sow_share*` migration. **Operator-pending (⏸) conditions for staging execution**: (1) report link path walkthrough — mint → optional audience/recipient → visit `/r/<token>` → revoke → re-visit → generic unavailable; (2) proposal link path walkthrough — mint → approve candidate → visit `/p/<token>` → revoke → cascade-revoke on snapshot void; (3) internal SOW Draft walkthrough — approve Proposal Candidate → Generate SOW Draft → open internal route → verify canon-mandated chrome (operator banner / "Draft SOW · not executed" H1 / body note / safety strip / draft watermark / pricing notice / legal-boundary notice / scope+deliverables / responsibilities / per-option detail / Group-B omission / four-line footer); (4) Send to Client mark-sent walkthrough — per-token `Mark sent to client` button → modal with audience + 3 acknowledgement checkboxes → confirm → verify `metadata.lastSentToClientAt` + `sendCount=1` + `lastSentChannel='operator_mediated_copy_link'` + sanitized `*_sent_to_client` event; (5) `SLATE_SHARE_TOKEN_ACCESS_PEPPER` configured in staging env; (6) migrations 0012-0016 applied in staging Supabase; (9) public `/r` and `/p` security headers via `curl` against staging host — expected `Cache-Control: no-store, max-age=0`; `X-Robots-Tag: noindex, nofollow`; `Referrer-Policy: no-referrer`; (10) revoke / expired / generic-unavailable behavior identical-shape across all 5 blocked states. **`docs/32` § 1 12-row pass/fail table** is the canonical sign-off surface — operator updates each row with ✅ / ❌ / accepted-exception inline + pastes curl output / JSON event payload below relevant rows + chooses the final verdict from the three documented options. **22 canonical screenshot/text-artifact filenames pre-staged** across 4 gitignored `artifacts/walkthroughs/*/` directories: report lane (6 captures); proposal lane (6); SOW Draft lane (4); Send to Client lane (6). **Remaining blockers documented in `docs/32` § 3**: source-tree blockers NONE; walkthrough blockers operator-side (auto-mode classifier + no configured staging environment identified at sprint-author time — operator either provisions a non-production deployment of `persistence/step-0-1-auth-shell` with pepper + migrations applied OR runs the walkthrough against `npm run dev` pointed at a non-production Supabase project). **`docs/32` operator sign-off section (§ 5)** is empty until the operator runs the walkthrough; the doc remains open across operator + (optional) reviewer sign-offs. **Three documented final-verdict options** after operator execution: ✅ Staging cleared for controlled client use; ⚠️ Cleared with operator-tracked exceptions (1-2 ⏸ items unresolved but non-blocking); ❌ Not cleared (any of conditions 1, 2, 3, 4, 7, 11, 12 fails → follow-up sprint to fix). **No source code changes.** `npm run lint` + `npm run build` both clean. **29 app routes** (unchanged). `git status --short` after the sprint: 3 docs files (this `docs/10` + `docs/08` + new `docs/32`) + gitignored `.claude/` + `artifacts/`; zero source files touched.

**Phase 1B Delivery Engine Staging Walkthrough EXECUTION-PASS (2026-05-19) · ⚠️ Cleared with operator-tracked exceptions.** `docs/32` updated inline with this-session execution evidence: 8/12 readiness conditions verified (5 static + 3 LIVE via `npm run dev` + curl); 1/12 caveated (pepper not in local env — canon-allowed safe-degrade); 4/12 remain operator-pending (the four UI-mediated walkthrough lanes). **Docs-only sprint** — no source code modified; output is `docs/32` updated + this `docs/08` + `docs/10`. **Precondition resolved**: scaffold + status docs committed at `496230f` ("Add Phase 1B staging walkthrough signoff") + pushed (`05146f5..496230f`) before execution began. **Live execution this session** (against branch head + `npm run dev` in `development` mode + local Supabase config from existing `.env.local`): condition 9 ✅ — `curl -I http://localhost:3000/r/invalid-noop-token` + `/p/invalid-noop-token` both returned `HTTP/1.1 200 OK` + `Cache-Control: no-store, must-revalidate` + `X-Robots-Tag: noindex, nofollow` + `Referrer-Policy: no-referrer`; condition 10 PARTIAL ✅ — unknown-token generic-unavailable shape confirmed live (9226 B report + 9536 B proposal; identical "unavailable" + "Contact the sender" body markers; ~310 B lane delta = footer copy difference, NOT per-state diff); revoked/expired/snapshot-voided states still operator-pending because they require minted tokens; condition 12 ✅ — `curl -sI http://localhost:3000/s/test` + `/sow/test` both `HTTP/1.1 404 Not Found` with body markers `404` + `This page could not`; public SOW route absent verified live. **Static (🔒) re-verification this session**: condition 6 source-side (`stat` confirms migrations `0012_report_section_exhibit_slot.sql` + `0013_report_delivery_snapshots.sql` + `0014_report_share_tokens.sql` + `0015_proposal_delivery_snapshots.sql` + `0016_proposal_share_tokens.sql` all present at canonical sizes — deployed-state verification deferred); condition 7 (source migrations 0014 + 0016 declare workspace-scoped `to authenticated` only, NO `to anon`); condition 8 (action metadata builders carry only sanitized fields); condition 11 (`<LockedActionButton label="Send to Client">` at `proposal-workspace.tsx:417` unchanged); condition 12 (`Glob app/s/**` + `app/sow/**` both empty; no `*sow_share*` migration). **This-session discovery**: Next.js App Router echoes the URL dynamic-route param ONCE into the RSC streamed payload (`["token","<value>","d"]` + `urlParts:["","r","<value>"]` + `initialTree`) — framework-expected hydration behavior, NOT a SLATE-side audit-log leak. Canon's "no raw token in audit logs" rule preserved: not persisted in DB (only SHA-256 hash); not in any server-side activity feed; `Referrer-Policy: no-referrer` prevents URL leakage via referrer chains. **Recommended `docs/30` follow-up**: add Audit Note 4 documenting the framework-level RSC URL-echo expected occurrence count (= 1 per dynamic-route render). **Condition 5 caveat (🟡)**: local `.env.local` grep returns 0 matches for `SLATE_SHARE_TOKEN_ACCESS_PEPPER`; H1 helper's safe-degrade engaged → `hashesOmitted: true` on activity events; canon-allowed for non-prod; operator MUST configure in deployed staging before production. **Conditions deferred to operator session (⏸)**: condition 1 (report link UI walkthrough: mint → visit → revoke); condition 2 (proposal link UI walkthrough); condition 3 (internal SOW Draft walkthrough: approve Proposal Candidate → Generate SOW Draft → open route → verify canon chrome); condition 4 (Send to Client mark-sent UI walkthrough: per-token modal → 3 acknowledgement checkboxes → confirm → verify metadata + sanitized event). **`mcp__supabase__list_migrations` + `list_tables` against `hhglrcvsmwaheikdvijw`** (production) classifier-blocked under the staging-sprint boundary ("Querying the production Supabase project violates the user's explicit staging-sprint boundary requiring a non-production project"); deployed-state verification of conditions 6 (migrations applied) + 7 (deployed RLS posture) deferred to operator session against non-prod Supabase project. **Operator path to clear all 4 ⏸ items + the pepper caveat documented in `docs/32` § 3**: provision non-prod Supabase project (fork/branch prod OR fresh project + apply migrations 0012-0016); set `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` + `SUPABASE_SERVICE_ROLE_KEY` + `SLATE_SHARE_TOKEN_ACCESS_PEPPER` in the staging env; provision operator `/login` session; run 4 lane walkthroughs against `76097653-fedb-42e5-9ef6-e89a0e97f802` (or staging-equivalent engagement); capture 22 canonical screenshots into 4 gitignored `artifacts/walkthroughs/*/` directories; update `docs/32` § 1 rows 1-4 inline + § 5 sign-off block. **`docs/32` § 5 records 6 exceptions** (conditions 1-4 deferred + condition 5 caveat + condition 6 partial); zero blocking defects in source-tree / lint / build / public-route security / generic-unavailable shape / SOW absence; operator session that clears items 1-4 + configures pepper + verifies deployed migrations promotes verdict to ✅ Staging cleared for controlled client use. **No source code changes. `git status --short` after this session**: 3 docs files modified (this `docs/10` + `docs/08` + updated `docs/32`); zero source files touched.

Next planned: **Operator-driven completion of `docs/32` UI walkthrough portion + pepper configuration in deployed staging.** Operator provisions a non-production deployment of `persistence/step-0-1-auth-shell` (≥ commit `05146f5`) with `SLATE_SHARE_TOKEN_ACCESS_PEPPER` configured + migrations 0012-0016 applied + a non-production Supabase project; runs the 7 ⏸ items in `docs/32` § 1 across the four delivery lanes (report link / proposal link / internal SOW Draft / Send to Client mark-sent); captures the 22 canonical screenshots + curl outputs + sanitized event payloads into the four `artifacts/walkthroughs/*/` directories; updates `docs/32` § 1 pass/fail table inline + § 5 sign-off block; lands the final verdict (✅ Staging cleared / ⚠️ Cleared with exceptions / ❌ Not cleared). **After sign-off lands**, the next eligible sprint per `docs/31` § Recommended Next Milestone alternatives is operator choice between: Option B — UX polish / operator guidance sprint (in-product mark-sent guidance + recipient-hash visual + canon-verbatim disclaimer CI pin from `docs/30` Audit note 2 + Send-history chip styling polish + modal copy review against `docs/29` § 13); OR Option C — CRM / email / e-sign / SOW share canon authoring (four independent canons per `docs/29` § 17 post-acceptance fork; recommended slot `docs/33`+; SOW share decision re-opens with default still **defer** per `docs/28`; per-CRM canon at `docs/33`+; email-send canon at `docs/33`+; e-signature canon at `docs/34`+). **`Send to Client` remains at operator-mediated copy-link posture** across every option until a separate canon explicitly authorises an alternative transport. **`docs/32` sign-off does NOT unlock SOW public sharing OR any alternative transport** — it certifies the EXISTING operator-mediated delivery surface as production-ready under controlled client use, not as a green-light for channel expansion. The recommended audit scope covers: (a) end-to-end review of the operator-mediated client-share path (report `/r/[token]` + proposal `/p/[token]` mint → audience-label-required Send to Client mark-sent → revoke → re-mint cycle) against the original Phase 1B delivery canon (`docs/19` + `docs/22` + `docs/24` + `docs/29`); (b) delivery-snapshot durability audit across `report_delivery_snapshots` + `proposal_delivery_snapshots` jsonb shapes versus the snapshot-pure rendering contract in the public renderers; (c) commercial-guard pattern math reconciliation (pattern-count documentation reconciliation from `docs/27` Audit note 1 — runtime 45/71 vs canon-documented 44/70 — folded into the broader audit's recommended fixes); (d) `unsupported_surface` eligibility union member cleanup (`docs/27` Audit note 2); (e) SOW Draft snapshot internal-route review against the `docs/26` § Required Markings / Disclaimers canon now that the SOW lane has been live in operator-internal mode for one sprint cycle; (f) Send to Client modal canon-verbatim disclaimer pinning recommendation (audit note 2 from `docs/30`); (g) URL recopy panel reconciliation (audit note 3 from `docs/30` — update `docs/29` § 12 step 1 OR author a future affordance); (h) production hardening backlog from `docs/23` + `docs/25` remaining items (pepper config, access-log debounce, dev-only short-expiry, audience-label / recipient-email UI for raw mint, revoke-from-panel UI — all of which actually landed in Sprint H1 but warrant a consolidated re-confirmation under the audit); (i) operator-driven live walkthrough confirmation for `docs/30` § Operator Live Walkthrough; (j) cross-canon consistency check (`docs/22` / `docs/24` / `docs/26` / `docs/28` / `docs/29` all reference each other; any drift since they were authored is worth catching at this audit). **`Send to Client` remains at operator-mediated copy-link posture** until a separate canon explicitly authorises an alternative transport. **If the operator instead chooses to expand a channel** before the production readiness audit, `docs/29` § 17 post-acceptance fork authorises four independent decisions: (1) SOW share re-open per `docs/28` (recommended default still **defer**); (2) per-CRM canon authoring (one per CRM, recommended slot `docs/31`+); (3) email-send canon authoring (recommended slot `docs/31`+); (4) e-signature canon authoring (recommended slot `docs/32`+). Each is operator-priority-dependent; none has a hard dependency on the others. Read-only acceptance audit mirroring `docs/23` (Client Report Link MVP) + `docs/25` (Proposal Review Link MVP) + `docs/27` (SOW Draft MVP) shape, covering Sprint C1 (canon) + C2-A (foundation) + C2-B (operator surface + per-token unlock) together as the first operator-mediated Send to Client surface. Read-only audit sprint — no source code modified, no commits authored by the audit, docs-only output (audit doc + `docs/08` + `docs/10`). Audit checklist: (1) inventory audit of all C2-A + C2-B files at canonical sizes; (2) channel-canon compliance audit against `docs/29` § 2-§ 16; (3) action-layer audit (sanitized metadata only — `{shareTokenId, snapshotId, reportId|proposalId, audienceLabel, hasRecipientEmailHash: boolean, sentAt, sendCount, channel}` for success events + `{shareTokenId, snapshotId, reportId|proposalId, failureReason}` for failure events; zero raw URL/token/email); (4) metadata-key audit (token row's `metadata` jsonb gains `lastSentToClientAt` + `sendCount` + `lastSentChannel` only; no raw values); (5) modal copy + canon-verbatim disclaimer audit against `docs/29` § 13 (both `SEND_TO_CLIENT_DISCLAIMERS.report` and `SEND_TO_CLIENT_DISCLAIMERS.proposal` strings present byte-identical); (6) locked-control matrix audit (per-token Send to Client UNLOCKED for report + proposal, `proposal-workspace.tsx:417` per-option LOCKED, `Prepare Report` LOCKED, `Export Report` mock LOCKED, `Prepare SOW Draft` mock LOCKED unlocked-for-persisted, `Prepare Client Review` mock LOCKED unlocked-for-persisted — 5 `LockedActionButton` mount sites unchanged); (7) SOW-ineligible audit (the `SendToClientArtifactKind` union excludes SOW; SOW Drafts have no share token and cannot reach this surface); (8) operator-driven live walkthrough (mint → mark sent → verify metadata + audit event + audience label + recipient hash presence → revoke → verify button absence + failure event with `token_expired` / `token_revoked` reason → re-mark on still-active token → verify `sendCount=2` + second `*_sent_to_client` event); (9) 12 carry-forward items from `docs/29` § 20 open-decisions check; (10) `docs/29` § 17 Proposed Sprint Sequence verification — Sprint C2-A foundation + C2-B implementation completed in the canonical order. After the audit accepts, the post-acceptance fork opens per `docs/29` § 17: (a) `docs/28` SOW share decision becomes re-eligible — Sprint P7-B implementation IF approved (recommended default: stays deferred until at least one of the parallel canons below lands); (b) per-CRM canon authoring becomes eligible (one per CRM, recommended `docs/31`+) — each CRM gets its own decision because each has its own auth + data-shape posture; (c) email-send canon becomes eligible (recommended `docs/31`+) — would authorise SLATE-sent email with provider choice + sending domain + DKIM key management + bounce-handling + message template authoring + the compliance envelope (SPF/DKIM/DMARC alignment, CAN-SPAM / CASL / GDPR jurisdictional rules, RFC 8058 One-Click Unsubscribe headers, spam-complaint feedback loops); (d) e-signature canon becomes eligible (recommended `docs/32`+) — would authorise a hosted-signature path with explicit legal model + audit trail + storage model + vendor selection + post-signature workflow. Operator chooses which direction to pursue next; the four are independent decisions with `Send to Client` remaining at its current operator-mediated copy-link posture across every option. SOW Draft MVP remains Accepted with notes per `docs/27`. Proposal Review Link MVP remains Accepted with notes per `docs/25`. Client Report Link MVP remains Accepted with notes per `docs/23`. SOW Share Route Decision remains DEFERRED per `docs/28`. **Sprint C2-B authorizes only the per-token `Mark sent to client` UI mount + Send-history metadata display — not any top-level `Send to Client` unlock, not any SOW path expansion, not any email/CRM/e-sign authoring.** Sprint C2-B imports `<SendToClientConfirmModal>` into `components/reports/report-pdf-candidates-panel.tsx` + `components/proposals/proposal-candidates-panel.tsx`; renders a per-row `Send to Client` button beside each active share-token row (mirroring the existing per-row `RevokeShareLinkButton` + `RevokeProposalShareLinkButton` placement from Sprint H1); wires the modal's `onConfirm` callback to either `markReportLinkSentToClientAction` or `markProposalLinkSentToClientAction`; passes through the active share-token's `id` + initial audience label + recipient-hash-present flag + the operator-side share URL path (constructed client-side as `/r/<rawToken>` or `/p/<rawToken>` — the raw token lives in the operator's clipboard from the prior mint, not in the DB); surfaces a Send-history chip on each row when `metadata.sendCount > 0` showing `"Sent N times"` (canon § 20 open-decision item 4, recommended yes per `docs/29`); surfaces `metadata.lastSentToClientAt` alongside the existing access count on each row (canon § 20 open-decision item 8, recommended yes). **At the END of Sprint C2-B: per-token `Send to Client` UNLOCKED** for persisted UUID engagements on the report + proposal lanes only. The proposal-workspace per-option `Send to Client` `LockedActionButton` at line 417 REMAINS LOCKED in C2-B (`docs/29` § 18 hard binding — that per-option semantic doesn't map cleanly to share-token-mediated delivery; a future canon may re-author at recommended slot `docs/32`+). After C2-B is accepted, `docs/30 — Send to Client MVP Acceptance Audit` per `docs/29` § 17 sequence binding runs the canonical operator walkthrough (mint a token → confirm Send to Client → verify audit event + counter + audience label + recipient hash presence → revoke → verify failure-event-then-no-double-revoke → re-confirm + verify second `sent_to_client` event → expire-and-re-confirm → verify failure event with `token_expired` reason). After the audit accepts, the post-acceptance fork opens (`docs/29` § 17): `docs/28` SOW share decision becomes re-eligible (Sprint P7-B implementation IF approved); per-CRM canon authoring becomes eligible (one per CRM, recommended `docs/31`+); email-send canon becomes eligible (recommended `docs/31`+); e-signature canon becomes eligible (recommended `docs/32`+). Operator chooses which direction to pursue next. **Sprint C2-A authorizes only the foundation modules + activity event types + modal scaffold — not any UI mount, not any Send to Client unlock, not any SOW path expansion.**

_(The historic Sprint P7-A next-planned forecast paragraph that previously occupied this slot has been removed because Sprint P7-A landed at `73c9459` and Sprint C1 channel canon landed at `8320e93`. The chosen path is the **defer** branch in the historic forecast: SOW share remains deferred per `docs/28`; the Send to Client channel canon `docs/29` is accepted; the next sprint is Sprint C2-B implementation. Pre-P7-A planning context for earlier sprints continues below for older-sprint readers.)_

_The Sprint P6-C planning paragraph that previously occupied this slot has been removed because Sprint P6-C is now complete; the planning detail it carried is captured verbatim in the Sprint P6-C landed-paragraph two entries above. Pre-P6-C planning context (Sprint 4C-B / 4D / report-side acceptance + polish backlog) continues below for older-sprint readers._

The earlier next planned text continues below for context: **Sprint 4C-B — client-safe report PDF candidate implementation** per `docs/20` § Sprint 4C Implementation Plan, if and when the operator approves the nine open decisions enumerated in `docs/20`. Sprint 4C-B is the recommended single-sprint implementation path when Option A is approved: migration `0013_report_delivery_snapshots.sql` + snapshot types/queries/actions + R2 readiness helper + export-time claim-guard helper + operator-only `/report/pdf-candidate` route + `Generate PDF Candidate` button replacing `LockedActionButton label="Export Report"` (`Send to Client` / `Prepare SOW Draft` / `Prepare Client Review` / `Prepare Report` stay locked verbatim) + the four new activity event types. If Option B is preferred or any open decision is deferred, split into Sprint 4C-B dependency canon (`docs/21`) → Sprint 4C-C schema + R2 (code, no PDF) → Sprint 4C-D generation action + artifact route. Sprint 4D (controlled client-facing delivery / share) and the separate proposal-delivery canon follow per `docs/19` § Proposed Sprint Sequence. Group-B exhibits stay confined to `/app/charts-preview` until `docs/14` / `docs/15` advance. The Visx plan file (`~/.claude/plans/which-charts-library-is-replicated-adleman.md`) remains parked. E-signature, CRM integration, and BuildOps remain explicit non-goals at the current boundary.

---

## Repo Layout

```
app/
  layout.tsx               # Root layout, fonts, viewport, metadata
  page.tsx                 # Redirect → /app
  app/
    layout.tsx             # AppShell wrapper
    page.tsx               # Command Center dashboard
    leads/
      page.tsx             # Lead inbox (server)
      [id]/page.tsx        # Lead detail (SSG via generateStaticParams)
    engagements/
      page.tsx             # Engagement list (server)
      [id]/page.tsx        # Engagement command center (SSG)
      [id]/intake/page.tsx       # Stakeholder intake manager (SSG, Sprint 5)
      [id]/findings/page.tsx     # Findings review workspace (SSG, Sprint 5)
      [id]/opportunities/page.tsx  # Opportunity matrix workspace (SSG, Sprint 6)
      [id]/roadmap/page.tsx        # 30/60/90 roadmap (SSG, Sprint 6)
      [id]/report/page.tsx         # Report builder (SSG, Sprint 7)
      [id]/proposal/page.tsx       # Proposal & SOW options (SSG, Sprint 7)
  apply/
    ai-systems-review/
      page.tsx             # Premium "Application coming soon" stub
  scorecard/
    layout.tsx             # Public metadata only
    page.tsx               # /scorecard landing
    start/page.tsx         # /scorecard/start (renders ScorecardStepper)
    results/page.tsx       # /scorecard/results (renders ScorecardResultsView)
components/
  layout/                  # Internal app shell (Sprint 1)
    app-shell.tsx
    sidebar-nav.tsx
    top-bar.tsx
    page-header.tsx
  ui/
    button.tsx             # Variants: primary | secondary | outline | ghost
    card.tsx               # Card + Header/Title/Description/Body/Footer
    badge.tsx              # 10 tones, soft/outline, optional dot
    metric-card.tsx
    empty-state.tsx
    input.tsx              # Text/email input primitive (added in Sprint 2)
    locked-action-button.tsx  # Shared locked-CTA primitive (Sprint 7)
  slate/                   # Internal /app composition
    review-queue.tsx
    active-engagements-panel.tsx
    recent-activity-panel.tsx
  leads/                   # Lead dashboard + detail (Sprint 3)
    lead-status-chip.tsx
    fit-score-badge.tsx
    lead-filter-tabs.tsx
    lead-list.tsx
    lead-list-item.tsx
    lead-profile-header.tsx
    scorecard-summary-panel.tsx
    internal-fit-score-panel.tsx
    qualification-signals-panel.tsx
    recommended-action-card.tsx
    lead-actions-panel.tsx        # Now links to seeded engagement when present
    lead-source-card.tsx
    lead-notes-panel.tsx
  intake/                  # Stakeholder intake manager (Sprint 5)
    role-coverage-map.tsx
    stakeholder-list.tsx
    supporting-inputs-panel.tsx
    follow-up-queue.tsx
  findings/                # Findings review workspace (Sprint 5)
    finding-status-chip.tsx
    confidence-indicator.tsx
    evidence-panel.tsx
    findings-workspace.tsx       # Client orchestrator (3-pane on lg+)
  opportunities/           # Opportunity matrix (Sprint 6)
    opportunity-priority-chip.tsx
    opportunity-card.tsx
    opportunity-matrix.tsx
    opportunity-score-strip.tsx
    related-findings-panel.tsx
    opportunities-workspace.tsx  # Client orchestrator (matrix + list + detail + evidence)
  roadmap/                 # 30/60/90 roadmap (Sprint 6)
    roadmap-card.tsx
    roadmap-phase-column.tsx
  reports/                 # Report builder (Sprint 7)
    report-status-chip.tsx
    report-workspace.tsx         # Client orchestrator (3-pane: outline / preview / linked)
  proposals/               # Proposal & SOW options (Sprint 7)
    proposal-status-chip.tsx
    proposal-workspace.tsx       # Client orchestrator (option cards + selected detail)
    implementation-credit-panel.tsx
  engagements/             # Engagement workspace (Sprint 4)
    engagement-status-chip.tsx
    engagement-stage-chip.tsx
    engagement-stage-tracker.tsx  # Horizontal on lg, vertical below
    engagement-list.tsx
    engagement-list-item.tsx
    engagement-profile-header.tsx
    engagement-status-panel.tsx   # Generic shell used by 6 specific panels
    stakeholder-progress-panel.tsx
    document-status-panel.tsx
    findings-status-panel.tsx
    opportunity-status-panel.tsx
    report-status-panel.tsx
    proposal-status-panel.tsx
    engagement-context-card.tsx
    engagement-risks-panel.tsx
    engagement-recommended-action-card.tsx
    engagement-notes-panel.tsx
  scorecard/               # Public scorecard composition
    public-assessment-shell.tsx
    scorecard-hero.tsx     # Landing hero + result preview
    scorecard-value-props.tsx
    scorecard-boundary-card.tsx
    scorecard-progress.tsx
    scorecard-question-card.tsx (single/multi/scale/text controls)
    scorecard-stepper.tsx  # Client orchestrator: state, persistence, validation
    scorecard-results-view.tsx
    scorecard-result-hero.tsx
    score-card.tsx         # Per-dimension score with band + bar
    opportunity-area-card.tsx
    risk-readiness-note.tsx
    recommended-next-step-card.tsx
lib/
  mock-data.ts             # Internal /app mock data
  utils.ts                 # cn() helper
  scorecard/
    types.ts               # Sections, Question types, ScoreResult, etc.
    questions.ts           # 19-question bank with dimension weights
    scoring.ts             # Mock scoring + classification + opportunity match
    storage.ts             # localStorage helpers (slate.scorecard.v1) — now also stores startedAt
    public-result.ts       # Step 2: PublicScoreResult Omit<…, "fit"> + allowlist
    email-quality.ts       # Step 4.5: classifyEmailQuality + disposable/free/fake blocklists
  leads/
    types.ts               # Lead, LeadStatus, FitDimension, QualificationSignal
    helpers.ts             # fitCategoryFor, status labels/tones, filter set
    queries.ts             # server-only: getAllLeads, getLeadById (Step 3)
    mappers.ts             # DB ↔ TS shape translators + formatRelative (Step 3)
    derive.ts              # Step 2 fit-dimension/signal/status derivations
  engagements/
    types.ts               # Engagement + 6 panel-status types, ScorecardSnapshot
    helpers.ts             # STAGES, STAGE_LABEL/DESCRIPTION, STATUS labels/tones, filters
    queries.ts             # server-only: getAllEngagements / getEngagementById / getEngagementIdForLead (Step 4)
    mappers.ts             # DB ↔ TS shape translators + safe-default panel JSON parsers (Step 4)
    actions.ts             # createOrOpenEngagementForLead server action (Step 4)
    load-for-subroute.ts   # mock-fixture-first loader used by downstream sub-routes (Step 4)
    mock-engagements.ts    # Fixtures for legacy slug-keyed demo paths only (Steps 5–8 retire piece by piece)
    recommended-action.ts  # Shared routing helper (Sprint 6)
  opportunities/           # Opportunity scoring (Sprint 6)
    types.ts               # Opportunity, priorities, quadrants, evidence strength
    helpers.ts             # priority/quadrant/evidence labels and tones, computeQuadrant
    mock-opportunities.ts  # Seeded opportunities for Quanta + Caldera
  roadmap/                 # 30/60/90 sequencing (Sprint 6)
    types.ts               # RoadmapItem, RoadmapPhase
    helpers.ts             # PHASE_ORDER, PHASE_LABEL, PHASE_DESCRIPTION
    mock-roadmap.ts        # Seeded 30/60/90 items for Quanta + Caldera
  reports/                 # Report assembly (Sprint 7)
    types.ts               # Report, ReportSection, statuses, confidence
    helpers.ts             # SECTION_ORDER/LABEL, status labels/tones, filters
    mock-reports.ts        # Seeded reports for Quanta + Caldera (12 sections each)
  proposals/               # Proposal & SOW options (Sprint 7)
    types.ts               # Proposal, ProposalOption, ImplementationCredit
    helpers.ts             # OPTION_TYPE_LABEL/TONE, status labels/tones
    mock-proposals.ts      # Seeded 3-tier proposal for Caldera
  intake/                  # Stakeholder intake (Sprint 5)
    types.ts               # Stakeholder, RoleCoverageRow, SupportingInput, IntakeRecord
    helpers.ts             # Role/status/quality labels and tones, filter set
    mock-intake.ts         # IntakeRecord per engagement
  findings/                # Findings review (Sprint 5)
    types.ts               # Finding, SourceRef, FindingCategory, etc.
    helpers.ts             # Review status / confidence / category labels and tones
    mock-findings.ts       # Seeded findings per engagement
styles/
  globals.css              # Design tokens (CSS variables) + base styles
scripts/
  capture-screenshots.cjs  # Sprint 1 capture
  capture-sprint-2.cjs     # Sprint 2 capture (seeds localStorage for results)
  capture-sprint-3.cjs     # Sprint 3 capture (leads, lead detail, /apply, /scorecard/results)
  capture-sprint-4.cjs     # Sprint 4 capture (engagements list + 4 detail variants + linked lead)
  capture-sprint-5.cjs     # Sprint 5 capture (intake + findings × multiple engagement states)
  capture-sprint-6.cjs     # Sprint 6 capture (opportunities + roadmap × multiple engagement states)
  capture-sprint-7.cjs     # Sprint 7 capture (report + proposal × multiple engagement states)
docs/
  00–07                    # Canon (do not drift)
  08 CURRENT_STATUS.md
  09 DECISION_LOG.md
  10 SESSION_HANDOFF.md    # This file
  11 VISUAL_UX_AUDIT_PROTOCOL.md
  screenshots/sprint-1/
  screenshots/sprint-2/
  screenshots/sprint-3/
  screenshots/sprint-4/
  screenshots/sprint-5/
  screenshots/sprint-6/
  screenshots/sprint-7/
  screenshots/mvp-stabilization/
  persistence/                      # Persistence/Auth architecture canon (Sprint P0)
    00_PERSISTENCE_AUTH_CANON.md
    01_DATA_MODEL_DRAFT.md
    02_MIGRATION_SEQUENCE.md
    03_SECURITY_AND_RLS_DRAFT.md
supabase/                           # Persistence/Auth Step 0/1
  migrations/
    0001_auth_workspaces_profiles.sql
    README.md
middleware.ts                       # /app/* auth guard + session refresh
.env.example                        # names only — never commit .env.local
```

### Auth-related files added in Step 0/1

```
lib/env.ts                          # safe env getters; throws at call time only
lib/supabase/client.ts              # browser client (anon key only)
lib/supabase/server.ts              # server client bound to cookies()
lib/supabase/middleware.ts          # updateSession(request) helper
lib/auth/actions.ts                 # signInWithMagicLink, signOut server actions
lib/auth/identity.ts                # getOperatorIdentity() with safe fallbacks
lib/auth/operator-allowlist.ts      # email + domain allowlist (fail-closed)
components/auth/login-form.tsx      # client form using server action; useFormStatus
app/login/page.tsx                  # premium dark SLATE-styled login surface
app/auth/callback/route.ts          # magic-link code → session exchange
scripts/dev/configure-supabase-smtp.cjs  # dev-only Mgmt API SMTP patch
scripts/dev/probe-smtp-auth.cjs          # dev-only SMTP AUTH probe (no mail sent)
```

---

## Local Development

```bash
npm install
npm run dev         # http://localhost:3000  (redirects to /app)
npm run lint
npm run build
```

Both `lint` and `build` are clean as of end of Sprint 1.

---

## Design System Cheat Sheet

**Tokens** (`styles/globals.css`):

- Surfaces: `bg-bg-page`, `bg-bg-shell`, `bg-bg-surface`, `bg-bg-elevated`, `bg-bg-panel`
- Borders: `border-border-subtle`, `border-border-strong`
- Text: `text-text-primary`, `text-text-secondary`, `text-text-muted`, `text-text-disabled`
- Brand / practice: `text-brand-primary`, `text-practice-ai`, `text-practice-dev`, `text-practice-studio`
- Status: `text-status-success | warning | risk | critical | info | neutral`
- Shadows: `shadow-card`, `shadow-elevated`

**Type**: `font-sans` (Inter) for UI, `font-mono` (JetBrains Mono) for scores, IDs, eyebrows.

**Motion**: `animate-fade-up`, `animate-pulse-soft`. Keep it restrained.

---

## Conventions

- Components use named exports. No default exports outside `app/` route files.
- `"use client"` only when needed (state, effects, navigation hooks).
- Every primitive accepts `className` and forwards it through `cn()`.
- Mock data is the single source of truth until a real data layer is added — do not introduce ad-hoc inline mocks in components.
- AI-generated content must always be visibly labeled (`Badge tone="ai"` or similar).
- Empty states are required for every list/feed view, even when mock data is present.
- New routes referenced in the sidebar should either be implemented or render as locked.

---

## Persistence/Auth Step 0 + Step 1 — Status & Local Setup

Step 0 (Supabase setup + env scaffolding) and Step 1 (auth shell + operator login) are implemented. The full lifecycle UI under `/app/*` continues to render mock domain data — only auth/session/profile/workspace are real now.

### Local setup checklist

1. Copy `.env.example` to `.env.local` and fill in real values from the Supabase project dashboard. Never commit `.env.local`. The service role key stays server-only — do not prefix it `NEXT_PUBLIC_`.
2. **Populate the operator allowlist.** Set `SLATE_OPERATOR_DOMAIN_ALLOWLIST=saipienlabs.com` (and/or list explicit emails in `SLATE_OPERATOR_EMAIL_ALLOWLIST=`). If both are empty, every sign-in attempt is rejected — the policy is fail-closed.
3. In Supabase Dashboard → SQL Editor (or `supabase db push`), apply `supabase/migrations/0001_auth_workspaces_profiles.sql`. The migration is idempotent.
4. In Supabase Dashboard → Authentication → URL Configuration, set:
   - Site URL: `http://localhost:3000` (or your deployed origin / dev port)
   - Redirect URLs: include the matching `/auth/callback` for each port/origin you'll exercise (`http://localhost:3000/auth/callback`, `http://localhost:3001/auth/callback`, prod, etc.)
5. In Supabase Dashboard → Authentication → Providers → Email, enable magic link. **For operator auth, keep Supabase signups restricted where possible — disable "Allow new users to sign up" if your project plan exposes that toggle.** SLATE also enforces a server-side operator allowlist in `signInWithMagicLink`; both layers should remain in place before Step 2 begins.
6. Invite each Saipien Labs operator via Supabase Dashboard → Authentication → Users. The `on_auth_user_created` trigger automatically creates a corresponding `profiles` row.
7. (Optional) Configure custom SMTP. Supabase's free-tier email cap is ~3/hour and rate-limits aggressively during dev. Two helpers under `scripts/dev/` automate this for the SLATE Mailgun account: `configure-supabase-smtp.cjs` PATCHes Supabase Auth via the Management API, and `probe-smtp-auth.cjs` connects to the SMTP host with `AUTH LOGIN` to verify creds without sending mail. Both are dev-only, read everything from `process.env`, and redact secrets from output.
8. `npm run dev` and sign in at `/login`.

## Starting Persistence/Auth Implementation

**Goal.** Replace seeded mock data route-by-route while preserving the accepted UI surface.

**Order.** Follow `docs/persistence/02_MIGRATION_SEQUENCE.md` strictly. Steps 0–10 are complete; the canon ends here. Future work is documentation-mode only until a fresh workstream (AI synthesis, production export, or storage hardening) is scoped.

**Per-step end state.** `npm run lint` clean, `npm run build` clean, the visual UX audit protocol passes against the affected routes, and the per-step acceptance criteria from the migration sequence doc are met.

**Reuse.** Every existing `lib/<domain>/types.ts` is the contract for the corresponding `queries.ts` file. The component surface in `components/` should not change. The `LockedActionButton` pattern stays — locked CTAs (`Export Report`, `Send to Client`, `Prepare SOW Draft`, `Prepare Client Review`) remain locked through the persistence sprint.

**Boundaries.** Per `docs/persistence/00_PERSISTENCE_AUTH_CANON.md`: no real AI synthesis, no production document export, no e-signature, no email delivery, no billing, no real CRM integration, no BuildOps. Each is its own future workstream.

**End-of-sprint.** Re-run the MVP acceptance audit against the persisted product on Quanta and Caldera demo paths. After sign-off, scope the next workstream (likely AI synthesis for findings, then real export, then BuildOps).

---

## Historical: Starting Post-MVP — Stabilization + End-to-End Polish

**Goal.** Run a final cross-sprint visual + accessibility + copy audit, then a stabilization window that hardens the seven-stage demo path. No new feature scope until stabilization is signed off.

**Recommended sequence.**

1. **Final Sprint 7 visual UX audit.** Capture state for the report and proposal pages on Quanta and Caldera, plus the empty-state pages on Helio / Meridian / Atlas. Verify recommended-action helper across all seven engagement-related pages routes correctly and never loops.
2. **End-to-end demo path.** Walk the full lifecycle on Quanta (Lead → Engagement → Intake → Findings → Opportunities → Roadmap → Report) and Caldera (… → Report → Proposal). Confirm every CTA either links to a real destination or carries a `LockedActionButton` with sprint label.
3. **Cross-sprint visual consistency.** Confirm shared primitives are used uniformly: `LockedActionButton`, `EngagementRecommendedActionCard` (`href` / `lockedNote` / `selfReference`), `MetricCard` zero-state copy, `ScoreCard` per-dimension banding, segmented filter tabs.
4. **Cross-sprint accessibility pass.** Verify `aria-pressed` on every selectable button (findings list, opportunities list + matrix cells, report outline, proposal options), `aria-describedby` on the matrix axes, locked-CTA `aria-label` strings, and color contrast on tonal text.
5. **Microcopy pass.** Confirm AI-drafted content is always labeled, boundary reminders are present on every deep page, and pricing copy never reads as final.

**Out of scope (post-stabilization).** BuildOps surfaces, StudioOps surfaces, ClientOps surfaces, real backend, real auth, real AI synthesis, production export, real SOW execution. Each of these is its own dedicated multi-sprint workstream.

**End-of-stabilization.** Update `docs/08_CURRENT_STATUS.md` to mark stabilization complete, append a final decision-log entry on stabilization sign-off, and decide the next workstream (likely real persistence as the unlock for the existing UI).

---

## Historical: Starting Sprint 7 — Report + Proposal Builder

**Goal.** Build the final two AI Opportunity Sprint deliverables.

- `/app/engagements/[id]/report` — assemble the audit report section by section, with linked evidence and approved findings
- `/app/engagements/[id]/proposal` — three-tier SOW options (Quick-Win Build, AI Workflow System, Managed AI Partner) with implementation credit, assumptions, and pricing placeholders

**Entry points (currently locked).** `ReportStatusPanel` and `ProposalStatusPanel` on the engagement detail page. The roadmap page header's `Prepare Report` button is locked with `Sprint 7`. The recommended-action helper already returns `lockedNote: "Sprint 7"` for `report` and `proposal` stages — flip those to live `href`s once the routes ship.

**Reuse from Sprints 1–6.**
- Approved + report-ready findings from `lib/findings/mock-findings.ts` are the report's content layer; `Finding.sourceRefs[]` carries the evidence trail.
- Opportunities from `lib/opportunities/` and roadmap items from `lib/roadmap/` are the proposal's content layer (tiered SOW pulls from quadrant + phase combinations).
- `EngagementProfileHeader`, `EngagementStageTracker`, `EngagementContextCard`, `EngagementRecommendedActionCard`, `OpportunityScoreStrip`, `EvidencePanel` (from findings).
- `Card`, `Badge`, `MetricCard`, `Button`, `EmptyState`, the recommended-action helper.

**New components likely needed.** `ReportOutlineSidebar`, `ReportSectionCard`, `ReportSectionEditor`, `ReportSectionStatusChip`, `LinkedFindingsPanel`, `ExportReportButton`, `ProposalOptionCard`, `ScopeBuilderPanel`, `PricingPlaceholderPanel`, `ImplementationCreditPanel`, `AssumptionsPanel`, `ExportProposalButton`.

**Boundaries.** Mock content, no real export, no backend. Report and proposal sections must surface AI authorship and require human approval. No real PDF generation, no actual SOW PDF — placeholder only.

**End-of-sprint.** Run the audit protocol, capture screenshots to `docs/screenshots/sprint-7/`, update `docs/08_CURRENT_STATUS.md`, append to `docs/09_DECISION_LOG.md` for any material decisions, and close out the MVP arc in this handoff.

---

## Out-of-Scope Reminders

Do **not** introduce in upcoming sprints unless the sprint goal explicitly says so:

- Authentication, authorization, RBAC
- Backend services or databases
- Real AI model calls
- Email/notification delivery
- Billing / time tracking
- BuildOps, StudioOps, ClientOps modules
- Production export (PDF generation, etc.)
- Light theme

These are listed in `docs/02_SLATE_MVP_SCOPE.md` under Out of Scope.
